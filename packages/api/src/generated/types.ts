// This file is auto-generated by scripts/generate.mjs
// Do not edit manually. Run `pnpm --filter @bpmn-sdk/api generate` to regenerate.

/** Audit log item. */
export interface AuditLogResult {
    /** The unique key of the audit log entry. */
    auditLogKey: string;
    /** System-generated entity key for an audit log entry. */
    entityKey: string;
    /** The type of entity affected by the operation. */
    entityType: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
    /** The type of operation performed. */
    operationType: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";
    /** Key of the batch operation. */
    batchOperationKey: string;
    /** The type of batch operation performed, if this is part of a batch. */
    batchOperationType: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
    /** The timestamp when the operation occurred. */
    timestamp: string;
    /** The ID of the actor who performed the operation. */
    actorId: string | null;
    /** The type of the actor who performed the operation. */
    actorType: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
    /** The element ID of the agent that performed the operation (e.g. ad-hoc subprocess element ID). */
    agentElementId: string | null;
    /** The tenant ID of the audit log. */
    tenantId: string;
    /** The result status of the operation. */
    result: "FAIL" | "SUCCESS";
    /** Additional notes about the operation. */
    annotation: string | null;
    /** The category of the audit log operation. */
    category: "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS";
    /** The process definition ID. */
    processDefinitionId: string;
    /** The key of the process definition. */
    processDefinitionKey: string;
    /** The key of the process instance. */
    processInstanceKey: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** The key of the element instance. */
    elementInstanceKey: string;
    /** The key of the job. */
    jobKey: string;
    /** The key of the user task. */
    userTaskKey: string;
    /** The decision requirements ID. */
    decisionRequirementsId: string | null;
    /** The assigned key of the decision requirements. */
    decisionRequirementsKey: string;
    /** The decision definition ID. */
    decisionDefinitionId: string;
    /** The key of the decision definition. */
    decisionDefinitionKey: string;
    /** The key of the decision evaluation. */
    decisionEvaluationKey: string;
    /** The key of the deployment. */
    deploymentKey: string;
    /** The key of the form. */
    formKey: string;
    /** The system-assigned key for this resource. */
    resourceKey: string;
    /**  The key of the related entity. The content depends on the operation type and entity type.
 For example, for authorization operations, this will contain the ID of the owner (e.g., user or group) the authorization belongs to. */
    relatedEntityKey: string;
    /**  The type of the related entity. The content depends on the operation type and entity type.
 For example, for authorization operations, this will contain the type of the owner (e.g., USER or GROUP) the authorization belongs to. */
    relatedEntityType: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
    /**  Additional description of the entity affected by the operation.
 For example, for variable operations, this will contain the variable name. */
    entityDescription: string | null;
}

export interface AuditLogSearchQuerySortRequest {
    /** The field to sort by. */
    field: "actorId" | "actorType" | "annotation" | "auditLogKey" | "batchOperationKey" | "batchOperationType" | "category" | "decisionDefinitionId" | "decisionDefinitionKey" | "decisionEvaluationKey" | "decisionRequirementsId" | "decisionRequirementsKey" | "elementInstanceKey" | "entityKey" | "entityType" | "jobKey" | "operationType" | "processDefinitionId" | "processDefinitionKey" | "processInstanceKey" | "result" | "tenantId" | "timestamp" | "userTaskKey";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Audit log search request. */
export type AuditLogSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Audit log filter request */
export interface AuditLogFilter {
    /** The audit log key search filter. */
    auditLogKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The process definition key search filter. */
    processDefinitionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The process instance key search filter. */
    processInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The element instance key search filter. */
    elementInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The operation type search filter. */
    operationType?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE" | {
      /** Checks for equality with the provided value. */
      $eq?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";
      /** Checks for inequality with the provided value. */
      $neq?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The result search filter. */
    result?: "FAIL" | "SUCCESS" | {
      /** Checks for equality with the provided value. */
      $eq?: "FAIL" | "SUCCESS";
      /** Checks for inequality with the provided value. */
      $neq?: "FAIL" | "SUCCESS";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"FAIL" | "SUCCESS">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The timestamp search filter. */
    timestamp?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The actor ID search filter. */
    actorId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The actor type search filter. */
    actorType?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER" | {
      /** Checks for equality with the provided value. */
      $eq?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
      /** Checks for inequality with the provided value. */
      $neq?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The agent element ID search filter. */
    agentElementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The entity key search filter. */
    entityKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The entity type search filter. */
    entityType?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT" | {
      /** Checks for equality with the provided value. */
      $eq?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
      /** Checks for inequality with the provided value. */
      $neq?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The tenant ID search filter. */
    tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The category search filter. */
    category?: "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS" | {
      /** Checks for equality with the provided value. */
      $eq?: "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS";
      /** Checks for inequality with the provided value. */
      $neq?: "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The deployment key search filter. */
    deploymentKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The form key search filter. */
    formKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The resource key search filter. */
    resourceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The batch operation type search filter. */
    batchOperationType?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE" | {
      /** Checks for equality with the provided value. */
      $eq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
      /** Checks for inequality with the provided value. */
      $neq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The process definition ID search filter. */
    processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The job key search filter. */
    jobKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The user task key search filter. */
    userTaskKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The decision requirements ID search filter. */
    decisionRequirementsId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The decision requirements key search filter. */
    decisionRequirementsKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The decision definition ID search filter. */
    decisionDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The decision definition key search filter. */
    decisionDefinitionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The decision evaluation key search filter. */
    decisionEvaluationKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The related entity key search filter. */
    relatedEntityKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The related entity type search filter. */
    relatedEntityType?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT" | {
      /** Checks for equality with the provided value. */
      $eq?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
      /** Checks for inequality with the provided value. */
      $neq?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The entity description filter. */
    entityDescription?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
}

/** Audit log search response. */
export interface AuditLogSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

/** System-generated entity key for an audit log entry. */
export type AuditLogEntityKey = string;

/** The type of entity affected by the operation. */
export type AuditLogEntityTypeEnum = "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";

/** The type of operation performed. */
export type AuditLogOperationTypeEnum = "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";

/** The type of actor who performed the operation. */
export type AuditLogActorTypeEnum = "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";

/** The result status of the operation. */
export type AuditLogResultEnum = "FAIL" | "SUCCESS";

/** The category of the audit log operation. */
export type AuditLogCategoryEnum = "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS";

/** EntityKey property with full advanced search capabilities. */
export type AuditLogEntityKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced entityKey filter. */
export interface AdvancedAuditLogEntityKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** AuditLogEntityTypeEnum property with full advanced search capabilities. */
export type EntityTypeFilterProperty = "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT" | {
    /** Checks for equality with the provided value. */
    $eq?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
    /** Checks for inequality with the provided value. */
    $neq?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced AuditLogEntityTypeEnum filter. */
export interface AdvancedEntityTypeFilter {
    /** Checks for equality with the provided value. */
    $eq?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
    /** Checks for inequality with the provided value. */
    $neq?: "AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"AUTHORIZATION" | "BATCH" | "DECISION" | "GROUP" | "INCIDENT" | "JOB" | "MAPPING_RULE" | "PROCESS_INSTANCE" | "RESOURCE" | "ROLE" | "TENANT" | "USER" | "USER_TASK" | "VARIABLE" | "CLIENT">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** AuditLogOperationTypeEnum property with full advanced search capabilities. */
export type OperationTypeFilterProperty = "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE" | {
    /** Checks for equality with the provided value. */
    $eq?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";
    /** Checks for inequality with the provided value. */
    $neq?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced AuditLogOperationTypeEnum filter. */
export interface AdvancedOperationTypeFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";
    /** Checks for inequality with the provided value. */
    $neq?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** AuditLogCategoryEnum property with full advanced search capabilities. */
export type CategoryFilterProperty = "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS" | {
    /** Checks for equality with the provided value. */
    $eq?: "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS";
    /** Checks for inequality with the provided value. */
    $neq?: "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced AuditLogCategoryEnum filter. */
export interface AdvancedCategoryFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS";
    /** Checks for inequality with the provided value. */
    $neq?: "ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ADMIN" | "DEPLOYED_RESOURCES" | "USER_TASKS">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** AuditLogResultEnum property with full advanced search capabilities. */
export type AuditLogResultFilterProperty = "FAIL" | "SUCCESS" | {
    /** Checks for equality with the provided value. */
    $eq?: "FAIL" | "SUCCESS";
    /** Checks for inequality with the provided value. */
    $neq?: "FAIL" | "SUCCESS";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"FAIL" | "SUCCESS">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced AuditLogResultEnum filter. */
export interface AdvancedResultFilter {
    /** Checks for equality with the provided value. */
    $eq?: "FAIL" | "SUCCESS";
    /** Checks for inequality with the provided value. */
    $neq?: "FAIL" | "SUCCESS";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"FAIL" | "SUCCESS">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** AuditLogActorTypeEnum property with full advanced search capabilities. */
export type AuditLogActorTypeFilterProperty = "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER" | {
    /** Checks for equality with the provided value. */
    $eq?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
    /** Checks for inequality with the provided value. */
    $neq?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced AuditLogActorTypeEnum filter. */
export interface AdvancedActorTypeFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
    /** Checks for inequality with the provided value. */
    $neq?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

export interface CamundaUserResult {
    /** The username of the user. */
    username?: string;
    /** The display name of the user. */
    displayName?: string | null;
    /** The email of the user. */
    email?: string | null;
    /** The web components the user is authorized to use. */
    authorizedComponents: Array<string>;
    /** The tenants the user is a member of. */
    tenants: Array<{
      /** The tenant name. */
      name?: string;
      /** The unique identifier of the tenant. */
      tenantId?: string;
      /** The tenant description. */
      description?: string;
  }>;
    /** The groups assigned to the user. */
    groups: Array<string>;
    /** The roles assigned to the user. */
    roles: Array<string>;
    /** The plan of the user. */
    salesPlanType: string | null;
    /** The links to the components in the C8 stack. */
    c8Links: {
      [key: string]: string;
  };
    /** Flag for understanding if the user is able to perform logout. */
    canLogout: boolean;
}

export interface AuthorizationIdBasedRequest {
    /** The ID of the owner of the permissions. */
    ownerId: string;
    /** The type of the owner of permissions. */
    ownerType: "USER" | "CLIENT" | "ROLE" | "GROUP" | "MAPPING_RULE" | "UNSPECIFIED";
    /** The ID of the resource to add permissions to. */
    resourceId: string;
    /** The type of resource to add permissions to. */
    resourceType: "AUDIT_LOG" | "AUTHORIZATION" | "BATCH" | "CLUSTER_VARIABLE" | "COMPONENT" | "DECISION_DEFINITION" | "DECISION_REQUIREMENTS_DEFINITION" | "DOCUMENT" | "EXPRESSION" | "GLOBAL_LISTENER" | "GROUP" | "MAPPING_RULE" | "MESSAGE" | "PROCESS_DEFINITION" | "RESOURCE" | "ROLE" | "SYSTEM" | "TENANT" | "USER" | "USER_TASK";
    /** The permission types to add. */
    permissionTypes: Array<"ACCESS" | "CANCEL_PROCESS_INSTANCE" | "CLAIM" | "CLAIM_USER_TASK" | "COMPLETE" | "COMPLETE_USER_TASK" | "CREATE" | "CREATE_BATCH_OPERATION_CANCEL_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_DECISION_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_DECISION_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MIGRATE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MODIFY_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_RESOLVE_INCIDENT" | "CREATE_DECISION_INSTANCE" | "CREATE_PROCESS_INSTANCE" | "CREATE_TASK_LISTENER" | "DELETE" | "DELETE_DECISION_INSTANCE" | "DELETE_DRD" | "DELETE_FORM" | "DELETE_PROCESS" | "DELETE_PROCESS_INSTANCE" | "DELETE_RESOURCE" | "DELETE_TASK_LISTENER" | "EVALUATE" | "MODIFY_PROCESS_INSTANCE" | "READ" | "READ_DECISION_DEFINITION" | "READ_DECISION_INSTANCE" | "READ_JOB_METRIC" | "READ_PROCESS_DEFINITION" | "READ_PROCESS_INSTANCE" | "READ_USAGE_METRIC" | "READ_USER_TASK" | "READ_TASK_LISTENER" | "UPDATE" | "UPDATE_PROCESS_INSTANCE" | "UPDATE_USER_TASK" | "UPDATE_TASK_LISTENER">;
}

export interface AuthorizationPropertyBasedRequest {
    /** The ID of the owner of the permissions. */
    ownerId: string;
    /** The type of the owner of permissions. */
    ownerType: "USER" | "CLIENT" | "ROLE" | "GROUP" | "MAPPING_RULE" | "UNSPECIFIED";
    /** The name of the resource property on which this authorization is based. */
    resourcePropertyName: string;
    /** The type of resource to add permissions to. */
    resourceType: "AUDIT_LOG" | "AUTHORIZATION" | "BATCH" | "CLUSTER_VARIABLE" | "COMPONENT" | "DECISION_DEFINITION" | "DECISION_REQUIREMENTS_DEFINITION" | "DOCUMENT" | "EXPRESSION" | "GLOBAL_LISTENER" | "GROUP" | "MAPPING_RULE" | "MESSAGE" | "PROCESS_DEFINITION" | "RESOURCE" | "ROLE" | "SYSTEM" | "TENANT" | "USER" | "USER_TASK";
    /** The permission types to add. */
    permissionTypes: Array<"ACCESS" | "CANCEL_PROCESS_INSTANCE" | "CLAIM" | "CLAIM_USER_TASK" | "COMPLETE" | "COMPLETE_USER_TASK" | "CREATE" | "CREATE_BATCH_OPERATION_CANCEL_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_DECISION_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_DECISION_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MIGRATE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MODIFY_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_RESOLVE_INCIDENT" | "CREATE_DECISION_INSTANCE" | "CREATE_PROCESS_INSTANCE" | "CREATE_TASK_LISTENER" | "DELETE" | "DELETE_DECISION_INSTANCE" | "DELETE_DRD" | "DELETE_FORM" | "DELETE_PROCESS" | "DELETE_PROCESS_INSTANCE" | "DELETE_RESOURCE" | "DELETE_TASK_LISTENER" | "EVALUATE" | "MODIFY_PROCESS_INSTANCE" | "READ" | "READ_DECISION_DEFINITION" | "READ_DECISION_INSTANCE" | "READ_JOB_METRIC" | "READ_PROCESS_DEFINITION" | "READ_PROCESS_INSTANCE" | "READ_USAGE_METRIC" | "READ_USER_TASK" | "READ_TASK_LISTENER" | "UPDATE" | "UPDATE_PROCESS_INSTANCE" | "UPDATE_USER_TASK" | "UPDATE_TASK_LISTENER">;
}

/**  Defines an authorization request.
 *  Either an id-based or a property-based authorization can be provided. */
export type AuthorizationRequest = {
    /** The ID of the owner of the permissions. */
    ownerId: string;
    /** The type of the owner of permissions. */
    ownerType: "USER" | "CLIENT" | "ROLE" | "GROUP" | "MAPPING_RULE" | "UNSPECIFIED";
    /** The ID of the resource to add permissions to. */
    resourceId: string;
    /** The type of resource to add permissions to. */
    resourceType: "AUDIT_LOG" | "AUTHORIZATION" | "BATCH" | "CLUSTER_VARIABLE" | "COMPONENT" | "DECISION_DEFINITION" | "DECISION_REQUIREMENTS_DEFINITION" | "DOCUMENT" | "EXPRESSION" | "GLOBAL_LISTENER" | "GROUP" | "MAPPING_RULE" | "MESSAGE" | "PROCESS_DEFINITION" | "RESOURCE" | "ROLE" | "SYSTEM" | "TENANT" | "USER" | "USER_TASK";
    /** The permission types to add. */
    permissionTypes: Array<"ACCESS" | "CANCEL_PROCESS_INSTANCE" | "CLAIM" | "CLAIM_USER_TASK" | "COMPLETE" | "COMPLETE_USER_TASK" | "CREATE" | "CREATE_BATCH_OPERATION_CANCEL_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_DECISION_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_DECISION_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MIGRATE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MODIFY_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_RESOLVE_INCIDENT" | "CREATE_DECISION_INSTANCE" | "CREATE_PROCESS_INSTANCE" | "CREATE_TASK_LISTENER" | "DELETE" | "DELETE_DECISION_INSTANCE" | "DELETE_DRD" | "DELETE_FORM" | "DELETE_PROCESS" | "DELETE_PROCESS_INSTANCE" | "DELETE_RESOURCE" | "DELETE_TASK_LISTENER" | "EVALUATE" | "MODIFY_PROCESS_INSTANCE" | "READ" | "READ_DECISION_DEFINITION" | "READ_DECISION_INSTANCE" | "READ_JOB_METRIC" | "READ_PROCESS_DEFINITION" | "READ_PROCESS_INSTANCE" | "READ_USAGE_METRIC" | "READ_USER_TASK" | "READ_TASK_LISTENER" | "UPDATE" | "UPDATE_PROCESS_INSTANCE" | "UPDATE_USER_TASK" | "UPDATE_TASK_LISTENER">;
} | {
    /** The ID of the owner of the permissions. */
    ownerId: string;
    /** The type of the owner of permissions. */
    ownerType: "USER" | "CLIENT" | "ROLE" | "GROUP" | "MAPPING_RULE" | "UNSPECIFIED";
    /** The name of the resource property on which this authorization is based. */
    resourcePropertyName: string;
    /** The type of resource to add permissions to. */
    resourceType: "AUDIT_LOG" | "AUTHORIZATION" | "BATCH" | "CLUSTER_VARIABLE" | "COMPONENT" | "DECISION_DEFINITION" | "DECISION_REQUIREMENTS_DEFINITION" | "DOCUMENT" | "EXPRESSION" | "GLOBAL_LISTENER" | "GROUP" | "MAPPING_RULE" | "MESSAGE" | "PROCESS_DEFINITION" | "RESOURCE" | "ROLE" | "SYSTEM" | "TENANT" | "USER" | "USER_TASK";
    /** The permission types to add. */
    permissionTypes: Array<"ACCESS" | "CANCEL_PROCESS_INSTANCE" | "CLAIM" | "CLAIM_USER_TASK" | "COMPLETE" | "COMPLETE_USER_TASK" | "CREATE" | "CREATE_BATCH_OPERATION_CANCEL_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_DECISION_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_DECISION_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MIGRATE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MODIFY_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_RESOLVE_INCIDENT" | "CREATE_DECISION_INSTANCE" | "CREATE_PROCESS_INSTANCE" | "CREATE_TASK_LISTENER" | "DELETE" | "DELETE_DECISION_INSTANCE" | "DELETE_DRD" | "DELETE_FORM" | "DELETE_PROCESS" | "DELETE_PROCESS_INSTANCE" | "DELETE_RESOURCE" | "DELETE_TASK_LISTENER" | "EVALUATE" | "MODIFY_PROCESS_INSTANCE" | "READ" | "READ_DECISION_DEFINITION" | "READ_DECISION_INSTANCE" | "READ_JOB_METRIC" | "READ_PROCESS_DEFINITION" | "READ_PROCESS_INSTANCE" | "READ_USAGE_METRIC" | "READ_USER_TASK" | "READ_TASK_LISTENER" | "UPDATE" | "UPDATE_PROCESS_INSTANCE" | "UPDATE_USER_TASK" | "UPDATE_TASK_LISTENER">;
};

export interface AuthorizationSearchQuerySortRequest {
    /** The field to sort by. */
    field: "ownerId" | "ownerType" | "resourceId" | "resourcePropertyName" | "resourceType";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export type AuthorizationSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Authorization search filter. */
export interface AuthorizationFilter {
    /** The ID of the owner of permissions. */
    ownerId?: string;
    /** The type of the owner of permissions. */
    ownerType?: "USER" | "CLIENT" | "ROLE" | "GROUP" | "MAPPING_RULE" | "UNSPECIFIED";
    /** The IDs of the resource to search permissions for. */
    resourceIds?: Array<string>;
    /** The names of the resource properties to search permissions for. */
    resourcePropertyNames?: Array<string>;
    /** The type of resource to search permissions for. */
    resourceType?: "AUDIT_LOG" | "AUTHORIZATION" | "BATCH" | "CLUSTER_VARIABLE" | "COMPONENT" | "DECISION_DEFINITION" | "DECISION_REQUIREMENTS_DEFINITION" | "DOCUMENT" | "EXPRESSION" | "GLOBAL_LISTENER" | "GROUP" | "MAPPING_RULE" | "MESSAGE" | "PROCESS_DEFINITION" | "RESOURCE" | "ROLE" | "SYSTEM" | "TENANT" | "USER" | "USER_TASK";
}

export interface AuthorizationResult {
    /** The ID of the owner of permissions. */
    ownerId?: string;
    /** The type of the owner of permissions. */
    ownerType?: "USER" | "CLIENT" | "ROLE" | "GROUP" | "MAPPING_RULE" | "UNSPECIFIED";
    /** The type of resource that the permissions relate to. */
    resourceType?: "AUDIT_LOG" | "AUTHORIZATION" | "BATCH" | "CLUSTER_VARIABLE" | "COMPONENT" | "DECISION_DEFINITION" | "DECISION_REQUIREMENTS_DEFINITION" | "DOCUMENT" | "EXPRESSION" | "GLOBAL_LISTENER" | "GROUP" | "MAPPING_RULE" | "MESSAGE" | "PROCESS_DEFINITION" | "RESOURCE" | "ROLE" | "SYSTEM" | "TENANT" | "USER" | "USER_TASK";
    /** ID of the resource the permission relates to (mutually exclusive with `resourcePropertyName`). */
    resourceId?: string | null;
    /** The name of the resource property the permission relates to (mutually exclusive with `resourceId`). */
    resourcePropertyName: string | null;
    /** Specifies the types of the permissions. */
    permissionTypes: Array<"ACCESS" | "CANCEL_PROCESS_INSTANCE" | "CLAIM" | "CLAIM_USER_TASK" | "COMPLETE" | "COMPLETE_USER_TASK" | "CREATE" | "CREATE_BATCH_OPERATION_CANCEL_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_DECISION_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_DECISION_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MIGRATE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MODIFY_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_RESOLVE_INCIDENT" | "CREATE_DECISION_INSTANCE" | "CREATE_PROCESS_INSTANCE" | "CREATE_TASK_LISTENER" | "DELETE" | "DELETE_DECISION_INSTANCE" | "DELETE_DRD" | "DELETE_FORM" | "DELETE_PROCESS" | "DELETE_PROCESS_INSTANCE" | "DELETE_RESOURCE" | "DELETE_TASK_LISTENER" | "EVALUATE" | "MODIFY_PROCESS_INSTANCE" | "READ" | "READ_DECISION_DEFINITION" | "READ_DECISION_INSTANCE" | "READ_JOB_METRIC" | "READ_PROCESS_DEFINITION" | "READ_PROCESS_INSTANCE" | "READ_USAGE_METRIC" | "READ_USER_TASK" | "READ_TASK_LISTENER" | "UPDATE" | "UPDATE_PROCESS_INSTANCE" | "UPDATE_USER_TASK" | "UPDATE_TASK_LISTENER">;
    /** The key of the authorization. */
    authorizationKey?: string;
}

export interface AuthorizationSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface AuthorizationCreateResult {
    /** The key of the created authorization. */
    authorizationKey?: string;
}

/** Specifies the type of permissions. */
export type PermissionTypeEnum = "ACCESS" | "CANCEL_PROCESS_INSTANCE" | "CLAIM" | "CLAIM_USER_TASK" | "COMPLETE" | "COMPLETE_USER_TASK" | "CREATE" | "CREATE_BATCH_OPERATION_CANCEL_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_DECISION_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_DECISION_INSTANCE" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_DEFINITION" | "CREATE_BATCH_OPERATION_DELETE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MIGRATE_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_MODIFY_PROCESS_INSTANCE" | "CREATE_BATCH_OPERATION_RESOLVE_INCIDENT" | "CREATE_DECISION_INSTANCE" | "CREATE_PROCESS_INSTANCE" | "CREATE_TASK_LISTENER" | "DELETE" | "DELETE_DECISION_INSTANCE" | "DELETE_DRD" | "DELETE_FORM" | "DELETE_PROCESS" | "DELETE_PROCESS_INSTANCE" | "DELETE_RESOURCE" | "DELETE_TASK_LISTENER" | "EVALUATE" | "MODIFY_PROCESS_INSTANCE" | "READ" | "READ_DECISION_DEFINITION" | "READ_DECISION_INSTANCE" | "READ_JOB_METRIC" | "READ_PROCESS_DEFINITION" | "READ_PROCESS_INSTANCE" | "READ_USAGE_METRIC" | "READ_USER_TASK" | "READ_TASK_LISTENER" | "UPDATE" | "UPDATE_PROCESS_INSTANCE" | "UPDATE_USER_TASK" | "UPDATE_TASK_LISTENER";

/** The type of resource to add/remove permissions to/from. */
export type ResourceTypeEnum = "AUDIT_LOG" | "AUTHORIZATION" | "BATCH" | "CLUSTER_VARIABLE" | "COMPONENT" | "DECISION_DEFINITION" | "DECISION_REQUIREMENTS_DEFINITION" | "DOCUMENT" | "EXPRESSION" | "GLOBAL_LISTENER" | "GROUP" | "MAPPING_RULE" | "MESSAGE" | "PROCESS_DEFINITION" | "RESOURCE" | "ROLE" | "SYSTEM" | "TENANT" | "USER" | "USER_TASK";

/** The type of the owner of permissions. */
export type OwnerTypeEnum = "USER" | "CLIENT" | "ROLE" | "GROUP" | "MAPPING_RULE" | "UNSPECIFIED";

/** System-generated key for an authorization. */
export type AuthorizationKey = string;

/** The created batch operation. */
export interface BatchOperationCreatedResult {
    /** Key of the batch operation. */
    batchOperationKey: string;
    /** The type of the batch operation. */
    batchOperationType: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
}

export interface BatchOperationSearchQuerySortRequest {
    /** The field to sort by. */
    field: "batchOperationKey" | "operationType" | "state" | "startDate" | "endDate" | "actorType" | "actorId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Batch operation search request. */
export type BatchOperationSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Batch operation filter request. */
export interface BatchOperationFilter {
    /** The key (or operate legacy ID) of the batch operation. */
    batchOperationKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The type of the batch operation. */
    operationType?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE" | {
      /** Checks for equality with the provided value. */
      $eq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
      /** Checks for inequality with the provided value. */
      $neq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The state of the batch operation. */
    state?: "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The type of the actor who performed the operation. */
    actorType?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
    /** The ID of the actor who performed the operation. */
    actorId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
}

/** The batch operation search query result. */
export interface BatchOperationSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface BatchOperationResponse {
    /** Key or (Operate Legacy ID = UUID) of the batch operation. */
    batchOperationKey: string;
    /** The batch operation state. */
    state: "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED";
    /** The type of the batch operation. */
    batchOperationType: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
    /**  The start date of the batch operation.
 This is `null` if the batch operation has not yet started. */
    startDate?: string | null;
    /**  The end date of the batch operation.
 This is `null` if the batch operation is still running. */
    endDate?: string | null;
    /**  The type of the actor who performed the operation.
 This is `null` if the batch operation was created before 8.9,
 or if the actor information is not available. */
    actorType: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
    /** The ID of the actor who performed the operation. Available for batch operations created since 8.9. */
    actorId: string | null;
    /** The total number of items contained in this batch operation. */
    operationsTotalCount: number;
    /** The number of items which failed during execution of the batch operation. (e.g. because they are rejected by the Zeebe engine). */
    operationsFailedCount: number;
    /** The number of successfully completed tasks. */
    operationsCompletedCount: number;
    /** The errors that occurred per partition during the batch operation. */
    errors: Array<{
      /** The partition ID where the error occurred. */
      partitionId?: number;
      /** The type of the error that occurred during the batch operation. */
      "type"?: "QUERY_FAILED" | "RESULT_BUFFER_SIZE_EXCEEDED";
      /** The error message that occurred during the batch operation. */
      message?: string;
  }>;
}

export interface BatchOperationError {
    /** The partition ID where the error occurred. */
    partitionId?: number;
    /** The type of the error that occurred during the batch operation. */
    "type"?: "QUERY_FAILED" | "RESULT_BUFFER_SIZE_EXCEEDED";
    /** The error message that occurred during the batch operation. */
    message?: string;
}

export interface BatchOperationItemSearchQuerySortRequest {
    /** The field to sort by. */
    field: "batchOperationKey" | "itemKey" | "processInstanceKey" | "processedDate" | "state";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Batch operation item search request. */
export type BatchOperationItemSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Batch operation item filter request. */
export interface BatchOperationItemFilter {
    /** The key (or operate legacy ID) of the batch operation. */
    batchOperationKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The key of the item, e.g. a process instance key. */
    itemKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The process instance key of the processed item. */
    processInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The state of the batch operation. */
    state?: "ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The type of the batch operation. */
    operationType?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE" | {
      /** Checks for equality with the provided value. */
      $eq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
      /** Checks for inequality with the provided value. */
      $neq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
}

export interface BatchOperationItemSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface BatchOperationItemResponse {
    /** The type of the batch operation. */
    operationType?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
    /** The key (or operate legacy ID) of the batch operation. */
    batchOperationKey?: string;
    /** Key of the item, e.g. a process instance key. */
    itemKey?: string;
    /** the process instance key of the processed item. */
    processInstanceKey?: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** State of the item. */
    state?: "ACTIVE" | "COMPLETED" | "SKIPPED" | "CANCELED" | "FAILED";
    /** the date this item was processed. */
    processedDate?: string;
    /** the error message from the engine in case of a failed operation. */
    errorMessage?: string | null;
}

/** The decision instance filter that defines which decision instances should be deleted. */
export interface DecisionInstanceDeletionBatchOperationRequest {
    /** The decision instance filter. */
    filter: {
      /** The key of the decision evaluation instance. */
      decisionEvaluationInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The state of the decision instance. */
      state?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN" | {
        /** Checks for equality with the provided value. */
        $eq?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
        /** Checks for inequality with the provided value. */
        $neq?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<"EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN">;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<"EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN">;
        /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
        $like?: string;
    };
      /** The evaluation failure of the decision instance. */
      evaluationFailure?: string;
      /** The evaluation date of the decision instance. */
      evaluationDate?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: string;
        /** Greater than or equal comparison with the provided value. */
        $gte?: string;
        /** Lower than comparison with the provided value. */
        $lt?: string;
        /** Lower than or equal comparison with the provided value. */
        $lte?: string;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
    };
      /** The ID of the DMN decision. */
      decisionDefinitionId?: string;
      /** The name of the DMN decision. */
      decisionDefinitionName?: string;
      /** The version of the decision. */
      decisionDefinitionVersion?: number;
      /** The type of the decision. UNSPECIFIED is deprecated and should not be used anymore, for removal in 8.10 */
      decisionDefinitionType?: "DECISION_TABLE" | "LITERAL_EXPRESSION" | "UNSPECIFIED" | "UNKNOWN";
      /** The tenant ID of the decision instance. */
      tenantId?: string;
      /** The key of the parent decision evaluation. Note that this is not the identifier of an individual decision instance; the `decisionEvaluationInstanceKey` is the identifier for a decision instance. */
      decisionEvaluationKey?: string;
      /** The key of the process definition. */
      processDefinitionKey?: string;
      /** The key of the process instance. */
      processInstanceKey?: string;
      /** The key of the decision. */
      decisionDefinitionKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The key of the element instance this decision instance is linked to. */
      elementInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The key of the root decision definition. */
      rootDecisionDefinitionKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The key of the decision requirements definition. */
      decisionRequirementsKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
  };
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

/** The process instance filter that defines which process instances should be canceled. */
export interface ProcessInstanceCancellationBatchOperationRequest {
    /** The process instance filter. */
    filter: {
  processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionName?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionVersion?: number | {
        /** Checks for equality with the provided value. */
        $eq?: number;
        /** Checks for inequality with the provided value. */
        $neq?: number;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: number;
        /** Greater than or equal comparison with the provided value. */
        $gte?: number;
        /** Lower than comparison with the provided value. */
        $lt?: number;
        /** Lower than or equal comparison with the provided value. */
        $lte?: number;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<number>;
    };
  processDefinitionVersionTag?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
  $or?: Array<{
        /** The start date. */
        startDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The end date. */
        endDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The process instance state. */
        state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** Whether this process instance has a related incident or not. */
        hasIncident?: boolean;
        /** The tenant id. */
        tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The process instance variables. */
        variables?: Array<{
          /** Name of the variable. */
          name: string;
          /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
          value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      }>;
        /** The key of this process instance. */
        processInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent process instance key. */
        parentProcessInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent element instance key. */
        parentElementInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The batch operation id. */
        batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The error message related to the process. */
        errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the process has failed jobs with retries left. */
        hasRetriesLeft?: boolean;
        /** The state of the element instances associated with the process instance. */
        elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** The element id associated with the process instance. */
        elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the element instance has an incident or not. */
        hasElementInstanceIncident?: boolean;
        /** The incident error hash code, associated with this process. */
        incidentErrorHashCode?: number | {
          /** Checks for equality with the provided value. */
          $eq?: number;
          /** Checks for inequality with the provided value. */
          $neq?: number;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: number;
          /** Greater than or equal comparison with the provided value. */
          $gte?: number;
          /** Lower than comparison with the provided value. */
          $lt?: number;
          /** Lower than or equal comparison with the provided value. */
          $lte?: number;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<number>;
      };
        /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
        tags?: Array<string>;
    }>;
};
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

/** The process instance filter that defines which process instances should have their incidents resolved. */
export interface ProcessInstanceIncidentResolutionBatchOperationRequest {
    /** The process instance filter. */
    filter: {
  processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionName?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionVersion?: number | {
        /** Checks for equality with the provided value. */
        $eq?: number;
        /** Checks for inequality with the provided value. */
        $neq?: number;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: number;
        /** Greater than or equal comparison with the provided value. */
        $gte?: number;
        /** Lower than comparison with the provided value. */
        $lt?: number;
        /** Lower than or equal comparison with the provided value. */
        $lte?: number;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<number>;
    };
  processDefinitionVersionTag?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
  $or?: Array<{
        /** The start date. */
        startDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The end date. */
        endDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The process instance state. */
        state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** Whether this process instance has a related incident or not. */
        hasIncident?: boolean;
        /** The tenant id. */
        tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The process instance variables. */
        variables?: Array<{
          /** Name of the variable. */
          name: string;
          /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
          value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      }>;
        /** The key of this process instance. */
        processInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent process instance key. */
        parentProcessInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent element instance key. */
        parentElementInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The batch operation id. */
        batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The error message related to the process. */
        errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the process has failed jobs with retries left. */
        hasRetriesLeft?: boolean;
        /** The state of the element instances associated with the process instance. */
        elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** The element id associated with the process instance. */
        elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the element instance has an incident or not. */
        hasElementInstanceIncident?: boolean;
        /** The incident error hash code, associated with this process. */
        incidentErrorHashCode?: number | {
          /** Checks for equality with the provided value. */
          $eq?: number;
          /** Checks for inequality with the provided value. */
          $neq?: number;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: number;
          /** Greater than or equal comparison with the provided value. */
          $gte?: number;
          /** Lower than comparison with the provided value. */
          $lt?: number;
          /** Lower than or equal comparison with the provided value. */
          $lte?: number;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<number>;
      };
        /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
        tags?: Array<string>;
    }>;
};
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

/** The process instance filter that defines which process instances should be deleted. */
export interface ProcessInstanceDeletionBatchOperationRequest {
    /** The process instance filter. */
    filter: {
  processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionName?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionVersion?: number | {
        /** Checks for equality with the provided value. */
        $eq?: number;
        /** Checks for inequality with the provided value. */
        $neq?: number;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: number;
        /** Greater than or equal comparison with the provided value. */
        $gte?: number;
        /** Lower than comparison with the provided value. */
        $lt?: number;
        /** Lower than or equal comparison with the provided value. */
        $lte?: number;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<number>;
    };
  processDefinitionVersionTag?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
  $or?: Array<{
        /** The start date. */
        startDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The end date. */
        endDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The process instance state. */
        state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** Whether this process instance has a related incident or not. */
        hasIncident?: boolean;
        /** The tenant id. */
        tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The process instance variables. */
        variables?: Array<{
          /** Name of the variable. */
          name: string;
          /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
          value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      }>;
        /** The key of this process instance. */
        processInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent process instance key. */
        parentProcessInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent element instance key. */
        parentElementInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The batch operation id. */
        batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The error message related to the process. */
        errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the process has failed jobs with retries left. */
        hasRetriesLeft?: boolean;
        /** The state of the element instances associated with the process instance. */
        elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** The element id associated with the process instance. */
        elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the element instance has an incident or not. */
        hasElementInstanceIncident?: boolean;
        /** The incident error hash code, associated with this process. */
        incidentErrorHashCode?: number | {
          /** Checks for equality with the provided value. */
          $eq?: number;
          /** Checks for inequality with the provided value. */
          $neq?: number;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: number;
          /** Greater than or equal comparison with the provided value. */
          $gte?: number;
          /** Lower than comparison with the provided value. */
          $lt?: number;
          /** Lower than or equal comparison with the provided value. */
          $lte?: number;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<number>;
      };
        /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
        tags?: Array<string>;
    }>;
};
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

export interface ProcessInstanceMigrationBatchOperationRequest {
    /** The process instance filter. */
    filter: {
  processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionName?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionVersion?: number | {
        /** Checks for equality with the provided value. */
        $eq?: number;
        /** Checks for inequality with the provided value. */
        $neq?: number;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: number;
        /** Greater than or equal comparison with the provided value. */
        $gte?: number;
        /** Lower than comparison with the provided value. */
        $lt?: number;
        /** Lower than or equal comparison with the provided value. */
        $lte?: number;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<number>;
    };
  processDefinitionVersionTag?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
  $or?: Array<{
        /** The start date. */
        startDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The end date. */
        endDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The process instance state. */
        state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** Whether this process instance has a related incident or not. */
        hasIncident?: boolean;
        /** The tenant id. */
        tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The process instance variables. */
        variables?: Array<{
          /** Name of the variable. */
          name: string;
          /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
          value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      }>;
        /** The key of this process instance. */
        processInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent process instance key. */
        parentProcessInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent element instance key. */
        parentElementInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The batch operation id. */
        batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The error message related to the process. */
        errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the process has failed jobs with retries left. */
        hasRetriesLeft?: boolean;
        /** The state of the element instances associated with the process instance. */
        elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** The element id associated with the process instance. */
        elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the element instance has an incident or not. */
        hasElementInstanceIncident?: boolean;
        /** The incident error hash code, associated with this process. */
        incidentErrorHashCode?: number | {
          /** Checks for equality with the provided value. */
          $eq?: number;
          /** Checks for inequality with the provided value. */
          $neq?: number;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: number;
          /** Greater than or equal comparison with the provided value. */
          $gte?: number;
          /** Lower than comparison with the provided value. */
          $lt?: number;
          /** Lower than or equal comparison with the provided value. */
          $lte?: number;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<number>;
      };
        /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
        tags?: Array<string>;
    }>;
};
    /** The migration plan. */
    migrationPlan: {
      /** The target process definition key. */
      targetProcessDefinitionKey: string;
      /** The mapping instructions. */
      mappingInstructions: Array<{
        /** The element id to migrate from. */
        sourceElementId: string;
        /** The element id to migrate into. */
        targetElementId: string;
    }>;
  };
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

/**  The migration instructions describe how to migrate a process instance from one process definition to another. */
export interface ProcessInstanceMigrationBatchOperationPlan {
    /** The target process definition key. */
    targetProcessDefinitionKey: string;
    /** The mapping instructions. */
    mappingInstructions: Array<{
      /** The element id to migrate from. */
      sourceElementId: string;
      /** The element id to migrate into. */
      targetElementId: string;
  }>;
}

/**  The process instance filter to define on which process instances tokens should be moved,
 *  and new element instances should be activated or terminated. */
export interface ProcessInstanceModificationBatchOperationRequest {
    /** The process instance filter. */
    filter: {
  processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionName?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionVersion?: number | {
        /** Checks for equality with the provided value. */
        $eq?: number;
        /** Checks for inequality with the provided value. */
        $neq?: number;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: number;
        /** Greater than or equal comparison with the provided value. */
        $gte?: number;
        /** Lower than comparison with the provided value. */
        $lt?: number;
        /** Lower than or equal comparison with the provided value. */
        $lte?: number;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<number>;
    };
  processDefinitionVersionTag?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
  $or?: Array<{
        /** The start date. */
        startDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The end date. */
        endDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The process instance state. */
        state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** Whether this process instance has a related incident or not. */
        hasIncident?: boolean;
        /** The tenant id. */
        tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The process instance variables. */
        variables?: Array<{
          /** Name of the variable. */
          name: string;
          /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
          value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      }>;
        /** The key of this process instance. */
        processInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent process instance key. */
        parentProcessInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent element instance key. */
        parentElementInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The batch operation id. */
        batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The error message related to the process. */
        errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the process has failed jobs with retries left. */
        hasRetriesLeft?: boolean;
        /** The state of the element instances associated with the process instance. */
        elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** The element id associated with the process instance. */
        elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the element instance has an incident or not. */
        hasElementInstanceIncident?: boolean;
        /** The incident error hash code, associated with this process. */
        incidentErrorHashCode?: number | {
          /** Checks for equality with the provided value. */
          $eq?: number;
          /** Checks for inequality with the provided value. */
          $neq?: number;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: number;
          /** Greater than or equal comparison with the provided value. */
          $gte?: number;
          /** Lower than comparison with the provided value. */
          $lt?: number;
          /** Lower than or equal comparison with the provided value. */
          $lte?: number;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<number>;
      };
        /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
        tags?: Array<string>;
    }>;
};
    /** Instructions for moving tokens between elements. */
    moveInstructions: Array<{
      /** The source element ID. */
      sourceElementId: string;
      /** The target element ID. */
      targetElementId: string;
  }>;
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

/**  Instructions describing a move operation. This instruction will terminate all active
 *  element instances at `sourceElementId` and activate a new element instance for each
 *  terminated one at `targetElementId`. The new element instances are created in the parent
 *  scope of the source element instances. */
export interface ProcessInstanceModificationMoveBatchOperationInstruction {
    /** The source element ID. */
    sourceElementId: string;
    /** The target element ID. */
    targetElementId: string;
}

/** The batch operation item state. */
export type BatchOperationItemStateEnum = "ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED";

/** The batch operation state. */
export type BatchOperationStateEnum = "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED";

/** The type of the batch operation. */
export type BatchOperationTypeEnum = "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";

/** BatchOperationTypeEnum property with full advanced search capabilities. */
export type BatchOperationTypeFilterProperty = "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE" | {
    /** Checks for equality with the provided value. */
    $eq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
    /** Checks for inequality with the provided value. */
    $neq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced BatchOperationTypeEnum filter. */
export interface AdvancedBatchOperationTypeFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
    /** Checks for inequality with the provided value. */
    $neq?: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** BatchOperationStateEnum property with full advanced search capabilities. */
export type BatchOperationStateFilterProperty = "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED" | {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced BatchOperationStateEnum filter. */
export interface AdvancedBatchOperationStateFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "CANCELED" | "COMPLETED" | "CREATED" | "FAILED" | "PARTIALLY_COMPLETED" | "SUSPENDED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** BatchOperationItemStateEnum property with full advanced search capabilities. */
export type BatchOperationItemStateFilterProperty = "ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED" | {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced BatchOperationItemStateEnum filter. */
export interface AdvancedBatchOperationItemStateFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "COMPLETED" | "CANCELED" | "FAILED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

export interface ClockPinRequest {
    /** The exact time in epoch milliseconds to which the clock should be pinned. */
    timestamp: number;
}

/** The scope of a cluster variable. */
export type ClusterVariableScopeEnum = "GLOBAL" | "TENANT";

export interface CreateClusterVariableRequest {
    /** The name of the cluster variable. Must be unique within its scope (global or tenant-specific). */
    name: string;
    /** The value of the cluster variable. Can be any JSON object or primitive value. Will be serialized as a JSON string in responses. */
    value: Record<string, unknown>;
}

export interface UpdateClusterVariableRequest {
    /** The new value of the cluster variable. Can be any JSON object or primitive value. Will be serialized as a JSON string in responses. */
    value: Record<string, unknown>;
}

export interface ClusterVariableResult {
    /** The name of the cluster variable. Unique within its scope (global or tenant-specific). */
    name: string;
    /** The scope of a cluster variable. */
    scope: "GLOBAL" | "TENANT";
    /** Only provided if the cluster variable scope is TENANT. Null for global scope variables. */
    tenantId?: string | null;
}

/** Cluster variable search response item. */
export interface ClusterVariableSearchResult {
    /** The name of the cluster variable. Unique within its scope (global or tenant-specific). */
    name: string;
    /** The scope of a cluster variable. */
    scope: "GLOBAL" | "TENANT";
    /** Only provided if the cluster variable scope is TENANT. Null for global scope variables. */
    tenantId?: string | null;
}

/** Cluster variable response item. */
export interface ClusterVariableResultBase {
    /** The name of the cluster variable. Unique within its scope (global or tenant-specific). */
    name: string;
    /** The scope of a cluster variable. */
    scope: "GLOBAL" | "TENANT";
    /** Only provided if the cluster variable scope is TENANT. Null for global scope variables. */
    tenantId?: string | null;
}

/** Cluster variable search query request. */
export type ClusterVariableSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface ClusterVariableSearchQuerySortRequest {
    /** The field to sort by. */
    field: "name" | "value" | "tenantId" | "scope";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Cluster variable filter request. */
export interface ClusterVariableSearchQueryFilterRequest {
    /** Name of the cluster variable. */
    name?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The value of the cluster variable. */
    value?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The scope filter for cluster variables. */
    scope?: "GLOBAL" | "TENANT" | {
      /** Checks for equality with the provided value. */
      $eq?: "GLOBAL" | "TENANT";
      /** Checks for inequality with the provided value. */
      $neq?: "GLOBAL" | "TENANT";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"GLOBAL" | "TENANT">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** Tenant ID of this variable. */
    tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /**  Filter cluster variables by truncation status of their stored values. When true, returns only variables whose stored values are truncated (i.e., the value exceeds the storage size limit and is truncated in storage). When false, returns only variables with non-truncated stored values. This filter is based on the underlying storage characteristic, not the response format. */
    isTruncated?: boolean;
}

/** ClusterVariableScopeEnum property with full advanced search capabilities. */
export type ClusterVariableScopeFilterProperty = "GLOBAL" | "TENANT" | {
    /** Checks for equality with the provided value. */
    $eq?: "GLOBAL" | "TENANT";
    /** Checks for inequality with the provided value. */
    $neq?: "GLOBAL" | "TENANT";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"GLOBAL" | "TENANT">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced ClusterVariableScopeEnum filter. */
export interface AdvancedClusterVariableScopeFilter {
    /** Checks for equality with the provided value. */
    $eq?: "GLOBAL" | "TENANT";
    /** Checks for inequality with the provided value. */
    $neq?: "GLOBAL" | "TENANT";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"GLOBAL" | "TENANT">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** Cluster variable search query response. */
export interface ClusterVariableSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface ConditionalEvaluationInstruction {
    /**  Used to evaluate root-level conditional start events for a tenant with the given ID.
 This will only evaluate root-level conditional start events of process definitions which belong to the tenant. */
    tenantId?: string;
    /**  Used to evaluate root-level conditional start events of the process definition with the given key. */
    processDefinitionKey?: string;
    /**  JSON object representing the variables to use for evaluation of the conditions and to pass to the process instances that have been triggered. */
    variables: {
      [key: string]: unknown;
  };
}

export interface EvaluateConditionalResult {
    /** The unique key of the conditional evaluation operation. */
    conditionalEvaluationKey: string;
    /** The tenant ID of the conditional evaluation operation. */
    tenantId: string;
    /** List of process instances created. If no root-level conditional start events evaluated to true, the list will be empty. */
    processInstances: Array<{
      /** The key of the process definition. */
      processDefinitionKey?: string;
      /** The key of the created process instance. */
      processInstanceKey?: string;
  }>;
}

/** System-generated key for a conditional evaluation. */
export type ConditionalEvaluationKey = string;

export interface ProcessInstanceReference {
    /** The key of the process definition. */
    processDefinitionKey?: string;
    /** The key of the created process instance. */
    processInstanceKey?: string;
}

export interface MessageCorrelationRequest {
    /** The message name as defined in the BPMN process */
    name: string;
    /** The correlation key of the message. */
    correlationKey?: string;
    /** The message variables as JSON document */
    variables?: {
      [key: string]: unknown;
  };
    /** the tenant for which the message is published */
    tenantId?: string;
}

/**  The message key of the correlated message, as well as the first process instance key it
 *  correlated with. */
export interface MessageCorrelationResult {
    /** The tenant ID of the correlated message */
    tenantId: string;
    /** The key of the correlated message. */
    messageKey: string;
    /** The key of the first process instance the message correlated with */
    processInstanceKey: string;
}

export interface MessagePublicationRequest {
    /** The name of the message. */
    name: string;
    /** The correlation key of the message. */
    correlationKey?: string;
    /** Timespan (in ms) to buffer the message on the broker. */
    timeToLive?: number;
    /**  The unique ID of the message. This is used to ensure only one message with the given ID
 will be published during the lifetime of the message (if `timeToLive` is set). */
    messageId?: string;
    /** The message variables as JSON document. */
    variables?: {
      [key: string]: unknown;
  };
    /** The tenant of the message sender. */
    tenantId?: string;
}

/** The message key of the published message. */
export interface MessagePublicationResult {
    /** The tenant ID of the message. */
    tenantId: string;
    /** The key of the published message. */
    messageKey: string;
}

export interface MessageSubscriptionSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface MessageSubscriptionResult {
    /** The message subscription key associated with this message subscription. */
    messageSubscriptionKey?: string;
    /** The process definition ID associated with this message subscription. */
    processDefinitionId?: string;
    /** The process definition key associated with this message subscription. */
    processDefinitionKey?: string;
    /** The process instance key associated with this message subscription. */
    processInstanceKey?: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** The element ID associated with this message subscription. */
    elementId?: string;
    /** The element instance key associated with this message subscription. */
    elementInstanceKey?: string;
    /** The state of message subscription. */
    messageSubscriptionState?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";
    /** The last updated date of the message subscription. */
    lastUpdatedDate?: string;
    /** The name of the message associated with the message subscription. */
    messageName?: string;
    /** The correlation key of the message subscription. */
    correlationKey?: string;
    /** The unique identifier of the tenant. */
    tenantId?: string;
}

export interface MessageSubscriptionSearchQuerySortRequest {
    /** The field to sort by. */
    field: "messageSubscriptionKey" | "processDefinitionId" | "processInstanceKey" | "elementId" | "elementInstanceKey" | "messageSubscriptionState" | "lastUpdatedDate" | "messageName" | "correlationKey" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export type MessageSubscriptionSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Message subscription search filter. */
export interface MessageSubscriptionFilter {
    /** The message subscription key associated with this message subscription. */
    messageSubscriptionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for equality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The process definition key associated with this correlated message subscription. This only works for data created with 8.9 and later. */
    processDefinitionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The process definition ID associated with this message subscription. */
    processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The process instance key associated with this message subscription. */
    processInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The element ID associated with this message subscription. */
    elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The element instance key associated with this message subscription. */
    elementInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The message subscription state. */
    messageSubscriptionState?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED" | {
      /** Checks for equality with the provided value. */
      $eq?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";
      /** Checks for inequality with the provided value. */
      $neq?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"CORRELATED" | "CREATED" | "DELETED" | "MIGRATED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The last updated date of the message subscription. */
    lastUpdatedDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The name of the message associated with the message subscription. */
    messageName?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The correlation key of the message subscription. */
    correlationKey?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The unique external tenant ID. */
    tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
}

export interface CorrelatedMessageSubscriptionSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface CorrelatedMessageSubscriptionResult {
    /** The correlation key of the message. */
    correlationKey: string;
    /** The time when the message was correlated. */
    correlationTime: string;
    /** The element ID that received the message. */
    elementId: string;
    /** The element instance key that received the message. */
    elementInstanceKey?: string;
    /** The message key. */
    messageKey: string;
    /** The name of the message. */
    messageName: string;
    /** The partition ID that correlated the message. */
    partitionId: number;
    /** The process definition ID associated with this correlated message subscription. */
    processDefinitionId: string;
    /** The process definition key associated with this correlated message subscription. */
    processDefinitionKey?: string;
    /** The process instance key associated with this correlated message subscription. */
    processInstanceKey: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** The subscription key that received the message. */
    subscriptionKey: string;
    /** The tenant ID associated with this correlated message subscription. */
    tenantId: string;
}

export type CorrelatedMessageSubscriptionSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface CorrelatedMessageSubscriptionSearchQuerySortRequest {
    /** The field to sort by. */
    field: "correlationKey" | "correlationTime" | "elementId" | "elementInstanceKey" | "messageKey" | "messageName" | "partitionId" | "processDefinitionId" | "processDefinitionKey" | "processInstanceKey" | "subscriptionKey" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** The state of message subscription. */
export type MessageSubscriptionStateEnum = "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";

/** Correlated message subscriptions search filter. */
export interface CorrelatedMessageSubscriptionFilter {
    /** The correlation key of the message. */
    correlationKey?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The time when the message was correlated. */
    correlationTime?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The element ID that received the message. */
    elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The element instance key that received the message. */
    elementInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The message key. */
    messageKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The name of the message. */
    messageName?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The partition ID that correlated the message. */
    partitionId?: number | {
      /** Checks for equality with the provided value. */
      $eq?: number;
      /** Checks for inequality with the provided value. */
      $neq?: number;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: number;
      /** Greater than or equal comparison with the provided value. */
      $gte?: number;
      /** Lower than comparison with the provided value. */
      $lt?: number;
      /** Lower than or equal comparison with the provided value. */
      $lte?: number;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<number>;
  };
    /** The process definition ID associated with this correlated message subscription. */
    processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The process definition key associated with this correlated message subscription. For intermediate message events, this only works for data created with 8.9 and later. */
    processDefinitionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The process instance key associated with this correlated message subscription. */
    processInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The subscription key that received the message. */
    subscriptionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for equality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The tenant ID associated with this correlated message subscription. */
    tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
}

/** MessageSubscriptionStateEnum with full advanced search capabilities. */
export type MessageSubscriptionStateFilterProperty = "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED" | {
    /** Checks for equality with the provided value. */
    $eq?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";
    /** Checks for inequality with the provided value. */
    $neq?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"CORRELATED" | "CREATED" | "DELETED" | "MIGRATED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced MessageSubscriptionStateEnum filter */
export interface AdvancedMessageSubscriptionStateFilter {
    /** Checks for equality with the provided value. */
    $eq?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";
    /** Checks for inequality with the provided value. */
    $neq?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"CORRELATED" | "CREATED" | "DELETED" | "MIGRATED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** Advanced MessageSubscriptionKey filter. */
export interface AdvancedMessageSubscriptionKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for equality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** MessageSubscriptionKey property with full advanced search capabilities. */
export type MessageSubscriptionKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for equality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** System-generated key for a message subscription. */
export type MessageSubscriptionKey = string;

/** System-generated key for an message. */
export type MessageKey = string;

export interface DecisionDefinitionSearchQuerySortRequest {
    /** The field to sort by. */
    field: "decisionDefinitionKey" | "decisionDefinitionId" | "name" | "version" | "decisionRequirementsId" | "decisionRequirementsKey" | "decisionRequirementsName" | "decisionRequirementsVersion" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export type DecisionDefinitionSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Decision definition search filter. */
export interface DecisionDefinitionFilter {
    /** The DMN ID of the decision definition. */
    decisionDefinitionId?: string;
    /** The DMN name of the decision definition. */
    name?: string;
    /**  Whether to only return the latest version of each decision definition.
 When using this filter, pagination functionality is limited, you can only paginate forward using `after` and `limit`.
 The response contains no `startCursor` in the `page`, and requests ignore the `from` and `before` in the `page`. */
    isLatestVersion?: boolean;
    /** The assigned version of the decision definition. */
    version?: number;
    /** the DMN ID of the decision requirements graph that the decision definition is part of. */
    decisionRequirementsId?: string;
    /** The tenant ID of the decision definition. */
    tenantId?: string;
    /** The assigned key, which acts as a unique identifier for this decision definition. */
    decisionDefinitionKey?: string;
    /** The assigned key of the decision requirements graph that the decision definition is part of. */
    decisionRequirementsKey?: string;
    /** The DMN name of the decision requirements that the decision definition is part of. */
    decisionRequirementsName?: string;
    /** The assigned version of the decision requirements that the decision definition is part of. */
    decisionRequirementsVersion?: number;
}

export interface DecisionDefinitionSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface DecisionDefinitionResult {
    /** The DMN ID of the decision definition. */
    decisionDefinitionId: string;
    /** The assigned key, which acts as a unique identifier for this decision definition. */
    decisionDefinitionKey: string;
    /** the DMN ID of the decision requirements graph that the decision definition is part of. */
    decisionRequirementsId: string;
    /** The assigned key of the decision requirements graph that the decision definition is part of. */
    decisionRequirementsKey: string;
    /** The DMN name of the decision requirements that the decision definition is part of. */
    decisionRequirementsName: string;
    /** The assigned version of the decision requirements that the decision definition is part of. */
    decisionRequirementsVersion: number;
    /** The DMN name of the decision definition. */
    name: string;
    /** The tenant ID of the decision definition. */
    tenantId: string;
    /** The assigned version of the decision definition. */
    version: number;
}

export type DecisionEvaluationInstruction = {
    /**  The ID of the decision to be evaluated.
 When using the decision ID, the latest
 deployed version of the decision is used. */
    decisionDefinitionId: string;
    /** The message variables as JSON document. */
    variables?: {
      [key: string]: unknown;
  };
    /** The tenant ID of the decision. */
    tenantId?: string;
} | {
    /** System-generated key for a decision definition. */
    decisionDefinitionKey: string;
    /** The message variables as JSON document. */
    variables?: {
      [key: string]: unknown;
  };
    /** The tenant ID of the decision. */
    tenantId?: string;
};

export interface DecisionEvaluationById {
    /**  The ID of the decision to be evaluated.
 When using the decision ID, the latest
 deployed version of the decision is used. */
    decisionDefinitionId: string;
    /** The message variables as JSON document. */
    variables?: {
      [key: string]: unknown;
  };
    /** The tenant ID of the decision. */
    tenantId?: string;
}

export interface DecisionEvaluationByKey {
    /** System-generated key for a decision definition. */
    decisionDefinitionKey: string;
    /** The message variables as JSON document. */
    variables?: {
      [key: string]: unknown;
  };
    /** The tenant ID of the decision. */
    tenantId?: string;
}

export interface EvaluateDecisionResult {
    /** The ID of the decision which was evaluated. */
    decisionDefinitionId: string;
    /** The unique key identifying the decision which was evaluated. */
    decisionDefinitionKey: string;
    /** The name of the decision which was evaluated. */
    decisionDefinitionName: string;
    /** The version of the decision which was evaluated. */
    decisionDefinitionVersion: number;
    /** The unique key identifying this decision evaluation. */
    decisionEvaluationKey: string;
    /** Deprecated, please refer to `decisionEvaluationKey`. */
    decisionInstanceKey: string;
    /** The ID of the decision requirements graph that the decision which was evaluated is part of. */
    decisionRequirementsId: string;
    /** The unique key identifying the decision requirements graph that the decision which was evaluated is part of. */
    decisionRequirementsKey: string;
    /** Decisions that were evaluated within the requested decision evaluation. */
    evaluatedDecisions: Array<{
      /** The ID of the decision which was evaluated. */
      decisionDefinitionId?: string;
      /** The name of the decision which was evaluated. */
      decisionDefinitionName?: string;
      /** The version of the decision which was evaluated. */
      decisionDefinitionVersion?: number;
      /** The type of the decision which was evaluated. */
      decisionDefinitionType: string;
      /**  JSON document that will instantiate the result of the decision which was evaluated. */
      output?: string;
      /** The tenant ID of the evaluated decision. */
      tenantId?: string;
      /** The decision rules that matched within this decision evaluation. */
      matchedRules: Array<{
        /** The ID of the matched rule. */
        ruleId?: string;
        /** The index of the matched rule. */
        ruleIndex?: number;
        /** The evaluated decision outputs. */
        evaluatedOutputs: Array<{
          /** The ID of the evaluated decison output item. */
          outputId?: string;
          /** The name of the of the evaluated decison output item. */
          outputName?: string;
          /** The value of the evaluated decison output item. */
          outputValue?: string;
          /** The ID of the matched rule. */
          ruleId: string | null;
          /** The index of the matched rule. */
          ruleIndex: number | null;
      }>;
    }>;
      /** The decision inputs that were evaluated within this decision evaluation. */
      evaluatedInputs: Array<{
        /** The identifier of the decision input. */
        inputId?: string;
        /** The name of the decision input. */
        inputName?: string;
        /** The description of the decision input. */
        inputValue?: string;
    }>;
      /** The unique key identifying the decision which was evaluate. */
      decisionDefinitionKey?: string;
      /** The unique key identifying this decision evaluation instance. */
      decisionEvaluationInstanceKey?: string;
  }>;
    /** The ID of the decision which failed during evaluation. */
    failedDecisionDefinitionId: string;
    /** Message describing why the decision which was evaluated failed. */
    failureMessage: string | null;
    /**  JSON document that will instantiate the result of the decision which was evaluated. */
    output: string;
    /** The tenant ID of the evaluated decision. */
    tenantId: string;
}

/** A decision that was evaluated. */
export interface EvaluatedDecisionResult {
    /** The ID of the decision which was evaluated. */
    decisionDefinitionId?: string;
    /** The name of the decision which was evaluated. */
    decisionDefinitionName?: string;
    /** The version of the decision which was evaluated. */
    decisionDefinitionVersion?: number;
    /** The type of the decision which was evaluated. */
    decisionDefinitionType: string;
    /**  JSON document that will instantiate the result of the decision which was evaluated. */
    output?: string;
    /** The tenant ID of the evaluated decision. */
    tenantId?: string;
    /** The decision rules that matched within this decision evaluation. */
    matchedRules: Array<{
      /** The ID of the matched rule. */
      ruleId?: string;
      /** The index of the matched rule. */
      ruleIndex?: number;
      /** The evaluated decision outputs. */
      evaluatedOutputs: Array<{
        /** The ID of the evaluated decison output item. */
        outputId?: string;
        /** The name of the of the evaluated decison output item. */
        outputName?: string;
        /** The value of the evaluated decison output item. */
        outputValue?: string;
        /** The ID of the matched rule. */
        ruleId: string | null;
        /** The index of the matched rule. */
        ruleIndex: number | null;
    }>;
  }>;
    /** The decision inputs that were evaluated within this decision evaluation. */
    evaluatedInputs: Array<{
      /** The identifier of the decision input. */
      inputId?: string;
      /** The name of the decision input. */
      inputName?: string;
      /** The description of the decision input. */
      inputValue?: string;
  }>;
    /** The unique key identifying the decision which was evaluate. */
    decisionDefinitionKey?: string;
    /** The unique key identifying this decision evaluation instance. */
    decisionEvaluationInstanceKey?: string;
}

export interface DecisionInstanceSearchQuerySortRequest {
    /** The field to sort by. */
    field: "decisionDefinitionId" | "decisionDefinitionKey" | "decisionDefinitionName" | "decisionDefinitionType" | "decisionDefinitionVersion" | "decisionEvaluationInstanceKey" | "decisionEvaluationKey" | "elementInstanceKey" | "evaluationDate" | "evaluationFailure" | "processDefinitionKey" | "processInstanceKey" | "rootDecisionDefinitionKey" | "state" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export type DecisionInstanceSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Decision instance search filter. */
export interface DecisionInstanceFilter {
    /** The key of the decision evaluation instance. */
    decisionEvaluationInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The state of the decision instance. */
    state?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN" | {
      /** Checks for equality with the provided value. */
      $eq?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
      /** Checks for inequality with the provided value. */
      $neq?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN">;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<"EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The evaluation failure of the decision instance. */
    evaluationFailure?: string;
    /** The evaluation date of the decision instance. */
    evaluationDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The ID of the DMN decision. */
    decisionDefinitionId?: string;
    /** The name of the DMN decision. */
    decisionDefinitionName?: string;
    /** The version of the decision. */
    decisionDefinitionVersion?: number;
    /** The type of the decision. UNSPECIFIED is deprecated and should not be used anymore, for removal in 8.10 */
    decisionDefinitionType?: "DECISION_TABLE" | "LITERAL_EXPRESSION" | "UNSPECIFIED" | "UNKNOWN";
    /** The tenant ID of the decision instance. */
    tenantId?: string;
    /** The key of the parent decision evaluation. Note that this is not the identifier of an individual decision instance; the `decisionEvaluationInstanceKey` is the identifier for a decision instance. */
    decisionEvaluationKey?: string;
    /** The key of the process definition. */
    processDefinitionKey?: string;
    /** The key of the process instance. */
    processInstanceKey?: string;
    /** The key of the decision. */
    decisionDefinitionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The key of the element instance this decision instance is linked to. */
    elementInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The key of the root decision definition. */
    rootDecisionDefinitionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The key of the decision requirements definition. */
    decisionRequirementsKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
}

export type DeleteDecisionInstanceRequest = {
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
} | null;

export interface DecisionInstanceSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface DecisionInstanceResult {
    /** System-generated key for a decision evaluation instance. */
    decisionEvaluationInstanceKey?: string;
    /** The state of the decision instance. UNSPECIFIED and UNKNOWN are deprecated and should not be used anymore, for removal in 8.10 */
    state?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
    /** The evaluation date of the decision instance. */
    evaluationDate?: string;
    /** The evaluation failure of the decision instance. */
    evaluationFailure: string | null;
    /** The ID of the DMN decision. */
    decisionDefinitionId?: string;
    /** The name of the DMN decision. */
    decisionDefinitionName?: string;
    /** The version of the decision. */
    decisionDefinitionVersion?: number;
    /** The type of the decision. UNSPECIFIED is deprecated and should not be used anymore, for removal in 8.10 */
    decisionDefinitionType?: "DECISION_TABLE" | "LITERAL_EXPRESSION" | "UNSPECIFIED" | "UNKNOWN";
    /** The result of the decision instance. */
    result?: string;
    /** The tenant ID of the decision instance. */
    tenantId: string;
    /** The key of the decision evaluation where this instance was created. */
    decisionEvaluationKey?: string;
    /** The key of the process definition. */
    processDefinitionKey?: string;
    /** The key of the process instance. */
    processInstanceKey?: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** The key of the decision. */
    decisionDefinitionKey?: string;
    /** The key of the element instance this decision instance is linked to. */
    elementInstanceKey?: string;
    /** The key of the root decision definition. */
    rootDecisionDefinitionKey?: string;
}

export interface DecisionInstanceGetQueryResult {
  decisionEvaluationInstanceKey?: string;
  state?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
  evaluationDate?: string;
  evaluationFailure: string | null;
  decisionDefinitionId?: string;
  decisionDefinitionName?: string;
  decisionDefinitionVersion?: number;
  decisionDefinitionType?: "DECISION_TABLE" | "LITERAL_EXPRESSION" | "UNSPECIFIED" | "UNKNOWN";
  result?: string;
  tenantId: string;
  decisionEvaluationKey?: string;
  processDefinitionKey?: string;
  processInstanceKey?: string;
  rootProcessInstanceKey: string;
  decisionDefinitionKey?: string;
  elementInstanceKey?: string;
  rootDecisionDefinitionKey?: string;
  evaluatedInputs?: Array<{
      /** The identifier of the decision input. */
      inputId?: string;
      /** The name of the decision input. */
      inputName?: string;
      /** The description of the decision input. */
      inputValue?: string;
  }>;
  matchedRules?: Array<{
      /** The ID of the matched rule. */
      ruleId?: string;
      /** The index of the matched rule. */
      ruleIndex?: number;
      /** The evaluated decision outputs. */
      evaluatedOutputs: Array<{
        /** The ID of the evaluated decison output item. */
        outputId?: string;
        /** The name of the of the evaluated decison output item. */
        outputName?: string;
        /** The value of the evaluated decison output item. */
        outputValue?: string;
        /** The ID of the matched rule. */
        ruleId: string | null;
        /** The index of the matched rule. */
        ruleIndex: number | null;
    }>;
  }>;
}

/** A decision input that was evaluated within this decision evaluation. */
export interface EvaluatedDecisionInputItem {
    /** The identifier of the decision input. */
    inputId?: string;
    /** The name of the decision input. */
    inputName?: string;
    /** The description of the decision input. */
    inputValue?: string;
}

/** The evaluated decision outputs. */
export interface EvaluatedDecisionOutputItem {
    /** The ID of the evaluated decison output item. */
    outputId?: string;
    /** The name of the of the evaluated decison output item. */
    outputName?: string;
    /** The value of the evaluated decison output item. */
    outputValue?: string;
    /** The ID of the matched rule. */
    ruleId: string | null;
    /** The index of the matched rule. */
    ruleIndex: number | null;
}

/** A decision rule that matched within this decision evaluation. */
export interface MatchedDecisionRuleItem {
    /** The ID of the matched rule. */
    ruleId?: string;
    /** The index of the matched rule. */
    ruleIndex?: number;
    /** The evaluated decision outputs. */
    evaluatedOutputs: Array<{
      /** The ID of the evaluated decison output item. */
      outputId?: string;
      /** The name of the of the evaluated decison output item. */
      outputName?: string;
      /** The value of the evaluated decison output item. */
      outputValue?: string;
      /** The ID of the matched rule. */
      ruleId: string | null;
      /** The index of the matched rule. */
      ruleIndex: number | null;
  }>;
}

/** The type of the decision. UNSPECIFIED is deprecated and should not be used anymore, for removal in 8.10 */
export type DecisionDefinitionTypeEnum = "DECISION_TABLE" | "LITERAL_EXPRESSION" | "UNSPECIFIED" | "UNKNOWN";

/** The state of the decision instance. UNSPECIFIED and UNKNOWN are deprecated and should not be used anymore, for removal in 8.10 */
export type DecisionInstanceStateEnum = "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";

/** Advanced DecisionInstanceStateEnum filter. */
export interface AdvancedDecisionInstanceStateFilter {
    /** Checks for equality with the provided value. */
    $eq?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
    /** Checks for inequality with the provided value. */
    $neq?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN">;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<"EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** DecisionInstanceStateEnum property with full advanced search capabilities. */
export type DecisionInstanceStateFilterProperty = "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN" | {
    /** Checks for equality with the provided value. */
    $eq?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
    /** Checks for inequality with the provided value. */
    $neq?: "EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN">;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<"EVALUATED" | "FAILED" | "UNSPECIFIED" | "UNKNOWN">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

export interface DecisionRequirementsSearchQuerySortRequest {
    /** The field to sort by. */
    field: "decisionRequirementsKey" | "decisionRequirementsName" | "version" | "decisionRequirementsId" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export type DecisionRequirementsSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Decision requirements search filter. */
export interface DecisionRequirementsFilter {
    /** The DMN name of the decision requirements. */
    decisionRequirementsName?: string;
    /** the DMN ID of the decision requirements. */
    decisionRequirementsId?: string;
    /** System-generated key for a deployed decision requirements definition. */
    decisionRequirementsKey?: string;
    /** The assigned version of the decision requirements. */
    version?: number;
    /** The tenant ID of the decision requirements. */
    tenantId?: string;
    /** The name of the resource from which the decision requirements were parsed */
    resourceName?: string;
}

export interface DecisionRequirementsSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface DecisionRequirementsResult {
    /** The DMN name of the decision requirements. */
    decisionRequirementsName?: string;
    /** The assigned version of the decision requirements. */
    version?: number;
    /** The DMN ID of the decision requirements. */
    decisionRequirementsId?: string;
    /** The name of the resource from which this decision requirements was parsed. */
    resourceName?: string;
    /** The tenant ID of the decision requirements. */
    tenantId?: string;
    /** The assigned key, which acts as a unique identifier for this decision requirements. */
    decisionRequirementsKey?: string;
}

export interface DeploymentResult {
    /** The unique key identifying the deployment. */
    deploymentKey: string;
    /** The tenant ID associated with the deployment. */
    tenantId: string;
    /** Items deployed by the request. */
    deployments: Array<{
      /** Deployed process. */
      processDefinition: {
        /**  The bpmn process ID, as parsed during deployment, together with the version forms a
 unique identifier for a specific process definition. */
        processDefinitionId: string;
        /** The assigned process version. */
        processDefinitionVersion: number;
        /** The resource name from which this process was parsed. */
        resourceName: string;
        /** The tenant ID of the deployed process. */
        tenantId: string;
        /** The assigned key, which acts as a unique identifier for this process. */
        processDefinitionKey: string;
    };
      /** Deployed decision. */
      decisionDefinition: {
        /**  The dmn decision ID, as parsed during deployment, together with the version forms a
 unique identifier for a specific decision. */
        decisionDefinitionId?: string;
        /** The assigned decision version. */
        version?: number;
        /** The DMN name of the decision, as parsed during deployment. */
        name?: string;
        /** The tenant ID of the deployed decision. */
        tenantId?: string;
        /**  The dmn ID of the decision requirements graph that this decision is part of, as parsed during deployment. */
        decisionRequirementsId?: string;
        /**  The assigned decision key, which acts as a unique identifier for this decision. */
        decisionDefinitionKey?: string;
        /**  The assigned key of the decision requirements graph that this decision is part of. */
        decisionRequirementsKey?: string;
    };
      /** Deployed decision requirement definition. */
      decisionRequirements: {
        /** The id of the deployed decision requirements. */
        decisionRequirementsId?: string;
        /** The name of the deployed decision requirements. */
        decisionRequirementsName?: string;
        /** The version of the deployed decision requirements. */
        version?: number;
        /** The name of the resource. */
        resourceName?: string;
        /** The tenant ID of the deployed decision requirements. */
        tenantId?: string;
        /**  The assigned decision requirements key, which acts as a unique identifier for this decision requirements. */
        decisionRequirementsKey?: string;
    };
      /** Deployed form. */
      form: {
        /**  The form ID, as parsed during deployment, together with the version forms a
 unique identifier for a specific form. */
        formId?: string;
        /** The version of the deployed form. */
        version?: number;
        /** The name of the resource. */
        resourceName?: string;
        /** The unique identifier of the tenant. */
        tenantId?: string;
        /** The assigned key, which acts as a unique identifier for this form. */
        formKey?: string;
    };
      /** Deployed resource. */
      resource: {
        /** The resource id of the deployed resource. */
        resourceId?: string;
        /** The name of the deployed resource. */
        resourceName?: string;
        /** The description of the deployed resource. */
        version?: number;
        /** The unique identifier of the tenant. */
        tenantId?: string;
        /** The assigned key, which acts as a unique identifier for this Resource. */
        resourceKey?: string;
    };
  }>;
}

export interface DeploymentMetadataResult {
    /** Deployed process. */
    processDefinition: {
      /**  The bpmn process ID, as parsed during deployment, together with the version forms a
 unique identifier for a specific process definition. */
      processDefinitionId: string;
      /** The assigned process version. */
      processDefinitionVersion: number;
      /** The resource name from which this process was parsed. */
      resourceName: string;
      /** The tenant ID of the deployed process. */
      tenantId: string;
      /** The assigned key, which acts as a unique identifier for this process. */
      processDefinitionKey: string;
  };
    /** Deployed decision. */
    decisionDefinition: {
      /**  The dmn decision ID, as parsed during deployment, together with the version forms a
 unique identifier for a specific decision. */
      decisionDefinitionId?: string;
      /** The assigned decision version. */
      version?: number;
      /** The DMN name of the decision, as parsed during deployment. */
      name?: string;
      /** The tenant ID of the deployed decision. */
      tenantId?: string;
      /**  The dmn ID of the decision requirements graph that this decision is part of, as parsed during deployment. */
      decisionRequirementsId?: string;
      /**  The assigned decision key, which acts as a unique identifier for this decision. */
      decisionDefinitionKey?: string;
      /**  The assigned key of the decision requirements graph that this decision is part of. */
      decisionRequirementsKey?: string;
  };
    /** Deployed decision requirement definition. */
    decisionRequirements: {
      /** The id of the deployed decision requirements. */
      decisionRequirementsId?: string;
      /** The name of the deployed decision requirements. */
      decisionRequirementsName?: string;
      /** The version of the deployed decision requirements. */
      version?: number;
      /** The name of the resource. */
      resourceName?: string;
      /** The tenant ID of the deployed decision requirements. */
      tenantId?: string;
      /**  The assigned decision requirements key, which acts as a unique identifier for this decision requirements. */
      decisionRequirementsKey?: string;
  };
    /** Deployed form. */
    form: {
      /**  The form ID, as parsed during deployment, together with the version forms a
 unique identifier for a specific form. */
      formId?: string;
      /** The version of the deployed form. */
      version?: number;
      /** The name of the resource. */
      resourceName?: string;
      /** The unique identifier of the tenant. */
      tenantId?: string;
      /** The assigned key, which acts as a unique identifier for this form. */
      formKey?: string;
  };
    /** Deployed resource. */
    resource: {
      /** The resource id of the deployed resource. */
      resourceId?: string;
      /** The name of the deployed resource. */
      resourceName?: string;
      /** The description of the deployed resource. */
      version?: number;
      /** The unique identifier of the tenant. */
      tenantId?: string;
      /** The assigned key, which acts as a unique identifier for this Resource. */
      resourceKey?: string;
  };
}

/** A deployed process. */
export interface DeploymentProcessResult {
    /**  The bpmn process ID, as parsed during deployment, together with the version forms a
 unique identifier for a specific process definition. */
    processDefinitionId: string;
    /** The assigned process version. */
    processDefinitionVersion: number;
    /** The resource name from which this process was parsed. */
    resourceName: string;
    /** The tenant ID of the deployed process. */
    tenantId: string;
    /** The assigned key, which acts as a unique identifier for this process. */
    processDefinitionKey: string;
}

/** A deployed decision. */
export interface DeploymentDecisionResult {
    /**  The dmn decision ID, as parsed during deployment, together with the version forms a
 unique identifier for a specific decision. */
    decisionDefinitionId?: string;
    /** The assigned decision version. */
    version?: number;
    /** The DMN name of the decision, as parsed during deployment. */
    name?: string;
    /** The tenant ID of the deployed decision. */
    tenantId?: string;
    /**  The dmn ID of the decision requirements graph that this decision is part of, as parsed during deployment. */
    decisionRequirementsId?: string;
    /**  The assigned decision key, which acts as a unique identifier for this decision. */
    decisionDefinitionKey?: string;
    /**  The assigned key of the decision requirements graph that this decision is part of. */
    decisionRequirementsKey?: string;
}

/** Deployed decision requirements. */
export interface DeploymentDecisionRequirementsResult {
    /** The id of the deployed decision requirements. */
    decisionRequirementsId?: string;
    /** The name of the deployed decision requirements. */
    decisionRequirementsName?: string;
    /** The version of the deployed decision requirements. */
    version?: number;
    /** The name of the resource. */
    resourceName?: string;
    /** The tenant ID of the deployed decision requirements. */
    tenantId?: string;
    /**  The assigned decision requirements key, which acts as a unique identifier for this decision requirements. */
    decisionRequirementsKey?: string;
}

/** A deployed form. */
export interface DeploymentFormResult {
    /**  The form ID, as parsed during deployment, together with the version forms a
 unique identifier for a specific form. */
    formId?: string;
    /** The version of the deployed form. */
    version?: number;
    /** The name of the resource. */
    resourceName?: string;
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /** The assigned key, which acts as a unique identifier for this form. */
    formKey?: string;
}

/** A deployed Resource. */
export interface DeploymentResourceResult {
    /** The resource id of the deployed resource. */
    resourceId?: string;
    /** The name of the deployed resource. */
    resourceName?: string;
    /** The description of the deployed resource. */
    version?: number;
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /** The assigned key, which acts as a unique identifier for this Resource. */
    resourceKey?: string;
}

export type DeleteResourceRequest = {
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
    /**  Indicates if the historic data of a process resource should be deleted via a
 batch operation asynchronously.

 This flag is only effective for process resources. For other resource types
 (decisions, forms, generic resources), this flag is ignored and no history
 will be deleted. In those cases, the `batchOperation` field in the response
 will not be populated. */
    deleteHistory?: boolean;
} | null;

export interface DeleteResourceResponse {
    /** The system-assigned key for this resource, requested to be deleted. */
    resourceKey: string;
    /**  The batch operation created for asynchronously deleting the historic data.

 This field is only populated when the request `deleteHistory` is set to `true` and the resource
 is a process definition. For other resource types (decisions, forms, generic resources),
 this field will be `null`. */
    batchOperation?: {
      /** Key of the batch operation. */
      batchOperationKey: string;
      /** The type of the batch operation. */
      batchOperationType: "ADD_VARIABLE" | "CANCEL_PROCESS_INSTANCE" | "DELETE_DECISION_DEFINITION" | "DELETE_DECISION_INSTANCE" | "DELETE_PROCESS_DEFINITION" | "DELETE_PROCESS_INSTANCE" | "MIGRATE_PROCESS_INSTANCE" | "MODIFY_PROCESS_INSTANCE" | "RESOLVE_INCIDENT" | "UPDATE_VARIABLE";
  };
}

export interface ResourceResult {
    /** The resource name from which this resource was parsed. */
    resourceName?: string;
    /** The assigned resource version. */
    version?: number;
    /** The version tag of this resource. */
    versionTag?: string;
    /** The resource ID of this resource. */
    resourceId?: string;
    /** The tenant ID of this resource. */
    tenantId?: string;
    /** The unique key of this resource. */
    resourceKey?: string;
}

/** Key for a deployment. */
export type DeploymentKey = string;

/** The system-assigned key for this resource. */
export type ResourceKey = string;

/** DeploymentKey property with full advanced search capabilities. */
export type DeploymentKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced DeploymentKey filter. */
export interface AdvancedDeploymentKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** ResourceKey property with full advanced search capabilities. */
export type ResourceKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced ResourceKey filter. */
export interface AdvancedResourceKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

export interface DocumentReference {
    /** Document discriminator. Always set to "camunda". */
    "camunda.document.type": "camunda";
    /** The ID of the document store. */
    storeId: string;
    /** The ID of the document. */
    documentId: string;
    /** The hash of the document. */
    contentHash: string | null;
    /** Information about the document that is returned in responses. */
    metadata: {
      /** The content type of the document. */
      contentType: string;
      /** The name of the file. */
      fileName: string;
      /** The date and time when the document expires. */
      expiresAt: string | null;
      /** The size of the document in bytes. */
      size: number;
      /** The ID of the process definition that created the document. */
      processDefinitionId: string;
      /** The key of the process instance that created the document. */
      processInstanceKey: string;
      /** Custom properties of the document. */
      customProperties: {
        [key: string]: unknown;
    };
  };
}

export interface DocumentCreationFailureDetail {
    /** The name of the file that failed to upload. */
    fileName?: string;
    /** The HTTP status code of the failure. */
    status?: number;
    /** A short, human-readable summary of the problem type. */
    title?: string;
    /** A human-readable explanation specific to this occurrence of the problem. */
    detail?: string;
}

export interface DocumentCreationBatchResponse {
    /** Documents that were successfully created. */
    failedDocuments?: Array<{
      /** The name of the file that failed to upload. */
      fileName?: string;
      /** The HTTP status code of the failure. */
      status?: number;
      /** A short, human-readable summary of the problem type. */
      title?: string;
      /** A human-readable explanation specific to this occurrence of the problem. */
      detail?: string;
  }>;
    /** Documents that failed creation. */
    createdDocuments?: Array<{
      /** Document discriminator. Always set to "camunda". */
      "camunda.document.type": "camunda";
      /** The ID of the document store. */
      storeId: string;
      /** The ID of the document. */
      documentId: string;
      /** The hash of the document. */
      contentHash: string | null;
      /** Information about the document that is returned in responses. */
      metadata: {
        /** The content type of the document. */
        contentType: string;
        /** The name of the file. */
        fileName: string;
        /** The date and time when the document expires. */
        expiresAt: string | null;
        /** The size of the document in bytes. */
        size: number;
        /** The ID of the process definition that created the document. */
        processDefinitionId: string;
        /** The key of the process instance that created the document. */
        processInstanceKey: string;
        /** Custom properties of the document. */
        customProperties: {
          [key: string]: unknown;
      };
    };
  }>;
}

/** Information about the document. */
export interface DocumentMetadata {
    /** The content type of the document. */
    contentType?: string;
    /** The name of the file. */
    fileName?: string;
    /** The date and time when the document expires. */
    expiresAt?: string;
    /** The size of the document in bytes. */
    size?: number;
    /** The ID of the process definition that created the document. */
    processDefinitionId?: string;
    /** The key of the process instance that created the document. */
    processInstanceKey?: string;
    /** Custom properties of the document. */
    customProperties?: {
      [key: string]: unknown;
  };
}

/** Information about the document that is returned in responses. */
export interface DocumentMetadataResponse {
    /** The content type of the document. */
    contentType: string;
    /** The name of the file. */
    fileName: string;
    /** The date and time when the document expires. */
    expiresAt: string | null;
    /** The size of the document in bytes. */
    size: number;
    /** The ID of the process definition that created the document. */
    processDefinitionId: string;
    /** The key of the process instance that created the document. */
    processInstanceKey: string;
    /** Custom properties of the document. */
    customProperties: {
      [key: string]: unknown;
  };
}

export interface DocumentLinkRequest {
    /** The time-to-live of the document link in ms. */
    timeToLive?: number;
}

export interface DocumentLink {
    /** The link to the document. */
    url: string;
    /** The date and time when the link expires. */
    expiresAt: string;
}

/** Document Id that uniquely identifies a document. */
export type DocumentId = string;

export interface ElementInstanceSearchQuerySortRequest {
    /** The field to sort by. */
    field: "elementInstanceKey" | "processInstanceKey" | "processDefinitionKey" | "processDefinitionId" | "startDate" | "endDate" | "elementId" | "elementName" | "type" | "state" | "incidentKey" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Element instance search request. */
export type ElementInstanceSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Element instance filter. */
export interface ElementInstanceFilter {
    /** The process definition ID associated to this element instance. */
    processDefinitionId?: string;
    /** State of element instance as defined set of values. */
    state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** Type of element as defined set of values. */
    "type"?: "UNSPECIFIED" | "PROCESS" | "SUB_PROCESS" | "EVENT_SUB_PROCESS" | "AD_HOC_SUB_PROCESS" | "AD_HOC_SUB_PROCESS_INNER_INSTANCE" | "START_EVENT" | "INTERMEDIATE_CATCH_EVENT" | "INTERMEDIATE_THROW_EVENT" | "BOUNDARY_EVENT" | "END_EVENT" | "SERVICE_TASK" | "RECEIVE_TASK" | "USER_TASK" | "MANUAL_TASK" | "TASK" | "EXCLUSIVE_GATEWAY" | "INCLUSIVE_GATEWAY" | "PARALLEL_GATEWAY" | "EVENT_BASED_GATEWAY" | "SEQUENCE_FLOW" | "MULTI_INSTANCE_BODY" | "CALL_ACTIVITY" | "BUSINESS_RULE_TASK" | "SCRIPT_TASK" | "SEND_TASK" | "UNKNOWN";
    /** The element ID for this element instance. */
    elementId?: string;
    /** The element name. This only works for data created with 8.8 and onwards. Instances from prior versions don't contain this data and cannot be found. */
    elementName?: string;
    /** Shows whether this element instance has an incident related to. */
    hasIncident?: boolean;
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /** The assigned key, which acts as a unique identifier for this element instance. */
    elementInstanceKey?: string;
    /** The process instance key associated to this element instance. */
    processInstanceKey?: string;
    /** The process definition key associated to this element instance. */
    processDefinitionKey?: string;
    /** The key of incident if field incident is true. */
    incidentKey?: string;
    /** The start date of this element instance. */
    startDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The end date of this element instance. */
    endDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The scope key of this element instance. If provided with a process instance key it will return element instances that are immediate children of the process instance. If provided with an element instance key it will return element instances that are immediate children of the element instance. */
    elementInstanceScopeKey?: string;
}

/** ElementInstanceStateEnum property with full advanced search capabilities. */
export type ElementInstanceStateFilterProperty = "ACTIVE" | "COMPLETED" | "TERMINATED" | {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced ElementInstanceStateEnum filter. */
export interface AdvancedElementInstanceStateFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

export interface ElementInstanceSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface ElementInstanceResult {
    /** The process definition ID associated to this element instance. */
    processDefinitionId: string;
    /** Date when element instance started. */
    startDate: string;
    /** Date when element instance finished. */
    endDate?: string | null;
    /** The element ID for this element instance. */
    elementId: string;
    /** The element name for this element instance. */
    elementName: string;
    /** Type of element as defined set of values. */
    "type": "UNSPECIFIED" | "PROCESS" | "SUB_PROCESS" | "EVENT_SUB_PROCESS" | "AD_HOC_SUB_PROCESS" | "AD_HOC_SUB_PROCESS_INNER_INSTANCE" | "START_EVENT" | "INTERMEDIATE_CATCH_EVENT" | "INTERMEDIATE_THROW_EVENT" | "BOUNDARY_EVENT" | "END_EVENT" | "SERVICE_TASK" | "RECEIVE_TASK" | "USER_TASK" | "MANUAL_TASK" | "TASK" | "EXCLUSIVE_GATEWAY" | "INCLUSIVE_GATEWAY" | "PARALLEL_GATEWAY" | "EVENT_BASED_GATEWAY" | "SEQUENCE_FLOW" | "MULTI_INSTANCE_BODY" | "CALL_ACTIVITY" | "BUSINESS_RULE_TASK" | "SCRIPT_TASK" | "SEND_TASK" | "UNKNOWN";
    /** State of element instance as defined set of values. */
    state: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Shows whether this element instance has an incident. If true also an incidentKey is provided. */
    hasIncident: boolean;
    /** The tenant ID of the incident. */
    tenantId: string;
    /** The assigned key, which acts as a unique identifier for this element instance. */
    elementInstanceKey: string;
    /** The process instance key associated to this element instance. */
    processInstanceKey: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** The process definition key associated to this element instance. */
    processDefinitionKey: string;
    /** Incident key associated with this element instance. */
    incidentKey?: string;
}

/** Element states */
export type ElementInstanceStateEnum = "ACTIVE" | "COMPLETED" | "TERMINATED";

export interface AdHocSubProcessActivateActivitiesInstruction {
    /** Activities to activate. */
    elements: Array<{
      /** The ID of the element that should be activated. */
      elementId: string;
      /** Variables to be set when activating the element. */
      variables?: {
        [key: string]: unknown;
    };
  }>;
    /** Whether to cancel remaining instances of the ad-hoc sub-process. */
    cancelRemainingInstances?: boolean;
}

export interface AdHocSubProcessActivateActivityReference {
    /** The ID of the element that should be activated. */
    elementId: string;
    /** Variables to be set when activating the element. */
    variables?: {
      [key: string]: unknown;
  };
}

export type ExpressionEvaluationRequest = {
    /** The expression to evaluate (e.g., "=x + y") */
    expression: string;
    /** Required when the expression references tenant-scoped cluster variables */
    tenantId?: string;
    /** Optional variables for expression evaluation. These variables are only used for the current evaluation and do not persist beyond it. */
    variables?: {
      [key: string]: unknown;
  } | null;
};

export interface ExpressionEvaluationResult {
    /** The evaluated expression */
    expression: string;
    /** The result value. Its type can vary. */
    result: unknown;
    /** List of warnings generated during expression evaluation */
    warnings: Array<string>;
}

/** How the global listener was defined. */
export type GlobalListenerSourceEnum = "CONFIGURATION" | "API";

/** The event type that triggers the user task listener. */
export type GlobalTaskListenerEventTypeEnum = "all" | "creating" | "assigning" | "updating" | "completing" | "canceling";

export interface GlobalListenerBase {
    /** The name of the job type, used as a reference to specify which job workers request the respective listener job. */
    "type"?: string;
    /** Number of retries for the listener job. */
    retries?: number;
    /** Whether the listener should run after model-level listeners. */
    afterNonGlobal?: boolean;
    /** The priority of the listener. Higher priority listeners are executed before lower priority ones. */
    priority?: number;
}

export interface GlobalTaskListenerBase {
    /** The name of the job type, used as a reference to specify which job workers request the respective listener job. */
    "type"?: string;
    /** Number of retries for the listener job. */
    retries?: number;
    /** Whether the listener should run after model-level listeners. */
    afterNonGlobal?: boolean;
    /** The priority of the listener. Higher priority listeners are executed before lower priority ones. */
    priority?: number;
}

/** List of user task event types that trigger the listener. */
export type GlobalTaskListenerEventTypes = Array<"all" | "creating" | "assigning" | "updating" | "completing" | "canceling">;

export interface CreateGlobalTaskListenerRequest {
    /** The name of the job type, used as a reference to specify which job workers request the respective listener job. */
    "type"?: string;
    /** Number of retries for the listener job. */
    retries?: number;
    /** Whether the listener should run after model-level listeners. */
    afterNonGlobal?: boolean;
    /** The priority of the listener. Higher priority listeners are executed before lower priority ones. */
    priority?: number;
}

export interface UpdateGlobalTaskListenerRequest {
    /** The name of the job type, used as a reference to specify which job workers request the respective listener job. */
    "type"?: string;
    /** Number of retries for the listener job. */
    retries?: number;
    /** Whether the listener should run after model-level listeners. */
    afterNonGlobal?: boolean;
    /** The priority of the listener. Higher priority listeners are executed before lower priority ones. */
    priority?: number;
}

export interface GlobalTaskListenerResult {
    /** The name of the job type, used as a reference to specify which job workers request the respective listener job. */
    "type"?: string;
    /** Number of retries for the listener job. */
    retries?: number;
    /** Whether the listener should run after model-level listeners. */
    afterNonGlobal?: boolean;
    /** The priority of the listener. Higher priority listeners are executed before lower priority ones. */
    priority?: number;
}

/** Global listener search query request. */
export type GlobalTaskListenerSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface GlobalTaskListenerSearchQuerySortRequest {
    /** The field to sort by. */
    field: "id" | "type" | "afterNonGlobal" | "priority" | "source";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Global listener filter request. */
export interface GlobalTaskListenerSearchQueryFilterRequest {
    /** Id of the global listener. */
    id?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Job type of the global listener. */
    "type"?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Number of retries of the global listener. */
    retries?: number | {
      /** Checks for equality with the provided value. */
      $eq?: number;
      /** Checks for inequality with the provided value. */
      $neq?: number;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: number;
      /** Greater than or equal comparison with the provided value. */
      $gte?: number;
      /** Lower than comparison with the provided value. */
      $lt?: number;
      /** Lower than or equal comparison with the provided value. */
      $lte?: number;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<number>;
  };
    /** Event types of the global listener. */
    eventTypes?: Array<"all" | "creating" | "assigning" | "updating" | "completing" | "canceling" | {
      /** Checks for equality with the provided value. */
      $eq?: "all" | "creating" | "assigning" | "updating" | "completing" | "canceling";
      /** Checks for inequality with the provided value. */
      $neq?: "all" | "creating" | "assigning" | "updating" | "completing" | "canceling";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"all" | "creating" | "assigning" | "updating" | "completing" | "canceling">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  }>;
    /** Whether the listener runs after model-level listeners. */
    afterNonGlobal?: boolean;
    /** Priority of the global listener. */
    priority?: number | {
      /** Checks for equality with the provided value. */
      $eq?: number;
      /** Checks for inequality with the provided value. */
      $neq?: number;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: number;
      /** Greater than or equal comparison with the provided value. */
      $gte?: number;
      /** Lower than comparison with the provided value. */
      $lt?: number;
      /** Lower than or equal comparison with the provided value. */
      $lte?: number;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<number>;
  };
    /** How the global listener was defined. */
    source?: "CONFIGURATION" | "API" | {
      /** Checks for equality with the provided value. */
      $eq?: "CONFIGURATION" | "API";
      /** Checks for inequality with the provided value. */
      $neq?: "CONFIGURATION" | "API";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"CONFIGURATION" | "API">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
}

/** Global listener source property with full advanced search capabilities. */
export type GlobalListenerSourceFilterProperty = "CONFIGURATION" | "API" | {
    /** Checks for equality with the provided value. */
    $eq?: "CONFIGURATION" | "API";
    /** Checks for inequality with the provided value. */
    $neq?: "CONFIGURATION" | "API";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"CONFIGURATION" | "API">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced global listener source filter. */
export interface AdvancedGlobalListenerSourceFilter {
    /** Checks for equality with the provided value. */
    $eq?: "CONFIGURATION" | "API";
    /** Checks for inequality with the provided value. */
    $neq?: "CONFIGURATION" | "API";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"CONFIGURATION" | "API">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** Global listener event type property with full advanced search capabilities. */
export type GlobalTaskListenerEventTypeFilterProperty = "all" | "creating" | "assigning" | "updating" | "completing" | "canceling" | {
    /** Checks for equality with the provided value. */
    $eq?: "all" | "creating" | "assigning" | "updating" | "completing" | "canceling";
    /** Checks for inequality with the provided value. */
    $neq?: "all" | "creating" | "assigning" | "updating" | "completing" | "canceling";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"all" | "creating" | "assigning" | "updating" | "completing" | "canceling">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced global listener event type filter. */
export interface AdvancedGlobalTaskListenerEventTypeFilter {
    /** Checks for equality with the provided value. */
    $eq?: "all" | "creating" | "assigning" | "updating" | "completing" | "canceling";
    /** Checks for inequality with the provided value. */
    $neq?: "all" | "creating" | "assigning" | "updating" | "completing" | "canceling";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"all" | "creating" | "assigning" | "updating" | "completing" | "canceling">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** Global listener search query response. */
export interface GlobalTaskListenerSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface GroupCreateRequest {
    /** The ID of the new group. */
    groupId: string;
    /** The display name of the new group. */
    name: string;
    /** The description of the new group. */
    description?: string;
}

export interface GroupCreateResult {
    /** The ID of the created group. */
    groupId?: string;
    /** The display name of the created group. */
    name?: string;
    /** The description of the created group. */
    description?: string;
}

export interface GroupUpdateRequest {
    /** The new name of the group. */
    name: string;
    /** The new description of the group. */
    description?: string;
}

export interface GroupUpdateResult {
    /** The unique external group ID. */
    groupId?: string;
    /** The name of the group. */
    name?: string;
    /** The description of the group. */
    description?: string;
}

/** Group search response item. */
export interface GroupResult {
    /** The group name. */
    name?: string;
    /** The group ID. */
    groupId?: string;
    /** The group description. */
    description?: string;
}

export interface GroupSearchQuerySortRequest {
    /** The field to sort by. */
    field: "name" | "groupId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Group search request. */
export type GroupSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Group filter request */
export interface GroupFilter {
    /** The group ID search filters. */
    groupId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The group name search filters. */
    name?: string;
}

/** Group search response. */
export interface GroupSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface GroupUserResult {
    /** The unique name of a user. */
    username?: string;
}

export interface GroupUserSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export type GroupUserSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface GroupUserSearchQuerySortRequest {
    /** The field to sort by. */
    field: "username";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface GroupClientResult {
    /** The ID of the client. */
    clientId?: string;
}

export interface GroupClientSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export type GroupClientSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface GroupMappingRuleSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface GroupRoleSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface GroupClientSearchQuerySortRequest {
    /** The field to sort by. */
    field: "clientId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export type IncidentSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Incident search filter. */
export interface IncidentFilter {
    /** The process definition ID associated to this incident. */
    processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Incident error type with a defined set of values. */
    errorType?: "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED" | {
      /** Checks for equality with the provided value. */
      $eq?: "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED";
      /** Checks for inequality with the provided value. */
      $neq?: "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED">;
      /** Checks if the property does not match any of the provided values. */
      $notIn?: Array<"AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The error message of this incident. */
    errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The element ID associated to this incident. */
    elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Date of incident creation. */
    creationTime?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** State of this incident with a defined set of values. */
    state?: "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN">;
      /** Checks if the property does not match any of the provided values. */
      $notIn?: Array<"ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The tenant ID of the incident. */
    tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The assigned key, which acts as a unique identifier for this incident. */
    incidentKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The process definition key associated to this incident. */
    processDefinitionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The process instance key associated to this incident. */
    processInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The element instance key associated to this incident. */
    elementInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The job key, if exists, associated with this incident. */
    jobKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
}

/** IncidentErrorTypeEnum with full advanced search capabilities. */
export type IncidentErrorTypeFilterProperty = "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED" | {
    /** Checks for equality with the provided value. */
    $eq?: "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED";
    /** Checks for inequality with the provided value. */
    $neq?: "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED">;
    /** Checks if the property does not match any of the provided values. */
    $notIn?: Array<"AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced IncidentErrorTypeEnum filter */
export interface AdvancedIncidentErrorTypeFilter {
    /** Checks for equality with the provided value. */
    $eq?: "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED";
    /** Checks for inequality with the provided value. */
    $neq?: "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED">;
    /** Checks if the property does not match any of the provided values. */
    $notIn?: Array<"AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** Incident error type with a defined set of values. */
export type IncidentErrorTypeEnum = "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED";

/** IncidentStateEnum with full advanced search capabilities. */
export type IncidentStateFilterProperty = "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN" | {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN">;
    /** Checks if the property does not match any of the provided values. */
    $notIn?: Array<"ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced IncidentStateEnum filter */
export interface AdvancedIncidentStateFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN">;
    /** Checks if the property does not match any of the provided values. */
    $notIn?: Array<"ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** Incident states with a defined set of values. */
export type IncidentStateEnum = "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN";

export interface IncidentSearchQuerySortRequest {
    /** The field to sort by. */
    field: "incidentKey" | "processDefinitionKey" | "processDefinitionId" | "processInstanceKey" | "errorType" | "errorMessage" | "elementId" | "elementInstanceKey" | "creationTime" | "state" | "jobKey" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface IncidentSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface IncidentResult {
    /** The process definition ID associated to this incident. */
    processDefinitionId?: string;
    /** The type of the incident error. */
    errorType?: "AD_HOC_SUB_PROCESS_NO_RETRIES" | "CALLED_DECISION_ERROR" | "CALLED_ELEMENT_ERROR" | "CONDITION_ERROR" | "DECISION_EVALUATION_ERROR" | "EXECUTION_LISTENER_NO_RETRIES" | "EXTRACT_VALUE_ERROR" | "FORM_NOT_FOUND" | "IO_MAPPING_ERROR" | "JOB_NO_RETRIES" | "MESSAGE_SIZE_EXCEEDED" | "RESOURCE_NOT_FOUND" | "TASK_LISTENER_NO_RETRIES" | "UNHANDLED_ERROR_EVENT" | "UNKNOWN" | "UNSPECIFIED";
    /** Error message which describes the error in more detail. */
    errorMessage?: string;
    /** The element ID associated to this incident. */
    elementId?: string;
    /** The creation time of the incident. */
    creationTime?: string;
    /** The incident state. */
    state?: "ACTIVE" | "MIGRATED" | "PENDING" | "RESOLVED" | "UNKNOWN";
    /** The tenant ID of the incident. */
    tenantId: string;
    /** The assigned key, which acts as a unique identifier for this incident. */
    incidentKey?: string;
    /** The process definition key associated to this incident. */
    processDefinitionKey?: string;
    /** The process instance key associated to this incident. */
    processInstanceKey?: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** The element instance key associated to this incident. */
    elementInstanceKey?: string;
    /** The job key, if exists, associated with this incident. */
    jobKey: string;
}

export interface IncidentResolutionRequest {
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

export interface IncidentProcessInstanceStatisticsByErrorQuery {
    /**  Pagination parameters for process instance statistics grouped by incident error. */
    page?: {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
    /** Sorting criteria for process instance statistics grouped by incident error. */
    sort?: Array<{
      /** The field to sort the incident error statistics by. */
      field: "errorMessage" | "activeInstancesWithErrorCount";
      /** The order in which to sort the related field. */
      order?: "ASC" | "DESC";
  }>;
}

export interface IncidentProcessInstanceStatisticsByErrorQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface IncidentProcessInstanceStatisticsByErrorResult {
    /** The hash code identifying a specific incident error.. */
    errorHashCode?: number;
    /** The error message associated with the incident error hash code. */
    errorMessage?: string;
    /**  The number of active process instances that currently have an active incident with this error. */
    activeInstancesWithErrorCount?: number;
}

export interface IncidentProcessInstanceStatisticsByErrorQuerySortRequest {
    /** The field to sort the incident error statistics by. */
    field: "errorMessage" | "activeInstancesWithErrorCount";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface IncidentProcessInstanceStatisticsByDefinitionQuery {
    /** Filter criteria for the aggregated process instance statistics. */
    filter: {
      /**  The error hash code of the incidents to filter the process instance statistics by. */
      errorHashCode: number;
  };
    /** Pagination parameters for the aggregated process instance statistics. */
    page?: {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
    /** Sorting criteria for process instance statistics grouped by process definition. */
    sort?: Array<{
      /** The aggregated field by which the process instance statistics are sorted. */
      field: "activeInstancesWithErrorCount" | "processDefinitionKey" | "tenantId";
      /** The order in which to sort the related field. */
      order?: "ASC" | "DESC";
  }>;
}

export interface IncidentProcessInstanceStatisticsByDefinitionQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface IncidentProcessInstanceStatisticsByDefinitionResult {
    /** Id of a process definition, from the model. Only ids of process definitions that are deployed are useful. */
    processDefinitionId?: string;
    /** System-generated key for a deployed process definition. */
    processDefinitionKey?: string;
    /** The name of the process definition. */
    processDefinitionName?: string;
    /** The version of the process definition. */
    processDefinitionVersion?: number;
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /**  The number of active process instances that currently have an incident
 with the specified error hash code. */
    activeInstancesWithErrorCount?: number;
}

/** Filter for the incident process instance statistics by definition query. */
export interface IncidentProcessInstanceStatisticsByDefinitionFilter {
    /**  The error hash code of the incidents to filter the process instance statistics by. */
    errorHashCode: number;
}

export interface IncidentProcessInstanceStatisticsByDefinitionQuerySortRequest {
    /** The aggregated field by which the process instance statistics are sorted. */
    field: "activeInstancesWithErrorCount" | "processDefinitionKey" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface JobActivationRequest {
    /** The job type, as defined in the BPMN process (e.g. <zeebe:taskDefinition type="payment-service" />) */
    "type": string;
    /** The name of the worker activating the jobs, mostly used for logging purposes. */
    worker?: string;
    /** A job returned after this call will not be activated by another call until the timeout (in ms) has been reached. */
    timeout: number;
    /** The maximum jobs to activate by this request. */
    maxJobsToActivate: number;
    /** A list of variables to fetch as the job variables; if empty, all visible variables at the time of activation for the scope of the job will be returned. */
    fetchVariable?: Array<string>;
    /** The request will be completed when at least one job is activated or after the requestTimeout (in ms). If the requestTimeout = 0, a default timeout is used. If the requestTimeout < 0, long polling is disabled and the request is completed immediately, even when no job is activated. */
    requestTimeout?: number;
    /** A list of IDs of tenants for which to activate jobs. */
    tenantIds?: Array<string>;
    /** The tenant filtering strategy - determines whether to use provided tenant IDs or assigned tenant IDs from the authenticated principal's authorized tenants. */
    tenantFilter?: "PROVIDED" | "ASSIGNED";
}

/** The list of activated jobs */
export interface JobActivationResult {
    /** The activated jobs. */
    jobs: Array<{
      /** The type of the job (should match what was requested). */
      "type": string;
      /** The bpmn process ID of the job's process definition. */
      processDefinitionId: string;
      /** The version of the job's process definition. */
      processDefinitionVersion: number;
      /** The associated task element ID. */
      elementId: string;
      /** A set of custom headers defined during modelling; returned as a serialized JSON document. */
      customHeaders: {
        [key: string]: unknown;
    };
      /** The name of the worker which activated this job. */
      worker: string;
      /** The amount of retries left to this job (should always be positive). */
      retries: number;
      /** When the job can be activated again, sent as a UNIX epoch timestamp. */
      deadline: number;
      /** All variables visible to the task scope, computed at activation time. */
      variables: {
        [key: string]: unknown;
    };
      /** The ID of the tenant that owns the job. */
      tenantId: string;
      /** The key, a unique identifier for the job. */
      jobKey: string;
      /** The job's process instance key. */
      processInstanceKey: string;
      /** The key of the job's process definition. */
      processDefinitionKey: string;
      /** The element instance key of the task. */
      elementInstanceKey: string;
      /** The job kind. */
      kind: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";
      /** The listener event type of the job. */
      listenerEventType: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";
      /**  User task properties, if the job is a user task.
 This is `null` if the job is not a user task. */
      userTask?: {
        /** The action performed on the user task. */
        action?: string;
        /** The user assigned to the task. */
        assignee?: string | null;
        /** The groups eligible to claim the task. */
        candidateGroups: Array<string>;
        /** The users eligible to claim the task. */
        candidateUsers: Array<string>;
        /** The attributes that were changed in the task. */
        changedAttributes: Array<string>;
        /** The due date of the user task in ISO 8601 format. */
        dueDate?: string | null;
        /** The follow-up date of the user task in ISO 8601 format. */
        followUpDate?: string | null;
        /** The key of the form associated with the user task. */
        formKey?: string;
        /** The priority of the user task. */
        priority?: number | null;
        /** The unique key identifying the user task. */
        userTaskKey?: string;
    };
      /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
      tags: Array<string>;
      /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
      rootProcessInstanceKey: string;
  }>;
}

export interface ActivatedJobResult {
    /** The type of the job (should match what was requested). */
    "type": string;
    /** The bpmn process ID of the job's process definition. */
    processDefinitionId: string;
    /** The version of the job's process definition. */
    processDefinitionVersion: number;
    /** The associated task element ID. */
    elementId: string;
    /** A set of custom headers defined during modelling; returned as a serialized JSON document. */
    customHeaders: {
      [key: string]: unknown;
  };
    /** The name of the worker which activated this job. */
    worker: string;
    /** The amount of retries left to this job (should always be positive). */
    retries: number;
    /** When the job can be activated again, sent as a UNIX epoch timestamp. */
    deadline: number;
    /** All variables visible to the task scope, computed at activation time. */
    variables: {
      [key: string]: unknown;
  };
    /** The ID of the tenant that owns the job. */
    tenantId: string;
    /** The key, a unique identifier for the job. */
    jobKey: string;
    /** The job's process instance key. */
    processInstanceKey: string;
    /** The key of the job's process definition. */
    processDefinitionKey: string;
    /** The element instance key of the task. */
    elementInstanceKey: string;
    /** The job kind. */
    kind: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";
    /** The listener event type of the job. */
    listenerEventType: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";
    /**  User task properties, if the job is a user task.
 This is `null` if the job is not a user task. */
    userTask?: {
      /** The action performed on the user task. */
      action?: string;
      /** The user assigned to the task. */
      assignee?: string | null;
      /** The groups eligible to claim the task. */
      candidateGroups: Array<string>;
      /** The users eligible to claim the task. */
      candidateUsers: Array<string>;
      /** The attributes that were changed in the task. */
      changedAttributes: Array<string>;
      /** The due date of the user task in ISO 8601 format. */
      dueDate?: string | null;
      /** The follow-up date of the user task in ISO 8601 format. */
      followUpDate?: string | null;
      /** The key of the form associated with the user task. */
      formKey?: string;
      /** The priority of the user task. */
      priority?: number | null;
      /** The unique key identifying the user task. */
      userTaskKey?: string;
  };
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags: Array<string>;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
}

/** Contains properties of a user task. */
export interface UserTaskProperties {
    /** The action performed on the user task. */
    action?: string;
    /** The user assigned to the task. */
    assignee?: string | null;
    /** The groups eligible to claim the task. */
    candidateGroups: Array<string>;
    /** The users eligible to claim the task. */
    candidateUsers: Array<string>;
    /** The attributes that were changed in the task. */
    changedAttributes: Array<string>;
    /** The due date of the user task in ISO 8601 format. */
    dueDate?: string | null;
    /** The follow-up date of the user task in ISO 8601 format. */
    followUpDate?: string | null;
    /** The key of the form associated with the user task. */
    formKey?: string;
    /** The priority of the user task. */
    priority?: number | null;
    /** The unique key identifying the user task. */
    userTaskKey?: string;
}

/** Job search request. */
export type JobSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface JobSearchQuerySortRequest {
    /** The field to sort by. */
    field: "deadline" | "deniedReason" | "elementId" | "elementInstanceKey" | "endTime" | "errorCode" | "errorMessage" | "hasFailedWithRetriesLeft" | "isDenied" | "jobKey" | "kind" | "listenerEventType" | "processDefinitionId" | "processDefinitionKey" | "processInstanceKey" | "retries" | "state" | "tenantId" | "type" | "worker";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Job search filter. */
export interface JobFilter {
    /** When the job can next be activated. */
    deadline?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The reason provided by the user task listener for denying the work. */
    deniedReason?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The element ID associated with the job. */
    elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The element instance key associated with the job. */
    elementInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** When the job ended. */
    endTime?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The error code provided for the failed job. */
    errorCode?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The error message that provides additional context for a failed job. */
    errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Indicates whether the job has failed with retries left. */
    hasFailedWithRetriesLeft?: boolean;
    /** Indicates whether the user task listener denies the work. */
    isDenied?: boolean | null;
    /** The key, a unique identifier for the job. */
    jobKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The kind of the job. */
    kind?: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS" | {
      /** Checks for equality with the provided value. */
      $eq?: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";
      /** Checks for inequality with the provided value. */
      $neq?: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The listener event type of the job. */
    listenerEventType?: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING" | {
      /** Checks for equality with the provided value. */
      $eq?: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";
      /** Checks for inequality with the provided value. */
      $neq?: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The process definition ID associated with the job. */
    processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The process definition key associated with the job. */
    processDefinitionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The process instance key associated with the job. */
    processInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The number of retries left. */
    retries?: number | {
      /** Checks for equality with the provided value. */
      $eq?: number;
      /** Checks for inequality with the provided value. */
      $neq?: number;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: number;
      /** Greater than or equal comparison with the provided value. */
      $gte?: number;
      /** Lower than comparison with the provided value. */
      $lt?: number;
      /** Lower than or equal comparison with the provided value. */
      $lte?: number;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<number>;
  };
    /** The state of the job. */
    state?: "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT" | {
      /** Checks for equality with the provided value. */
      $eq?: "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT";
      /** Checks for inequality with the provided value. */
      $neq?: "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The tenant ID. */
    tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The type of the job. */
    "type"?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The name of the worker for this job. */
    worker?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** When the job was created. Field is present for jobs created after 8.9. */
    creationTime?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** When the job was last updated. Field is present for jobs created after 8.9. */
    lastUpdateTime?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
}

/** Job search response. */
export interface JobSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface JobSearchResult {
    /** A set of custom headers defined during modelling. */
    customHeaders: {
      [key: string]: string;
  };
    /** If the job has been activated, when it will next be available to be activated. */
    deadline?: string | null;
    /** The reason provided by the user task listener for denying the work. */
    deniedReason?: string | null;
    /** The element ID associated with the job. May be missing on job failure. */
    elementId: string;
    /** The element instance key associated with the job. */
    elementInstanceKey: string;
    /**  End date of the job.
 This is `null` if the job is not in an end state yet. */
    endTime?: string | null;
    /** The error code provided for a failed job. */
    errorCode?: string | null;
    /** The error message that provides additional context for a failed job. */
    errorMessage?: string | null;
    /** Indicates whether the job has failed with retries left. */
    hasFailedWithRetriesLeft: boolean;
    /** Indicates whether the user task listener denies the work. */
    isDenied?: boolean | null;
    /** The key, a unique identifier for the job. */
    jobKey: string;
    /** The job kind. */
    kind: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";
    /** The listener event type of the job. */
    listenerEventType: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";
    /** The process definition ID associated with the job. */
    processDefinitionId: string;
    /** The process definition key associated with the job. */
    processDefinitionKey: string;
    /** The process instance key associated with the job. */
    processInstanceKey: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** The amount of retries left to this job. */
    retries: number;
    /** The state of the job. */
    state: "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT";
    /** The unique identifier of the tenant. */
    tenantId: string;
    /** The type of the job. */
    "type": string;
    /** The name of the worker of this job. */
    worker: string;
    /** When the job was created. Field is present for jobs created after 8.9. */
    creationTime?: string;
    /** When the job was last updated. Field is present for jobs created after 8.9. */
    lastUpdateTime?: string;
}

export interface JobFailRequest {
    /** The amount of retries the job should have left */
    retries?: number;
    /** An optional error message describing why the job failed; if not provided, an empty string is used. */
    errorMessage?: string;
    /** An optional retry back off for the failed job. The job will not be retryable before the current time plus the back off time. The default is 0 which means the job is retryable immediately. */
    retryBackOff?: number;
    /** JSON object that will instantiate the variables at the local scope of the job's associated task. */
    variables?: {
      [key: string]: unknown;
  };
}

export type JobErrorRequest = {
    /** The error code that will be matched with an error catch event. */
    errorCode: string;
    /** An error message that provides additional context. */
    errorMessage?: string | null;
    /** JSON object that will instantiate the variables at the local scope of the error catch event that catches the thrown error. */
    variables?: {
      [key: string]: unknown;
  } | null;
};

export type JobCompletionRequest = {
    /** The variables to complete the job with. */
    variables?: {
      [key: string]: unknown;
  } | null;
    /** The result of the completed job as determined by the worker. */
    result?: {
      /** Indicates whether the worker denies the work, i.e. explicitly doesn't approve it. For example, a user task listener can deny the completion of a task by setting this flag to true. In this example, the completion of a task is represented by a job that the worker can complete as denied. As a result, the completion request is rejected and the task remains active. Defaults to false. */
      denied?: boolean | null;
      /** The reason provided by the user task listener for denying the work. */
      deniedReason?: string | null;
      /**  JSON object with attributes that were corrected by the worker.

 The following attributes can be corrected, additional attributes will be ignored:

 * `assignee` - clear by providing an empty String
 * `dueDate` - clear by providing an empty String
 * `followUpDate` - clear by providing an empty String
 * `candidateGroups` - clear by providing an empty list
 * `candidateUsers` - clear by providing an empty list
 * `priority` - minimum 0, maximum 100, default 50

 Providing any of those attributes with a `null` value or omitting it preserves
 the persisted attribute's value. */
      corrections?: {
        /** Assignee of the task. */
        assignee?: string | null;
        /** The due date of the task. */
        dueDate?: string | null;
        /** The follow-up date of the task. */
        followUpDate?: string | null;
        /** The list of candidate users of the task. */
        candidateUsers?: Array<string> | null;
        /** The list of candidate groups of the task. */
        candidateGroups?: Array<string> | null;
        /** The priority of the task. */
        priority?: number | null;
    } | null;
      /** Used to distinguish between different types of job results. */
      "type"?: string;
  } | null | {
      /** Indicates which elements need to be activated in the ad-hoc subprocess. */
      activateElements?: Array<{
        /** The element ID to activate. */
        elementId?: string;
        /** Variables for the element. */
        variables?: {
          [key: string]: unknown;
      } | null;
    }>;
      /** Indicates whether the completion condition of the ad-hoc subprocess is fulfilled. */
      isCompletionConditionFulfilled?: boolean;
      /** Indicates whether the remaining instances of the ad-hoc subprocess should be canceled. */
      isCancelRemainingInstances?: boolean;
      /** Used to distinguish between different types of job results. */
      "type"?: string;
  } | null;
};

/** The result of the completed job as determined by the worker. */
export type JobResult = {
    /** Indicates whether the worker denies the work, i.e. explicitly doesn't approve it. For example, a user task listener can deny the completion of a task by setting this flag to true. In this example, the completion of a task is represented by a job that the worker can complete as denied. As a result, the completion request is rejected and the task remains active. Defaults to false. */
    denied?: boolean | null;
    /** The reason provided by the user task listener for denying the work. */
    deniedReason?: string | null;
    /**  JSON object with attributes that were corrected by the worker.

 The following attributes can be corrected, additional attributes will be ignored:

 * `assignee` - clear by providing an empty String
 * `dueDate` - clear by providing an empty String
 * `followUpDate` - clear by providing an empty String
 * `candidateGroups` - clear by providing an empty list
 * `candidateUsers` - clear by providing an empty list
 * `priority` - minimum 0, maximum 100, default 50

 Providing any of those attributes with a `null` value or omitting it preserves
 the persisted attribute's value. */
    corrections?: {
      /** Assignee of the task. */
      assignee?: string | null;
      /** The due date of the task. */
      dueDate?: string | null;
      /** The follow-up date of the task. */
      followUpDate?: string | null;
      /** The list of candidate users of the task. */
      candidateUsers?: Array<string> | null;
      /** The list of candidate groups of the task. */
      candidateGroups?: Array<string> | null;
      /** The priority of the task. */
      priority?: number | null;
  } | null;
    /** Used to distinguish between different types of job results. */
    "type"?: string;
} | null | {
    /** Indicates which elements need to be activated in the ad-hoc subprocess. */
    activateElements?: Array<{
      /** The element ID to activate. */
      elementId?: string;
      /** Variables for the element. */
      variables?: {
        [key: string]: unknown;
    } | null;
  }>;
    /** Indicates whether the completion condition of the ad-hoc subprocess is fulfilled. */
    isCompletionConditionFulfilled?: boolean;
    /** Indicates whether the remaining instances of the ad-hoc subprocess should be canceled. */
    isCancelRemainingInstances?: boolean;
    /** Used to distinguish between different types of job results. */
    "type"?: string;
} | null;

/** Job result details for a user task completion, optionally including a denial reason and corrected task properties. */
export type JobResultUserTask = {
    /** Indicates whether the worker denies the work, i.e. explicitly doesn't approve it. For example, a user task listener can deny the completion of a task by setting this flag to true. In this example, the completion of a task is represented by a job that the worker can complete as denied. As a result, the completion request is rejected and the task remains active. Defaults to false. */
    denied?: boolean | null;
    /** The reason provided by the user task listener for denying the work. */
    deniedReason?: string | null;
    /**  JSON object with attributes that were corrected by the worker.

 The following attributes can be corrected, additional attributes will be ignored:

 * `assignee` - clear by providing an empty String
 * `dueDate` - clear by providing an empty String
 * `followUpDate` - clear by providing an empty String
 * `candidateGroups` - clear by providing an empty list
 * `candidateUsers` - clear by providing an empty list
 * `priority` - minimum 0, maximum 100, default 50

 Providing any of those attributes with a `null` value or omitting it preserves
 the persisted attribute's value. */
    corrections?: {
      /** Assignee of the task. */
      assignee?: string | null;
      /** The due date of the task. */
      dueDate?: string | null;
      /** The follow-up date of the task. */
      followUpDate?: string | null;
      /** The list of candidate users of the task. */
      candidateUsers?: Array<string> | null;
      /** The list of candidate groups of the task. */
      candidateGroups?: Array<string> | null;
      /** The priority of the task. */
      priority?: number | null;
  } | null;
    /** Used to distinguish between different types of job results. */
    "type"?: string;
} | null;

/**  JSON object with attributes that were corrected by the worker.
 * 
 *  The following attributes can be corrected, additional attributes will be ignored:
 * 
 *  * `assignee` - clear by providing an empty String
 *  * `dueDate` - clear by providing an empty String
 *  * `followUpDate` - clear by providing an empty String
 *  * `candidateGroups` - clear by providing an empty list
 *  * `candidateUsers` - clear by providing an empty list
 *  * `priority` - minimum 0, maximum 100, default 50
 * 
 *  Providing any of those attributes with a `null` value or omitting it preserves
 *  the persisted attribute's value. */
export type JobResultCorrections = {
    /** Assignee of the task. */
    assignee?: string | null;
    /** The due date of the task. */
    dueDate?: string | null;
    /** The follow-up date of the task. */
    followUpDate?: string | null;
    /** The list of candidate users of the task. */
    candidateUsers?: Array<string> | null;
    /** The list of candidate groups of the task. */
    candidateGroups?: Array<string> | null;
    /** The priority of the task. */
    priority?: number | null;
} | null;

/** Job result details for an ad‑hoc sub‑process, including elements to activate and flags indicating completion or cancellation behavior. */
export type JobResultAdHocSubProcess = {
    /** Indicates which elements need to be activated in the ad-hoc subprocess. */
    activateElements?: Array<{
      /** The element ID to activate. */
      elementId?: string;
      /** Variables for the element. */
      variables?: {
        [key: string]: unknown;
    } | null;
  }>;
    /** Indicates whether the completion condition of the ad-hoc subprocess is fulfilled. */
    isCompletionConditionFulfilled?: boolean;
    /** Indicates whether the remaining instances of the ad-hoc subprocess should be canceled. */
    isCancelRemainingInstances?: boolean;
    /** Used to distinguish between different types of job results. */
    "type"?: string;
} | null;

/** Instruction to activate a single BPMN element within an ad‑hoc sub‑process, optionally providing variables scoped to that element. */
export type JobResultActivateElement = {
    /** The element ID to activate. */
    elementId?: string;
    /** Variables for the element. */
    variables?: {
      [key: string]: unknown;
  } | null;
};

export interface JobUpdateRequest {
    /** JSON object with changed job attribute values. The job cannot be completed or failed with this endpoint, use the complete job or fail job endpoints instead. */
    changeset: {
      /** The new number of retries for the job. */
      retries?: number | null;
      /** The new timeout for the job in milliseconds. */
      timeout?: number | null;
  };
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

/** JSON object with changed job attribute values. The job cannot be completed or failed with this endpoint, use the complete job or fail job endpoints instead. */
export interface JobChangeset {
    /** The new number of retries for the job. */
    retries?: number | null;
    /** The new timeout for the job in milliseconds. */
    timeout?: number | null;
}

/** The tenant filtering strategy for job activation. Determines whether to use tenant IDs provided in the request or tenant IDs assigned to the authenticated principal. */
export type TenantFilterEnum = "PROVIDED" | "ASSIGNED";

/** The state of the job. */
export type JobStateEnum = "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT";

/** The job kind. */
export type JobKindEnum = "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";

/** The listener event type of the job. */
export type JobListenerEventTypeEnum = "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";

/** JobKindEnum property with full advanced search capabilities. */
export type JobKindFilterProperty = "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS" | {
    /** Checks for equality with the provided value. */
    $eq?: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";
    /** Checks for inequality with the provided value. */
    $neq?: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced JobKindEnum filter. */
export interface AdvancedJobKindFilter {
    /** Checks for equality with the provided value. */
    $eq?: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";
    /** Checks for inequality with the provided value. */
    $neq?: "BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"BPMN_ELEMENT" | "EXECUTION_LISTENER" | "TASK_LISTENER" | "AD_HOC_SUB_PROCESS">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** JobListenerEventTypeEnum property with full advanced search capabilities. */
export type JobListenerEventTypeFilterProperty = "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING" | {
    /** Checks for equality with the provided value. */
    $eq?: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";
    /** Checks for inequality with the provided value. */
    $neq?: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced JobListenerEventTypeEnum filter. */
export interface AdvancedJobListenerEventTypeFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";
    /** Checks for inequality with the provided value. */
    $neq?: "ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ASSIGNING" | "CANCELING" | "COMPLETING" | "CREATING" | "END" | "START" | "UNSPECIFIED" | "UPDATING">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** JobStateEnum property with full advanced search capabilities. */
export type JobStateFilterProperty = "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT" | {
    /** Checks for equality with the provided value. */
    $eq?: "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT";
    /** Checks for inequality with the provided value. */
    $neq?: "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced JobStateEnum filter. */
export interface AdvancedJobStateFilter {
    /** Checks for equality with the provided value. */
    $eq?: "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT";
    /** Checks for inequality with the provided value. */
    $neq?: "CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"CANCELED" | "COMPLETED" | "CREATED" | "ERROR_THROWN" | "FAILED" | "MIGRATED" | "RETRIES_UPDATED" | "TIMED_OUT">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** Global job statistics query result. */
export interface GlobalJobStatisticsQueryResult {
    /** Metric for a single job status. */
    created: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** Metric for a single job status. */
    completed: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** Metric for a single job status. */
    failed: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** True if some data is missing because internal limits were reached and some metrics were not recorded. */
    isIncomplete: boolean;
}

/** Metric for a single job status. */
export interface StatusMetric {
    /** Number of jobs in this status. */
    count: number;
    /** ISO 8601 timestamp of the last update for this status. */
    lastUpdatedAt: string | null;
}

/** Job type statistics query. */
export interface JobTypeStatisticsQuery {
    /** Job type statistics search filter. */
    filter?: {
      /**  Start of the time window to filter metrics. ISO 8601 date-time format. */
      from: string;
      /**  End of the time window to filter metrics. ISO 8601 date-time format. */
      to: string;
      /**  Optional job type filter with advanced search capabilities.
 Supports exact match, pattern matching, and other operators. */
      jobType?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  };
    /** Search cursor pagination. */
    page?: {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
}

/** Job type statistics search filter. */
export interface JobTypeStatisticsFilter {
    /**  Start of the time window to filter metrics. ISO 8601 date-time format. */
    from: string;
    /**  End of the time window to filter metrics. ISO 8601 date-time format. */
    to: string;
    /**  Optional job type filter with advanced search capabilities.
 Supports exact match, pattern matching, and other operators. */
    jobType?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
}

/** Job type statistics query result. */
export interface JobTypeStatisticsQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

/** Statistics for a single job type. */
export interface JobTypeStatisticsItem {
    /** The job type identifier. */
    jobType: string;
    /** Metric for a single job status. */
    created: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** Metric for a single job status. */
    completed: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** Metric for a single job status. */
    failed: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** Number of distinct workers observed for this job type. */
    workers: number;
}

/** Job worker statistics query. */
export interface JobWorkerStatisticsQuery {
    /** Job worker statistics search filter. */
    filter: {
      /**  Start of the time window to filter metrics. ISO 8601 date-time format. */
      from: string;
      /**  End of the time window to filter metrics. ISO 8601 date-time format. */
      to: string;
      /** Job type to return worker metrics for. */
      jobType: string;
  };
    /** Search cursor pagination. */
    page?: {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
}

/** Job worker statistics search filter. */
export interface JobWorkerStatisticsFilter {
    /**  Start of the time window to filter metrics. ISO 8601 date-time format. */
    from: string;
    /**  End of the time window to filter metrics. ISO 8601 date-time format. */
    to: string;
    /** Job type to return worker metrics for. */
    jobType: string;
}

/** Job worker statistics query result. */
export interface JobWorkerStatisticsQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

/** Statistics for a single worker within a job type. */
export interface JobWorkerStatisticsItem {
    /** The worker identifier. */
    worker: string;
    /** Metric for a single job status. */
    created: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** Metric for a single job status. */
    completed: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** Metric for a single job status. */
    failed: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
}

/** Job time-series statistics query. */
export interface JobTimeSeriesStatisticsQuery {
    /** Job time-series statistics search filter. */
    filter: {
      /**  Start of the time window to filter metrics. ISO 8601 date-time format. */
      from: string;
      /**  End of the time window to filter metrics. ISO 8601 date-time format. */
      to: string;
      /** Job type to return time-series metrics for. */
      jobType: string;
      /**  Time bucket resolution as an ISO 8601 duration (for example `PT1M` for 1 minute,
 `PT1H` for 1 hour). If omitted, the server chooses a sensible default. */
      resolution?: string;
  };
    /** Search cursor pagination. */
    page?: {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
}

/** Job time-series statistics search filter. */
export interface JobTimeSeriesStatisticsFilter {
    /**  Start of the time window to filter metrics. ISO 8601 date-time format. */
    from: string;
    /**  End of the time window to filter metrics. ISO 8601 date-time format. */
    to: string;
    /** Job type to return time-series metrics for. */
    jobType: string;
    /**  Time bucket resolution as an ISO 8601 duration (for example `PT1M` for 1 minute,
 `PT1H` for 1 hour). If omitted, the server chooses a sensible default. */
    resolution?: string;
}

/** Job time-series statistics query result. */
export interface JobTimeSeriesStatisticsQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

/** Aggregated job metrics for a single time bucket. */
export interface JobTimeSeriesStatisticsItem {
    /** ISO 8601 timestamp representing the start of this time bucket. */
    time: string;
    /** Metric for a single job status. */
    created: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** Metric for a single job status. */
    completed: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
    /** Metric for a single job status. */
    failed: {
      /** Number of jobs in this status. */
      count: number;
      /** ISO 8601 timestamp of the last update for this status. */
      lastUpdatedAt: string | null;
  };
}

/** The response of a license request. */
export interface LicenseResponse {
    /** True if the Camunda license is valid, false if otherwise */
    validLicense: boolean;
    /** Will return the license type property of the Camunda license */
    licenseType: string;
    /** Will be false when a license contains a non-commerical=true property */
    isCommercial: boolean;
    /** The date when the Camunda license expires */
    expiresAt: string | null;
}

export interface MappingRuleCreateUpdateRequest {
    /** The name of the claim to map. */
    claimName: string;
    /** The value of the claim to map. */
    claimValue: string;
    /** The name of the mapping rule. */
    name: string;
}

export interface MappingRuleCreateRequest {
    /** The name of the claim to map. */
    claimName: string;
    /** The value of the claim to map. */
    claimValue: string;
    /** The name of the mapping rule. */
    name: string;
}

export interface MappingRuleUpdateRequest {
    /** The name of the claim to map. */
    claimName: string;
    /** The value of the claim to map. */
    claimValue: string;
    /** The name of the mapping rule. */
    name: string;
}

export interface MappingRuleCreateUpdateResult {
    /** The name of the claim to map. */
    claimName?: string;
    /** The value of the claim to map. */
    claimValue?: string;
    /** The name of the mapping rule. */
    name?: string;
    /** The unique ID of the mapping rule. */
    mappingRuleId?: string;
}

export interface MappingRuleCreateResult {
    /** The name of the claim to map. */
    claimName?: string;
    /** The value of the claim to map. */
    claimValue?: string;
    /** The name of the mapping rule. */
    name?: string;
    /** The unique ID of the mapping rule. */
    mappingRuleId?: string;
}

export interface MappingRuleUpdateResult {
    /** The name of the claim to map. */
    claimName?: string;
    /** The value of the claim to map. */
    claimValue?: string;
    /** The name of the mapping rule. */
    name?: string;
    /** The unique ID of the mapping rule. */
    mappingRuleId?: string;
}

export interface MappingRuleSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface MappingRuleResult {
    /** The name of the claim to map. */
    claimName?: string;
    /** The value of the claim to map. */
    claimValue?: string;
    /** The name of the mapping rule. */
    name?: string;
    /** The ID of the mapping rule. */
    mappingRuleId?: string;
}

export interface MappingRuleSearchQuerySortRequest {
    /** The field to sort by. */
    field: "mappingRuleId" | "claimName" | "claimValue" | "name";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export type MappingRuleSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Mapping rule search filter. */
export interface MappingRuleFilter {
    /** The claim name to match against a token. */
    claimName?: string;
    /** The value of the claim to match. */
    claimValue?: string;
    /** The name of the mapping rule. */
    name?: string;
    /** The ID of the mapping rule. */
    mappingRuleId?: string;
}

export interface ProcessDefinitionSearchQuerySortRequest {
    /** The field to sort by. */
    field: "processDefinitionKey" | "name" | "resourceName" | "version" | "versionTag" | "processDefinitionId" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export type ProcessDefinitionSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Process definition search filter. */
export interface ProcessDefinitionFilter {
    /** Name of this process definition. */
    name?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /**  Whether to only return the latest version of each process definition.
 When using this filter, pagination functionality is limited, you can only paginate forward using `after` and `limit`.
 The response contains no `startCursor` in the `page`, and requests ignore the `from` and `before` in the `page`. */
    isLatestVersion?: boolean;
    /** Resource name of this process definition. */
    resourceName?: string;
    /** Version of this process definition. */
    version?: number;
    /** Version tag of this process definition. */
    versionTag?: string;
    /** Process definition ID of this process definition. */
    processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Tenant ID of this process definition. */
    tenantId?: string;
    /** The key for this process definition. */
    processDefinitionKey?: string;
    /** Indicates whether the start event of the process has an associated Form Key. */
    hasStartForm?: boolean;
}

export interface ProcessDefinitionSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface ProcessDefinitionResult {
    /** Name of this process definition. */
    name?: string | null;
    /** Resource name for this process definition. */
    resourceName?: string;
    /** Version of this process definition. */
    version?: number;
    /** Version tag of this process definition. */
    versionTag?: string | null;
    /** Process definition ID of this process definition. */
    processDefinitionId?: string;
    /** Tenant ID of this process definition. */
    tenantId?: string;
    /** The key for this process definition. */
    processDefinitionKey?: string;
    /** Indicates whether the start event of the process has an associated Form Key. */
    hasStartForm?: boolean;
}

/** Process definition element statistics request. */
export interface ProcessDefinitionElementStatisticsQuery {
    /** The process definition statistics search filters. */
    filter?: {
  startDate?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: string;
        /** Greater than or equal comparison with the provided value. */
        $gte?: string;
        /** Lower than comparison with the provided value. */
        $lt?: string;
        /** Lower than or equal comparison with the provided value. */
        $lte?: string;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
    };
  endDate?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: string;
        /** Greater than or equal comparison with the provided value. */
        $gte?: string;
        /** Lower than comparison with the provided value. */
        $lt?: string;
        /** Lower than or equal comparison with the provided value. */
        $lte?: string;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
    };
  state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
        /** Checks for equality with the provided value. */
        $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks for inequality with the provided value. */
        $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
        /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
        $like?: string;
    };
  hasIncident?: boolean;
  tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  variables?: Array<{
        /** Name of the variable. */
        name: string;
        /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
        value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    }>;
  processInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
  parentProcessInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
  parentElementInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
  batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  hasRetriesLeft?: boolean;
  elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
        /** Checks for equality with the provided value. */
        $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks for inequality with the provided value. */
        $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
        /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
        $like?: string;
    };
  elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  hasElementInstanceIncident?: boolean;
  incidentErrorHashCode?: number | {
        /** Checks for equality with the provided value. */
        $eq?: number;
        /** Checks for inequality with the provided value. */
        $neq?: number;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: number;
        /** Greater than or equal comparison with the provided value. */
        $gte?: number;
        /** Lower than comparison with the provided value. */
        $lt?: number;
        /** Lower than or equal comparison with the provided value. */
        $lte?: number;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<number>;
    };
  tags?: Array<string>;
  $or?: Array<{
        /** The start date. */
        startDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The end date. */
        endDate?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: string;
          /** Greater than or equal comparison with the provided value. */
          $gte?: string;
          /** Lower than comparison with the provided value. */
          $lt?: string;
          /** Lower than or equal comparison with the provided value. */
          $lte?: string;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
      };
        /** The process instance state. */
        state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** Whether this process instance has a related incident or not. */
        hasIncident?: boolean;
        /** The tenant id. */
        tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The process instance variables. */
        variables?: Array<{
          /** Name of the variable. */
          name: string;
          /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
          value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      }>;
        /** The key of this process instance. */
        processInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent process instance key. */
        parentProcessInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The parent element instance key. */
        parentElementInstanceKey?: string | {
          /** Checks for equality with the provided value. */
          $eq?: string;
          /** Checks for inequality with the provided value. */
          $neq?: string;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<string>;
          /** Checks if the property matches none of the provided values. */
          $notIn?: Array<string>;
      };
        /** The batch operation id. */
        batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** The error message related to the process. */
        errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the process has failed jobs with retries left. */
        hasRetriesLeft?: boolean;
        /** The state of the element instances associated with the process instance. */
        elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
          /** Checks for equality with the provided value. */
          $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks for inequality with the provided value. */
          $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
          /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
          $like?: string;
      };
        /** The element id associated with the process instance. */
        elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
        /** Whether the element instance has an incident or not. */
        hasElementInstanceIncident?: boolean;
        /** The incident error hash code, associated with this process. */
        incidentErrorHashCode?: number | {
          /** Checks for equality with the provided value. */
          $eq?: number;
          /** Checks for inequality with the provided value. */
          $neq?: number;
          /** Checks if the current property exists. */
          $exists?: boolean;
          /** Greater than comparison with the provided value. */
          $gt?: number;
          /** Greater than or equal comparison with the provided value. */
          $gte?: number;
          /** Lower than comparison with the provided value. */
          $lt?: number;
          /** Lower than or equal comparison with the provided value. */
          $lte?: number;
          /** Checks if the property matches any of the provided values. */
          $in?: Array<number>;
      };
        /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
        tags?: Array<string>;
    }>;
};
}

/** Process definition element statistics query response. */
export interface ProcessDefinitionElementStatisticsQueryResult {
    /** The element statistics. */
    items: Array<{
      /** The element ID for which the results are aggregated. */
      elementId?: string;
      /** The total number of active instances of the element. */
      active?: number;
      /** The total number of canceled instances of the element. */
      canceled?: number;
      /** The total number of incidents for the element. */
      incidents?: number;
      /** The total number of completed instances of the element. */
      completed?: number;
  }>;
}

/** Process element statistics response. */
export interface ProcessElementStatisticsResult {
    /** The element ID for which the results are aggregated. */
    elementId?: string;
    /** The total number of active instances of the element. */
    active?: number;
    /** The total number of canceled instances of the element. */
    canceled?: number;
    /** The total number of incidents for the element. */
    incidents?: number;
    /** The total number of completed instances of the element. */
    completed?: number;
}

export interface ProcessDefinitionMessageSubscriptionStatisticsQuery {
    /** Search cursor pagination. */
    page?: {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
    /** The message subscription filters. */
    filter?: {
      /** The message subscription key associated with this message subscription. */
      messageSubscriptionKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for equality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The process definition key associated with this correlated message subscription. This only works for data created with 8.9 and later. */
      processDefinitionKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The process definition ID associated with this message subscription. */
      processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** The process instance key associated with this message subscription. */
      processInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The element ID associated with this message subscription. */
      elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** The element instance key associated with this message subscription. */
      elementInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The message subscription state. */
      messageSubscriptionState?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED" | {
        /** Checks for equality with the provided value. */
        $eq?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";
        /** Checks for inequality with the provided value. */
        $neq?: "CORRELATED" | "CREATED" | "DELETED" | "MIGRATED";
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<"CORRELATED" | "CREATED" | "DELETED" | "MIGRATED">;
        /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
        $like?: string;
    };
      /** The last updated date of the message subscription. */
      lastUpdatedDate?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: string;
        /** Greater than or equal comparison with the provided value. */
        $gte?: string;
        /** Lower than comparison with the provided value. */
        $lt?: string;
        /** Lower than or equal comparison with the provided value. */
        $lte?: string;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
    };
      /** The name of the message associated with the message subscription. */
      messageName?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** The correlation key of the message subscription. */
      correlationKey?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** The unique external tenant ID. */
      tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  };
}

export interface ProcessDefinitionMessageSubscriptionStatisticsQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface ProcessDefinitionMessageSubscriptionStatisticsResult {
    /** The process definition ID associated with this message subscription. */
    processDefinitionId?: string;
    /** The tenant ID associated with this message subscription. */
    tenantId?: string;
    /** The process definition key associated with this message subscription. */
    processDefinitionKey?: string;
    /** The number of process instances with active message subscriptions. */
    processInstancesWithActiveSubscriptions?: number;
    /** The total number of active message subscriptions for this process definition key. */
    activeSubscriptions?: number;
}

export interface ProcessDefinitionInstanceStatisticsQuery {
    /** Search cursor pagination. */
    page?: {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
    /** Sort field criteria. */
    sort?: Array<{
      /** The field to sort by. */
      field: "processDefinitionId" | "activeInstancesWithIncidentCount" | "activeInstancesWithoutIncidentCount";
      /** The order in which to sort the related field. */
      order?: "ASC" | "DESC";
  }>;
}

export interface ProcessDefinitionInstanceStatisticsQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

/** Process definition instance statistics response. */
export interface ProcessDefinitionInstanceStatisticsResult {
    /** Id of a process definition, from the model. Only ids of process definitions that are deployed are useful. */
    processDefinitionId?: string;
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /** Name of the latest deployed process definition instance version. */
    latestProcessDefinitionName?: string | null;
    /** Indicates whether multiple versions of this process definition instance are deployed. */
    hasMultipleVersions?: boolean;
    /** Total number of currently active process instances of this definition that do not have incidents. */
    activeInstancesWithoutIncidentCount?: number;
    /** Total number of currently active process instances of this definition that have at least one incident. */
    activeInstancesWithIncidentCount?: number;
}

export interface ProcessDefinitionInstanceStatisticsQuerySortRequest {
    /** The field to sort by. */
    field: "processDefinitionId" | "activeInstancesWithIncidentCount" | "activeInstancesWithoutIncidentCount";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface ProcessDefinitionInstanceVersionStatisticsQuery {
    /** Pagination criteria. */
    page?: {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
    /** Sort field criteria. */
    sort?: Array<{
      /** The field to sort by. */
      field: "processDefinitionId" | "processDefinitionKey" | "processDefinitionName" | "processDefinitionVersion" | "activeInstancesWithIncidentCount" | "activeInstancesWithoutIncidentCount";
      /** The order in which to sort the related field. */
      order?: "ASC" | "DESC";
  }>;
    /** The process definition instance version statistics search filters. */
    filter: {
      /** The ID of the process definition to retrieve version statistics for. */
      processDefinitionId: string;
      /** Tenant ID of this process definition. */
      tenantId?: string;
  };
}

/** Process definition instance version statistics search filter. */
export interface ProcessDefinitionInstanceVersionStatisticsFilter {
    /** The ID of the process definition to retrieve version statistics for. */
    processDefinitionId: string;
    /** Tenant ID of this process definition. */
    tenantId?: string;
}

export interface ProcessDefinitionInstanceVersionStatisticsQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

/** Process definition instance version statistics response. */
export interface ProcessDefinitionInstanceVersionStatisticsResult {
    /** The ID associated with the process definition. */
    processDefinitionId: string;
    /** The unique key of the process definition. */
    processDefinitionKey: string;
    /** The name of the process definition. */
    processDefinitionName: string | null;
    /** The tenant ID associated with the process definition. */
    tenantId: string;
    /** The version number of the process definition. */
    processDefinitionVersion: number;
    /** The number of active process instances for this version that currently have incidents. */
    activeInstancesWithIncidentCount: number;
    /** The number of active process instances for this version that do not have any incidents. */
    activeInstancesWithoutIncidentCount: number;
}

export interface ProcessDefinitionInstanceVersionStatisticsQuerySortRequest {
    /** The field to sort by. */
    field: "processDefinitionId" | "processDefinitionKey" | "processDefinitionName" | "processDefinitionVersion" | "activeInstancesWithIncidentCount" | "activeInstancesWithoutIncidentCount";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/**  Instructions for creating a process instance. The process definition can be specified
 *  either by id or by key. */
export type ProcessInstanceCreationInstruction = {
    /**  The unique key identifying the process definition, for example, returned for a process in the
 deploy resources endpoint. */
    processDefinitionKey: string;
    /**  As the version is already identified by the `processDefinitionKey`, the value of this field is ignored.
 It's here for backwards-compatibility only as previous releases accepted it in request bodies. */
    processDefinitionVersion?: number;
    /**  Set of variables as JSON object to instantiate in the root variable scope of the process
 instance. Can include nested complex objects. */
    variables?: {
      [key: string]: unknown;
  };
    /**  List of start instructions. By default, the process instance will start at
 the start event. If provided, the process instance will apply start instructions
 after it has been created. */
    startInstructions?: Array<{
      /**  Future extensions might include:
   - different types of start instructions
   - ability to set local variables for different flow scopes

 For now, however, the start instruction is implicitly a "startBeforeElement" instruction */
      elementId: string;
  }>;
    /**  Runtime instructions (alpha). List of instructions that affect the runtime behavior of
 the process instance. Refer to specific instruction types for more details.

 This parameter is an alpha feature and may be subject to change
 in future releases. */
    runtimeInstructions?: Array<{
      /** The type of the runtime instruction */
      "type"?: string;
      /**  The id of the element that, once completed or terminated, will cause the process to be terminated. */
      afterElementId: string;
  }>;
    /**  The tenant id of the process definition.
 If multi-tenancy is enabled, provide the tenant id of the process definition to start a
 process instance of. If multi-tenancy is disabled, don't provide this parameter. */
    tenantId?: string;
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
    /**  Wait for the process instance to complete. If the process instance does not complete
 within the request timeout limit, a 504 response status will be returned. The process
 instance will continue to run in the background regardless of the timeout. Disabled by
 default. */
    awaitCompletion?: boolean;
    /**  Timeout (in ms) the request waits for the process to complete. By default or
 when set to 0, the generic request timeout configured in the cluster is applied. */
    requestTimeout?: number;
    /**  List of variables by name to be included in the response when awaitCompletion is set to true.
 If empty, all visible variables in the root scope will be returned. */
    fetchVariables?: Array<string>;
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags?: Array<string>;
    /**  An optional, user-defined string identifier that identifies the process instance
 within the scope of a process definition (scoped by tenant). If provided and uniqueness
 enforcement is enabled, the engine will reject creation if another root process instance
 with the same business id is already active for the same process definition.
 Note that any active child process instances with the same business id are not taken into account. */
    businessId?: string;
} | {
    /**  The BPMN process id of the process definition to start an instance of. */
    processDefinitionId: string;
    /**  The version of the process. By default, the latest version of the process is used. */
    processDefinitionVersion?: number;
    /**  JSON object that will instantiate the variables for the root variable scope
 of the process instance. */
    variables?: {
      [key: string]: unknown;
  };
    /**  The tenant id of the process definition.
 If multi-tenancy is enabled, provide the tenant id of the process definition to start a
 process instance of. If multi-tenancy is disabled, don't provide this parameter. */
    tenantId?: string;
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
    /**  List of start instructions. By default, the process instance will start at
 the start event. If provided, the process instance will apply start instructions
 after it has been created. */
    startInstructions?: Array<{
      /**  Future extensions might include:
   - different types of start instructions
   - ability to set local variables for different flow scopes

 For now, however, the start instruction is implicitly a "startBeforeElement" instruction */
      elementId: string;
  }>;
    /**  Runtime instructions (alpha). List of instructions that affect the runtime behavior of
 the process instance. Refer to specific instruction types for more details.

 This parameter is an alpha feature and may be subject to change
 in future releases. */
    runtimeInstructions?: Array<{
      /** The type of the runtime instruction */
      "type"?: string;
      /**  The id of the element that, once completed or terminated, will cause the process to be terminated. */
      afterElementId: string;
  }>;
    /**  Wait for the process instance to complete. If the process instance does not complete
 within the request timeout limit, a 504 response status will be returned. The process
 instance will continue to run in the background regardless of the timeout. Disabled by
 default. */
    awaitCompletion?: boolean;
    /**  List of variables by name to be included in the response when awaitCompletion is set to true.
 If empty, all visible variables in the root scope will be returned. */
    fetchVariables?: Array<string>;
    /**  Timeout (in ms) the request waits for the process to complete. By default or
 when set to 0, the generic request timeout configured in the cluster is applied. */
    requestTimeout?: number;
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags?: Array<string>;
    /**  An optional, user-defined string identifier that identifies the process instance
 within the scope of a process definition (scoped by tenant). If provided and uniqueness
 enforcement is enabled, the engine will reject creation if another root process instance
 with the same business id is already active for the same process definition.
 Note that any active child process instances with the same business id are not taken into account. */
    businessId?: string;
};

export interface ProcessInstanceCreationInstructionById {
    /**  The BPMN process id of the process definition to start an instance of. */
    processDefinitionId: string;
    /**  The version of the process. By default, the latest version of the process is used. */
    processDefinitionVersion?: number;
    /**  JSON object that will instantiate the variables for the root variable scope
 of the process instance. */
    variables?: {
      [key: string]: unknown;
  };
    /**  The tenant id of the process definition.
 If multi-tenancy is enabled, provide the tenant id of the process definition to start a
 process instance of. If multi-tenancy is disabled, don't provide this parameter. */
    tenantId?: string;
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
    /**  List of start instructions. By default, the process instance will start at
 the start event. If provided, the process instance will apply start instructions
 after it has been created. */
    startInstructions?: Array<{
      /**  Future extensions might include:
   - different types of start instructions
   - ability to set local variables for different flow scopes

 For now, however, the start instruction is implicitly a "startBeforeElement" instruction */
      elementId: string;
  }>;
    /**  Runtime instructions (alpha). List of instructions that affect the runtime behavior of
 the process instance. Refer to specific instruction types for more details.

 This parameter is an alpha feature and may be subject to change
 in future releases. */
    runtimeInstructions?: Array<{
      /** The type of the runtime instruction */
      "type"?: string;
      /**  The id of the element that, once completed or terminated, will cause the process to be terminated. */
      afterElementId: string;
  }>;
    /**  Wait for the process instance to complete. If the process instance does not complete
 within the request timeout limit, a 504 response status will be returned. The process
 instance will continue to run in the background regardless of the timeout. Disabled by
 default. */
    awaitCompletion?: boolean;
    /**  List of variables by name to be included in the response when awaitCompletion is set to true.
 If empty, all visible variables in the root scope will be returned. */
    fetchVariables?: Array<string>;
    /**  Timeout (in ms) the request waits for the process to complete. By default or
 when set to 0, the generic request timeout configured in the cluster is applied. */
    requestTimeout?: number;
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags?: Array<string>;
    /**  An optional, user-defined string identifier that identifies the process instance
 within the scope of a process definition (scoped by tenant). If provided and uniqueness
 enforcement is enabled, the engine will reject creation if another root process instance
 with the same business id is already active for the same process definition.
 Note that any active child process instances with the same business id are not taken into account. */
    businessId?: string;
}

export interface ProcessInstanceCreationInstructionByKey {
    /**  The unique key identifying the process definition, for example, returned for a process in the
 deploy resources endpoint. */
    processDefinitionKey: string;
    /**  As the version is already identified by the `processDefinitionKey`, the value of this field is ignored.
 It's here for backwards-compatibility only as previous releases accepted it in request bodies. */
    processDefinitionVersion?: number;
    /**  Set of variables as JSON object to instantiate in the root variable scope of the process
 instance. Can include nested complex objects. */
    variables?: {
      [key: string]: unknown;
  };
    /**  List of start instructions. By default, the process instance will start at
 the start event. If provided, the process instance will apply start instructions
 after it has been created. */
    startInstructions?: Array<{
      /**  Future extensions might include:
   - different types of start instructions
   - ability to set local variables for different flow scopes

 For now, however, the start instruction is implicitly a "startBeforeElement" instruction */
      elementId: string;
  }>;
    /**  Runtime instructions (alpha). List of instructions that affect the runtime behavior of
 the process instance. Refer to specific instruction types for more details.

 This parameter is an alpha feature and may be subject to change
 in future releases. */
    runtimeInstructions?: Array<{
      /** The type of the runtime instruction */
      "type"?: string;
      /**  The id of the element that, once completed or terminated, will cause the process to be terminated. */
      afterElementId: string;
  }>;
    /**  The tenant id of the process definition.
 If multi-tenancy is enabled, provide the tenant id of the process definition to start a
 process instance of. If multi-tenancy is disabled, don't provide this parameter. */
    tenantId?: string;
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
    /**  Wait for the process instance to complete. If the process instance does not complete
 within the request timeout limit, a 504 response status will be returned. The process
 instance will continue to run in the background regardless of the timeout. Disabled by
 default. */
    awaitCompletion?: boolean;
    /**  Timeout (in ms) the request waits for the process to complete. By default or
 when set to 0, the generic request timeout configured in the cluster is applied. */
    requestTimeout?: number;
    /**  List of variables by name to be included in the response when awaitCompletion is set to true.
 If empty, all visible variables in the root scope will be returned. */
    fetchVariables?: Array<string>;
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags?: Array<string>;
    /**  An optional, user-defined string identifier that identifies the process instance
 within the scope of a process definition (scoped by tenant). If provided and uniqueness
 enforcement is enabled, the engine will reject creation if another root process instance
 with the same business id is already active for the same process definition.
 Note that any active child process instances with the same business id are not taken into account. */
    businessId?: string;
}

export interface ProcessInstanceCreationStartInstruction {
    /**  Future extensions might include:
   - different types of start instructions
   - ability to set local variables for different flow scopes

 For now, however, the start instruction is implicitly a "startBeforeElement" instruction */
    elementId: string;
}

export interface ProcessInstanceCreationRuntimeInstruction {
    /** The type of the runtime instruction */
    "type"?: string;
    /**  The id of the element that, once completed or terminated, will cause the process to be terminated. */
    afterElementId: string;
}

/**  Terminates the process instance after a specific BPMN element is completed or terminated. */
export interface ProcessInstanceCreationTerminateInstruction {
    /** The type of the runtime instruction */
    "type"?: string;
    /**  The id of the element that, once completed or terminated, will cause the process to be terminated. */
    afterElementId: string;
}

export interface CreateProcessInstanceResult {
    /**  The BPMN process id of the process definition which was used to create the process.
 instance */
    processDefinitionId: string;
    /**  The version of the process definition which was used to create the process instance. */
    processDefinitionVersion: number;
    /** The tenant id of the created process instance. */
    tenantId: string;
    /** All the variables visible in the root scope. */
    variables: {
      [key: string]: unknown;
  };
    /**  The key of the process definition which was used to create the process instance. */
    processDefinitionKey: string;
    /**  The unique identifier of the created process instance; to be used wherever a request
 needs a process instance key (e.g. CancelProcessInstanceRequest). */
    processInstanceKey: string;
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags: Array<string>;
    /** Business id as provided on creation. */
    businessId?: string;
}

export interface ProcessInstanceSearchQuerySortRequest {
    /** The field to sort by. */
    field: "processInstanceKey" | "processDefinitionId" | "processDefinitionName" | "processDefinitionVersion" | "processDefinitionVersionTag" | "processDefinitionKey" | "parentProcessInstanceKey" | "parentElementInstanceKey" | "startDate" | "endDate" | "state" | "hasIncident" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Process instance search request. */
export type ProcessInstanceSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Base process instance search filter. */
export interface BaseProcessInstanceFilterFields {
    /** The start date. */
    startDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The end date. */
    endDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The process instance state. */
    state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** Whether this process instance has a related incident or not. */
    hasIncident?: boolean;
    /** The tenant id. */
    tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The process instance variables. */
    variables?: Array<{
      /** Name of the variable. */
      name: string;
      /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
      value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  }>;
    /** The key of this process instance. */
    processInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The parent process instance key. */
    parentProcessInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The parent element instance key. */
    parentElementInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The batch operation id. */
    batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The error message related to the process. */
    errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Whether the process has failed jobs with retries left. */
    hasRetriesLeft?: boolean;
    /** The state of the element instances associated with the process instance. */
    elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The element id associated with the process instance. */
    elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Whether the element instance has an incident or not. */
    hasElementInstanceIncident?: boolean;
    /** The incident error hash code, associated with this process. */
    incidentErrorHashCode?: number | {
      /** Checks for equality with the provided value. */
      $eq?: number;
      /** Checks for inequality with the provided value. */
      $neq?: number;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: number;
      /** Greater than or equal comparison with the provided value. */
      $gte?: number;
      /** Lower than comparison with the provided value. */
      $lt?: number;
      /** Lower than or equal comparison with the provided value. */
      $lte?: number;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<number>;
  };
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags?: Array<string>;
}

/** Process definition statistics search filter. */
export interface ProcessDefinitionStatisticsFilter {
  startDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
  endDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
  state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
  hasIncident?: boolean;
  tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  variables?: Array<{
      /** Name of the variable. */
      name: string;
      /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
      value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  }>;
  processInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
  parentProcessInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
  parentElementInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
  batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  hasRetriesLeft?: boolean;
  elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
  elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  hasElementInstanceIncident?: boolean;
  incidentErrorHashCode?: number | {
      /** Checks for equality with the provided value. */
      $eq?: number;
      /** Checks for inequality with the provided value. */
      $neq?: number;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: number;
      /** Greater than or equal comparison with the provided value. */
      $gte?: number;
      /** Lower than comparison with the provided value. */
      $lt?: number;
      /** Lower than or equal comparison with the provided value. */
      $lte?: number;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<number>;
  };
  tags?: Array<string>;
  $or?: Array<{
      /** The start date. */
      startDate?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: string;
        /** Greater than or equal comparison with the provided value. */
        $gte?: string;
        /** Lower than comparison with the provided value. */
        $lt?: string;
        /** Lower than or equal comparison with the provided value. */
        $lte?: string;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
    };
      /** The end date. */
      endDate?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: string;
        /** Greater than or equal comparison with the provided value. */
        $gte?: string;
        /** Lower than comparison with the provided value. */
        $lt?: string;
        /** Lower than or equal comparison with the provided value. */
        $lte?: string;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
    };
      /** The process instance state. */
      state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
        /** Checks for equality with the provided value. */
        $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks for inequality with the provided value. */
        $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
        /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
        $like?: string;
    };
      /** Whether this process instance has a related incident or not. */
      hasIncident?: boolean;
      /** The tenant id. */
      tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** The process instance variables. */
      variables?: Array<{
        /** Name of the variable. */
        name: string;
        /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
        value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    }>;
      /** The key of this process instance. */
      processInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The parent process instance key. */
      parentProcessInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The parent element instance key. */
      parentElementInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The batch operation id. */
      batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** The error message related to the process. */
      errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** Whether the process has failed jobs with retries left. */
      hasRetriesLeft?: boolean;
      /** The state of the element instances associated with the process instance. */
      elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
        /** Checks for equality with the provided value. */
        $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks for inequality with the provided value. */
        $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
        /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
        $like?: string;
    };
      /** The element id associated with the process instance. */
      elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** Whether the element instance has an incident or not. */
      hasElementInstanceIncident?: boolean;
      /** The incident error hash code, associated with this process. */
      incidentErrorHashCode?: number | {
        /** Checks for equality with the provided value. */
        $eq?: number;
        /** Checks for inequality with the provided value. */
        $neq?: number;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: number;
        /** Greater than or equal comparison with the provided value. */
        $gte?: number;
        /** Lower than comparison with the provided value. */
        $lt?: number;
        /** Lower than or equal comparison with the provided value. */
        $lte?: number;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<number>;
    };
      /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
      tags?: Array<string>;
  }>;
}

/** Process instance search filter. */
export interface ProcessInstanceFilterFields {
    /** The start date. */
    startDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The end date. */
    endDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The process instance state. */
    state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** Whether this process instance has a related incident or not. */
    hasIncident?: boolean;
    /** The tenant id. */
    tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The process instance variables. */
    variables?: Array<{
      /** Name of the variable. */
      name: string;
      /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
      value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  }>;
    /** The key of this process instance. */
    processInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The parent process instance key. */
    parentProcessInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The parent element instance key. */
    parentElementInstanceKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The batch operation id. */
    batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The error message related to the process. */
    errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Whether the process has failed jobs with retries left. */
    hasRetriesLeft?: boolean;
    /** The state of the element instances associated with the process instance. */
    elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
      /** Checks for equality with the provided value. */
      $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks for inequality with the provided value. */
      $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The element id associated with the process instance. */
    elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Whether the element instance has an incident or not. */
    hasElementInstanceIncident?: boolean;
    /** The incident error hash code, associated with this process. */
    incidentErrorHashCode?: number | {
      /** Checks for equality with the provided value. */
      $eq?: number;
      /** Checks for inequality with the provided value. */
      $neq?: number;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: number;
      /** Greater than or equal comparison with the provided value. */
      $gte?: number;
      /** Lower than comparison with the provided value. */
      $lt?: number;
      /** Lower than or equal comparison with the provided value. */
      $lte?: number;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<number>;
  };
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags?: Array<string>;
}

/** Process instance search filter. */
export interface ProcessInstanceFilter {
  processDefinitionId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionName?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionVersion?: number | {
      /** Checks for equality with the provided value. */
      $eq?: number;
      /** Checks for inequality with the provided value. */
      $neq?: number;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: number;
      /** Greater than or equal comparison with the provided value. */
      $gte?: number;
      /** Lower than comparison with the provided value. */
      $lt?: number;
      /** Lower than or equal comparison with the provided value. */
      $lte?: number;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<number>;
  };
  processDefinitionVersionTag?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  processDefinitionKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
  $or?: Array<{
      /** The start date. */
      startDate?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: string;
        /** Greater than or equal comparison with the provided value. */
        $gte?: string;
        /** Lower than comparison with the provided value. */
        $lt?: string;
        /** Lower than or equal comparison with the provided value. */
        $lte?: string;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
    };
      /** The end date. */
      endDate?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: string;
        /** Greater than or equal comparison with the provided value. */
        $gte?: string;
        /** Lower than comparison with the provided value. */
        $lt?: string;
        /** Lower than or equal comparison with the provided value. */
        $lte?: string;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
    };
      /** The process instance state. */
      state?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
        /** Checks for equality with the provided value. */
        $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks for inequality with the provided value. */
        $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
        /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
        $like?: string;
    };
      /** Whether this process instance has a related incident or not. */
      hasIncident?: boolean;
      /** The tenant id. */
      tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** The process instance variables. */
      variables?: Array<{
        /** Name of the variable. */
        name: string;
        /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
        value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    }>;
      /** The key of this process instance. */
      processInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The parent process instance key. */
      parentProcessInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The parent element instance key. */
      parentElementInstanceKey?: string | {
        /** Checks for equality with the provided value. */
        $eq?: string;
        /** Checks for inequality with the provided value. */
        $neq?: string;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<string>;
        /** Checks if the property matches none of the provided values. */
        $notIn?: Array<string>;
    };
      /** The batch operation id. */
      batchOperationId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** The error message related to the process. */
      errorMessage?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** Whether the process has failed jobs with retries left. */
      hasRetriesLeft?: boolean;
      /** The state of the element instances associated with the process instance. */
      elementInstanceState?: "ACTIVE" | "COMPLETED" | "TERMINATED" | {
        /** Checks for equality with the provided value. */
        $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks for inequality with the provided value. */
        $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
        /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
        $like?: string;
    };
      /** The element id associated with the process instance. */
      elementId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
      /** Whether the element instance has an incident or not. */
      hasElementInstanceIncident?: boolean;
      /** The incident error hash code, associated with this process. */
      incidentErrorHashCode?: number | {
        /** Checks for equality with the provided value. */
        $eq?: number;
        /** Checks for inequality with the provided value. */
        $neq?: number;
        /** Checks if the current property exists. */
        $exists?: boolean;
        /** Greater than comparison with the provided value. */
        $gt?: number;
        /** Greater than or equal comparison with the provided value. */
        $gte?: number;
        /** Lower than comparison with the provided value. */
        $lt?: number;
        /** Lower than or equal comparison with the provided value. */
        $lte?: number;
        /** Checks if the property matches any of the provided values. */
        $in?: Array<number>;
    };
      /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
      tags?: Array<string>;
  }>;
}

/** Process instance search response. */
export interface ProcessInstanceSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

/** Process instance search response item. */
export interface ProcessInstanceResult {
    /** Id of a process definition, from the model. Only ids of process definitions that are deployed are useful. */
    processDefinitionId: string;
    /** The process definition name. */
    processDefinitionName: string | null;
    /** The process definition version. */
    processDefinitionVersion: number;
    /** The process definition version tag. */
    processDefinitionVersionTag: string | null;
    /** The start time of the process instance. */
    startDate: string;
    /** The completion or termination time of the process instance. */
    endDate: string | null;
    /** Process instance states */
    state: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Whether this process instance has a related incident or not. */
    hasIncident: boolean;
    /** The unique identifier of the tenant. */
    tenantId: string;
    /** The key of this process instance. */
    processInstanceKey: string;
    /** The process definition key. */
    processDefinitionKey: string;
    /** The parent process instance key. */
    parentProcessInstanceKey: string;
    /** The parent element instance key. */
    parentElementInstanceKey: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags: Array<string>;
    /** The business id associated with this process instance. */
    businessId: string;
}

export type CancelProcessInstanceRequest = {
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
} | null;

export type DeleteProcessInstanceRequest = {
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
} | null;

export interface ProcessInstanceCallHierarchyEntry {
    /** The key of the process instance. */
    processInstanceKey: string;
    /** The key of the process definition. */
    processDefinitionKey: string;
    /** The name of the process definition (fall backs to the process definition id if not available). */
    processDefinitionName: string;
}

/** Process instance sequence flows query response. */
export interface ProcessInstanceSequenceFlowsQueryResult {
    /** The sequence flows. */
    items: Array<{
      /** The sequence flow id. */
      sequenceFlowId?: string;
      /** The key of this process instance. */
      processInstanceKey?: string;
      /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
      rootProcessInstanceKey: string;
      /** The process definition key. */
      processDefinitionKey?: string;
      /** The process definition id. */
      processDefinitionId?: string;
      /** The element id for this sequence flow, as provided in the BPMN process. */
      elementId?: string;
      /** The unique identifier of the tenant. */
      tenantId?: string;
  }>;
}

/** Process instance sequence flow result. */
export interface ProcessInstanceSequenceFlowResult {
    /** The sequence flow id. */
    sequenceFlowId?: string;
    /** The key of this process instance. */
    processInstanceKey?: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** The process definition key. */
    processDefinitionKey?: string;
    /** The process definition id. */
    processDefinitionId?: string;
    /** The element id for this sequence flow, as provided in the BPMN process. */
    elementId?: string;
    /** The unique identifier of the tenant. */
    tenantId?: string;
}

/** Process instance element statistics query response. */
export interface ProcessInstanceElementStatisticsQueryResult {
    /** The element statistics. */
    items: Array<{
      /** The element ID for which the results are aggregated. */
      elementId?: string;
      /** The total number of active instances of the element. */
      active?: number;
      /** The total number of canceled instances of the element. */
      canceled?: number;
      /** The total number of incidents for the element. */
      incidents?: number;
      /** The total number of completed instances of the element. */
      completed?: number;
  }>;
}

/**  The migration instructions describe how to migrate a process instance from one process definition to another. */
export interface ProcessInstanceMigrationInstruction {
    /** The key of process definition to migrate the process instance to. */
    targetProcessDefinitionKey: string;
    /** Element mappings from the source process instance to the target process instance. */
    mappingInstructions: Array<{
      /** The element id to migrate from. */
      sourceElementId: string;
      /** The element id to migrate into. */
      targetElementId: string;
  }>;
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

/**  The mapping instructions describe how to map elements from the source process definition to the target process definition. */
export interface MigrateProcessInstanceMappingInstruction {
    /** The element id to migrate from. */
    sourceElementId: string;
    /** The element id to migrate into. */
    targetElementId: string;
}

export type ProcessInstanceModificationInstruction = {
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
    /** Instructions describing which elements to activate in which scopes and which variables to create or update. */
    activateInstructions?: Array<{
      /** The id of the element to activate. */
      elementId: string;
      /** Instructions describing which variables to create or update. */
      variableInstructions?: Array<{
        /**  JSON document that will instantiate the variables at the scope defined by the scopeId.
 It must be a JSON object, as variables will be mapped in a key-value fashion. */
        variables: {
          [key: string]: unknown;
      };
        /**  The id of the element in which scope the variables should be created.
 Leave empty to create the variables in the global scope of the process instance. */
        scopeId?: string;
    }>;
      /**  The key of the ancestor scope the element instance should be created in.
 Set to -1 to create the new element instance within an existing element instance of the
 flow scope. If multiple instances of the target element's flow scope exist, choose one
 specifically with this property by providing its key. */
      ancestorElementInstanceKey?: string;
  }>;
    /** Instructions describing which elements to move from one scope to another. */
    moveInstructions?: Array<{
      /**  Defines the source element identifier for the move instruction. It can either be a sourceElementId, or sourceElementInstanceKey. */
      sourceElementInstruction: {
        /** The type of source element instruction. */
        sourceType: string;
        /**  The id of the source element for the move instruction. */
        sourceElementId: string;
    } | {
        /** The type of source element instruction. */
        sourceType: string;
        /**  The source element instance key for the move instruction. */
        sourceElementInstanceKey: string;
    };
      /** The target element id. */
      targetElementId: string;
      /**  Defines the ancestor scope for the created element instances. The default behavior resembles
 a "direct" scope instruction with an `ancestorElementInstanceKey` of `"-1"`. */
      ancestorScopeInstruction?: {
        /** The type of ancestor scope instruction. */
        ancestorScopeType: string;
        /**  The key of the ancestor scope the element instance should be created in.
 Set to -1 to create the new element instance within an existing element instance of the
 flow scope. If multiple instances of the target element's flow scope exist, choose one
 specifically with this property by providing its key. */
        ancestorElementInstanceKey: string;
    } | {
        /** The type of ancestor scope instruction. */
        ancestorScopeType: string;
    };
      /** Instructions describing which variables to create or update. */
      variableInstructions?: Array<{
        /**  JSON document that will instantiate the variables at the scope defined by the scopeId.
 It must be a JSON object, as variables will be mapped in a key-value fashion. */
        variables: {
          [key: string]: unknown;
      };
        /**  The id of the element in which scope the variables should be created.
 Leave empty to create the variables in the global scope of the process instance. */
        scopeId?: string;
    }>;
  }>;
    /** Instructions describing which elements to terminate. */
    terminateInstructions?: Array<{
      /** The id of the elements to terminate. The element instances are determined at runtime. */
      elementId: string;
  } | {
      /** The key of the element instance to terminate. */
      elementInstanceKey: string;
  }>;
};

/** Instruction describing an element to activate. */
export interface ProcessInstanceModificationActivateInstruction {
    /** The id of the element to activate. */
    elementId: string;
    /** Instructions describing which variables to create or update. */
    variableInstructions?: Array<{
      /**  JSON document that will instantiate the variables at the scope defined by the scopeId.
 It must be a JSON object, as variables will be mapped in a key-value fashion. */
      variables: {
        [key: string]: unknown;
    };
      /**  The id of the element in which scope the variables should be created.
 Leave empty to create the variables in the global scope of the process instance. */
      scopeId?: string;
  }>;
    /**  The key of the ancestor scope the element instance should be created in.
 Set to -1 to create the new element instance within an existing element instance of the
 flow scope. If multiple instances of the target element's flow scope exist, choose one
 specifically with this property by providing its key. */
    ancestorElementInstanceKey?: string;
}

/** Instruction describing which variables to create or update. */
export interface ModifyProcessInstanceVariableInstruction {
    /**  JSON document that will instantiate the variables at the scope defined by the scopeId.
 It must be a JSON object, as variables will be mapped in a key-value fashion. */
    variables: {
      [key: string]: unknown;
  };
    /**  The id of the element in which scope the variables should be created.
 Leave empty to create the variables in the global scope of the process instance. */
    scopeId?: string;
}

/**  Instruction describing a move operation. This instruction will terminate active element
 *  instances based on the sourceElementInstruction and activate a new element instance for each terminated
 *  one at targetElementId. Note that, for multi-instance activities, only the multi-instance
 *  body instances will activate new element instances at the target id. */
export type ProcessInstanceModificationMoveInstruction = {
    /**  Defines the source element identifier for the move instruction. It can either be a sourceElementId, or sourceElementInstanceKey. */
    sourceElementInstruction: {
      /** The type of source element instruction. */
      sourceType: string;
      /**  The id of the source element for the move instruction. */
      sourceElementId: string;
  } | {
      /** The type of source element instruction. */
      sourceType: string;
      /**  The source element instance key for the move instruction. */
      sourceElementInstanceKey: string;
  };
    /** The target element id. */
    targetElementId: string;
    /**  Defines the ancestor scope for the created element instances. The default behavior resembles
 a "direct" scope instruction with an `ancestorElementInstanceKey` of `"-1"`. */
    ancestorScopeInstruction?: {
      /** The type of ancestor scope instruction. */
      ancestorScopeType: string;
      /**  The key of the ancestor scope the element instance should be created in.
 Set to -1 to create the new element instance within an existing element instance of the
 flow scope. If multiple instances of the target element's flow scope exist, choose one
 specifically with this property by providing its key. */
      ancestorElementInstanceKey: string;
  } | {
      /** The type of ancestor scope instruction. */
      ancestorScopeType: string;
  };
    /** Instructions describing which variables to create or update. */
    variableInstructions?: Array<{
      /**  JSON document that will instantiate the variables at the scope defined by the scopeId.
 It must be a JSON object, as variables will be mapped in a key-value fashion. */
      variables: {
        [key: string]: unknown;
    };
      /**  The id of the element in which scope the variables should be created.
 Leave empty to create the variables in the global scope of the process instance. */
      scopeId?: string;
  }>;
};

/**  Defines the source element identifier for the move instruction. It can either be a sourceElementId, or sourceElementInstanceKey. */
export type SourceElementInstruction = {
    /** The type of source element instruction. */
    sourceType: string;
    /**  The id of the source element for the move instruction. */
    sourceElementId: string;
} | {
    /** The type of source element instruction. */
    sourceType: string;
    /**  The source element instance key for the move instruction. */
    sourceElementInstanceKey: string;
};

/**  Defines an instruction with a sourceElementId. The move instruction with this sourceType will terminate all active element
 *  instances with the sourceElementId and activate a new element instance for each terminated
 *  one at targetElementId. */
export interface SourceElementIdInstruction {
    /** The type of source element instruction. */
    sourceType: string;
    /**  The id of the source element for the move instruction. */
    sourceElementId: string;
}

/**  Defines an instruction with a sourceElementInstanceKey. The move instruction with this sourceType will terminate one active element
 *  instance with the sourceElementInstanceKey and activate a new element instance at targetElementId. */
export interface SourceElementInstanceKeyInstruction {
    /** The type of source element instruction. */
    sourceType: string;
    /**  The source element instance key for the move instruction. */
    sourceElementInstanceKey: string;
}

/**  Defines the ancestor scope for the created element instances. The default behavior resembles
 *  a "direct" scope instruction with an `ancestorElementInstanceKey` of `"-1"`. */
export type AncestorScopeInstruction = {
    /** The type of ancestor scope instruction. */
    ancestorScopeType: string;
    /**  The key of the ancestor scope the element instance should be created in.
 Set to -1 to create the new element instance within an existing element instance of the
 flow scope. If multiple instances of the target element's flow scope exist, choose one
 specifically with this property by providing its key. */
    ancestorElementInstanceKey: string;
} | {
    /** The type of ancestor scope instruction. */
    ancestorScopeType: string;
};

/** Provides a concrete key to use as ancestor scope for the created element instance. */
export interface DirectAncestorKeyInstruction {
    /** The type of ancestor scope instruction. */
    ancestorScopeType: string;
    /**  The key of the ancestor scope the element instance should be created in.
 Set to -1 to create the new element instance within an existing element instance of the
 flow scope. If multiple instances of the target element's flow scope exist, choose one
 specifically with this property by providing its key. */
    ancestorElementInstanceKey: string;
}

/**  Instructs the engine to derive the ancestor scope key from the source element's hierarchy. The engine traverses the source element's ancestry to find an instance that matches one of the target element's flow scopes, ensuring the target is activated in the correct scope. */
export interface InferredAncestorKeyInstruction {
    /** The type of ancestor scope instruction. */
    ancestorScopeType: string;
}

/**  Instructs the engine to use the source's direct parent key as the ancestor scope key for the target element. This is a simpler alternative to `inferred` that skips hierarchy traversal and directly uses the source's parent key. This is useful when the source and target elements are siblings within the same flow scope. */
export interface UseSourceParentKeyInstruction {
    /** The type of ancestor scope instruction. */
    ancestorScopeType: string;
}

/** Instruction describing which elements to terminate. */
export type ProcessInstanceModificationTerminateInstruction = {
    /** The id of the elements to terminate. The element instances are determined at runtime. */
    elementId: string;
} | {
    /** The key of the element instance to terminate. */
    elementInstanceKey: string;
};

/**  Instruction describing which elements to terminate. The element instances are determined
 *  at runtime by the given id. */
export interface ProcessInstanceModificationTerminateByIdInstruction {
    /** The id of the elements to terminate. The element instances are determined at runtime. */
    elementId: string;
}

/** Instruction providing the key of the element instance to terminate. */
export interface ProcessInstanceModificationTerminateByKeyInstruction {
    /** The key of the element instance to terminate. */
    elementInstanceKey: string;
}

/** Process instance states */
export type ProcessInstanceStateEnum = "ACTIVE" | "COMPLETED" | "TERMINATED";

/** Advanced ProcessInstanceStateEnum filter. */
export interface AdvancedProcessInstanceStateFilter {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** ProcessInstanceStateEnum property with full advanced search capabilities. */
export type ProcessInstanceStateFilterProperty = "ACTIVE" | "COMPLETED" | "TERMINATED" | {
    /** Checks for equality with the provided value. */
    $eq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Checks for inequality with the provided value. */
    $neq?: "ACTIVE" | "COMPLETED" | "TERMINATED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"ACTIVE" | "COMPLETED" | "TERMINATED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

export interface RoleCreateRequest {
    /** The ID of the new role. */
    roleId: string;
    /** The display name of the new role. */
    name: string;
    /** The description of the new role. */
    description?: string;
}

export interface RoleCreateResult {
    /** The ID of the created role. */
    roleId?: string;
    /** The display name of the created role. */
    name?: string;
    /** The description of the created role. */
    description?: string;
}

export interface RoleUpdateRequest {
    /** The display name of the new role. */
    name: string;
    /** The description of the new role. */
    description?: string;
}

export interface RoleUpdateResult {
    /** The display name of the updated role. */
    name?: string;
    /** The description of the updated role. */
    description?: string;
    /** The ID of the updated role. */
    roleId?: string;
}

/** Role search response item. */
export interface RoleResult {
    /** The role name. */
    name?: string;
    /** The role id. */
    roleId?: string;
    /** The description of the role. */
    description?: string;
}

export interface RoleSearchQuerySortRequest {
    /** The field to sort by. */
    field: "name" | "roleId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Role search request. */
export type RoleSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Role filter request */
export interface RoleFilter {
    /** The role ID search filters. */
    roleId?: string;
    /** The role name search filters. */
    name?: string;
}

/** Role search response. */
export interface RoleSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface RoleUserResult {
    /** The unique name of a user. */
    username?: string;
}

export interface RoleUserSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export type RoleUserSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface RoleUserSearchQuerySortRequest {
    /** The field to sort by. */
    field: "username";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface RoleClientResult {
    /** The ID of the client. */
    clientId?: string;
}

export interface RoleClientSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export type RoleClientSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface RoleClientSearchQuerySortRequest {
    /** The field to sort by. */
    field: "clientId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface RoleGroupResult {
    /** The id of the group. */
    groupId?: string;
}

export interface RoleGroupSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export type RoleGroupSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface RoleMappingRuleSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface RoleGroupSearchQuerySortRequest {
    /** The field to sort by. */
    field: "groupId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface SignalBroadcastRequest {
    /** The name of the signal to broadcast. */
    signalName: string;
    /** The signal variables as a JSON object. */
    variables?: {
      [key: string]: unknown;
  };
    /** The ID of the tenant that owns the signal. */
    tenantId?: string;
}

export interface SignalBroadcastResult {
    /** The tenant ID of the signal that was broadcast. */
    tenantId: string;
    /** The key of the broadcasted signal. */
    signalKey: string;
}

/** System-generated key for an signal. */
export type SignalKey = string;

/** The response of a topology request. */
export interface TopologyResponse {
    /** A list of brokers that are part of this cluster. */
    brokers: Array<{
      /** The unique (within a cluster) node ID for the broker. */
      nodeId: number;
      /** The hostname for reaching the broker. */
      host: string;
      /** The port for reaching the broker. */
      port: number;
      /** A list of partitions managed or replicated on this broker. */
      partitions: Array<{
        /** The unique ID of this partition. */
        partitionId: number;
        /** Describes the Raft role of the broker for a given partition. */
        role: "leader" | "follower" | "inactive";
        /** Describes the current health of the partition. */
        health: "healthy" | "unhealthy" | "dead";
    }>;
      /** The broker version. */
      version: string;
  }>;
    /** The cluster Id. */
    clusterId?: string | null;
    /** The number of brokers in the cluster. */
    clusterSize: number;
    /** The number of partitions are spread across the cluster. */
    partitionsCount: number;
    /** The configured replication factor for this cluster. */
    replicationFactor: number;
    /** The version of the Zeebe Gateway. */
    gatewayVersion: string;
    /** ID of the last completed change */
    lastCompletedChangeId: string;
}

/** Provides information on a broker node. */
export interface BrokerInfo {
    /** The unique (within a cluster) node ID for the broker. */
    nodeId: number;
    /** The hostname for reaching the broker. */
    host: string;
    /** The port for reaching the broker. */
    port: number;
    /** A list of partitions managed or replicated on this broker. */
    partitions: Array<{
      /** The unique ID of this partition. */
      partitionId: number;
      /** Describes the Raft role of the broker for a given partition. */
      role: "leader" | "follower" | "inactive";
      /** Describes the current health of the partition. */
      health: "healthy" | "unhealthy" | "dead";
  }>;
    /** The broker version. */
    version: string;
}

/** Provides information on a partition within a broker node. */
export interface Partition {
    /** The unique ID of this partition. */
    partitionId: number;
    /** Describes the Raft role of the broker for a given partition. */
    role: "leader" | "follower" | "inactive";
    /** Describes the current health of the partition. */
    health: "healthy" | "unhealthy" | "dead";
}

export interface UsageMetricsResponse {
    /** The amount of created root process instances. */
    processInstances?: number;
    /** The amount of executed decision instances. */
    decisionInstances?: number;
    /** The amount of unique active task users. */
    assignees?: number;
}

export interface UsageMetricsResponseItem {
    /** The amount of created root process instances. */
    processInstances?: number;
    /** The amount of executed decision instances. */
    decisionInstances?: number;
    /** The amount of unique active task users. */
    assignees?: number;
}

export interface TenantCreateRequest {
    /** The unique ID for the tenant. Must be 255 characters or less. Can contain letters, numbers, [`_`, `-`, `+`, `.`, `@`]. */
    tenantId: string;
    /** The name of the tenant. */
    name: string;
    /** The description of the tenant. */
    description?: string;
}

export interface TenantCreateResult {
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /** The name of the tenant. */
    name?: string;
    /** The description of the tenant. */
    description?: string;
}

export interface TenantUpdateRequest {
    /** The new name of the tenant. */
    name: string;
    /** The new description of the tenant. */
    description?: string;
}

export interface TenantUpdateResult {
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /** The name of the tenant. */
    name?: string;
    /** The description of the tenant. */
    description?: string;
}

/** Tenant search response item. */
export interface TenantResult {
    /** The tenant name. */
    name?: string;
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /** The tenant description. */
    description?: string;
}

export interface TenantSearchQuerySortRequest {
    /** The field to sort by. */
    field: "key" | "name" | "tenantId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Tenant search request */
export type TenantSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Tenant filter request */
export interface TenantFilter {
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /** The name of the tenant. */
    name?: string;
}

/** Tenant search response. */
export interface TenantSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface TenantUserResult {
    /** The unique name of a user. */
    username?: string;
}

export interface TenantUserSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export type TenantUserSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface TenantUserSearchQuerySortRequest {
    /** The field to sort by. */
    field: "username";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface TenantClientResult {
    /** The ID of the client. */
    clientId?: string;
}

export interface TenantClientSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export type TenantClientSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface TenantClientSearchQuerySortRequest {
    /** The field to sort by. */
    field: "clientId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface TenantGroupResult {
    /** The groupId of the group. */
    groupId?: string;
}

export interface TenantGroupSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export type TenantGroupSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

export interface TenantRoleSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface TenantMappingRuleSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface TenantGroupSearchQuerySortRequest {
    /** The field to sort by. */
    field: "groupId";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export interface UserRequest {
    /** The password of the user. */
    password: string;
    /** The username of the user. */
    username: string;
    /** The name of the user. */
    name?: string;
    /** The email of the user. */
    email?: string;
}

export interface UserCreateResult {
    /** The unique name of a user. */
    username?: string;
    /** The name of the user. */
    name?: string;
    /** The email of the user. */
    email?: string;
}

export interface UserUpdateRequest {
    /** The password of the user. If blank, the password is unchanged. */
    password?: string;
    /** The name of the user. */
    name?: string;
    /** The email of the user. */
    email?: string;
}

export interface UserUpdateResult {
    /** The unique name of a user. */
    username?: string;
    /** The name of the user. */
    name?: string;
    /** The email of the user. */
    email?: string;
}

export interface UserResult {
    /** The unique name of a user. */
    username?: string;
    /** The name of the user. */
    name?: string;
    /** The email of the user. */
    email?: string;
}

export interface UserSearchQuerySortRequest {
    /** The field to sort by. */
    field: "username" | "name" | "email";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

export type UserSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** User search filter. */
export interface UserFilter {
    /** The username of the user. */
    username?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The name of the user. */
    name?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The email of the user. */
    email?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
}

export interface UserSearchResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface UserTaskSearchQuerySortRequest {
    /** The field to sort by. */
    field: "creationDate" | "completionDate" | "followUpDate" | "dueDate" | "priority" | "name";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** User task search query request. */
export type UserTaskSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** User task filter request. */
export interface UserTaskFilter {
    /** The user task state. */
    state?: "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED" | {
      /** Checks for equality with the provided value. */
      $eq?: "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED";
      /** Checks for inequality with the provided value. */
      $neq?: "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The assignee of the user task. */
    assignee?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The priority of the user task. */
    priority?: number | {
      /** Checks for equality with the provided value. */
      $eq?: number;
      /** Checks for inequality with the provided value. */
      $neq?: number;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: number;
      /** Greater than or equal comparison with the provided value. */
      $gte?: number;
      /** Lower than comparison with the provided value. */
      $lt?: number;
      /** Lower than or equal comparison with the provided value. */
      $lte?: number;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<number>;
  };
    /** The element ID of the user task. */
    elementId?: string;
    /** The task name. This only works for data created with 8.8 and onwards. Instances from prior versions don't contain this data and cannot be found. */
    name?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The candidate group for this user task. */
    candidateGroup?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The candidate user for this user task. */
    candidateUser?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Tenant ID of this user task. */
    tenantId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** The ID of the process definition. */
    processDefinitionId?: string;
    /** The user task creation date. */
    creationDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The user task completion date. */
    completionDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The user task follow-up date. */
    followUpDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The user task due date. */
    dueDate?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The variables of the process instance. */
    processInstanceVariables?: Array<{
      /** Name of the variable. */
      name: string;
      /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
      value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  }>;
    /** The local variables of the user task. */
    localVariables?: Array<{
      /** Name of the variable. */
      name: string;
      /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
      value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
  }>;
    /** The key for this user task. */
    userTaskKey?: string;
    /** The key of the process definition. */
    processDefinitionKey?: string;
    /** The key of the process instance. */
    processInstanceKey?: string;
    /** The key of the element instance. */
    elementInstanceKey?: string;
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags?: Array<string>;
}

/** User task search query response. */
export interface UserTaskSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

export interface UserTaskResult {
    /** The name for this user task. */
    name?: string;
    /**  The state of the user task.
 Note: FAILED state is only for legacy job-worker-based tasks. */
    state?: "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED";
    /** The assignee of the user task. */
    assignee: string | null;
    /** The element ID of the user task. */
    elementId?: string;
    /** The candidate groups for this user task. */
    candidateGroups: Array<string>;
    /** The candidate users for this user task. */
    candidateUsers: Array<string>;
    /** The ID of the process definition. */
    processDefinitionId?: string;
    /** The creation date of a user task. */
    creationDate?: string;
    /** The completion date of a user task. */
    completionDate: string | null;
    /** The follow date of a user task. */
    followUpDate: string | null;
    /** The due date of a user task. */
    dueDate: string | null;
    /** The unique identifier of the tenant. */
    tenantId?: string;
    /** The external form reference. */
    externalFormReference: string | null;
    /** The version of the process definition. */
    processDefinitionVersion?: number;
    /** Custom headers for the user task. */
    customHeaders: {
      [key: string]: string;
  };
    /** The priority of a user task. The higher the value the higher the priority. */
    priority?: number;
    /** The key of the user task. */
    userTaskKey?: string;
    /** The key of the element instance. */
    elementInstanceKey?: string;
    /**  The name of the process definition.
 This is `null` if the process has no name defined. */
    processName?: string | null;
    /** The key of the process definition. */
    processDefinitionKey?: string;
    /** The key of the process instance. */
    processInstanceKey?: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
    /** The key of the form. */
    formKey: string;
    /** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
    tags: Array<string>;
}

export type UserTaskCompletionRequest = {
    /** The variables to complete the user task with. */
    variables?: {
      [key: string]: unknown;
  } | null;
    /** A custom action value that will be accessible from user task events resulting from this endpoint invocation. If not provided, it will default to "complete". */
    action?: string | null;
};

export interface UserTaskAssignmentRequest {
    /** The assignee for the user task. The assignee must not be empty or `null`. */
    assignee?: string;
    /** By default, the task is reassigned if it was already assigned. Set this to `false` to return an error in such cases. The task must then first be unassigned to be assigned again. Use this when you have users picking from group task queues to prevent race conditions. */
    allowOverride?: boolean | null;
    /** A custom action value that will be accessible from user task events resulting from this endpoint invocation. If not provided, it will default to "assign". */
    action?: string | null;
}

export type UserTaskUpdateRequest = {
    /**  JSON object with changed task attribute values.

 The following attributes can be adjusted with this endpoint, additional attributes
 will be ignored:

 * `candidateGroups` - reset by providing an empty list
 * `candidateUsers` - reset by providing an empty list
 * `dueDate` - reset by providing an empty String
 * `followUpDate` - reset by providing an empty String
 * `priority` - minimum 0, maximum 100, default 50

 Providing any of those attributes with a `null` value or omitting it preserves
 the persisted attribute's value.

 The assignee cannot be adjusted with this endpoint, use the Assign task endpoint.
 This ensures correct event emission for assignee changes. */
    changeset?: {
      /** The due date of the task. Reset by providing an empty String. */
      dueDate?: string | null;
      /** The follow-up date of the task. Reset by providing an empty String. */
      followUpDate?: string | null;
      /** The list of candidate users of the task. Reset by providing an empty list. */
      candidateUsers?: Array<string> | null;
      /** The list of candidate groups of the task. Reset by providing an empty list. */
      candidateGroups?: Array<string> | null;
      /** The priority of the task. */
      priority?: number | null;
      [key: string]: unknown;
  } | null;
    /** A custom action value that will be accessible from user task events resulting from this endpoint invocation. If not provided, it will default to "update". */
    action?: string | null;
};

/**  JSON object with changed task attribute values.
 * 
 *  The following attributes can be adjusted with this endpoint, additional attributes
 *  will be ignored:
 * 
 *  * `candidateGroups` - reset by providing an empty list
 *  * `candidateUsers` - reset by providing an empty list
 *  * `dueDate` - reset by providing an empty String
 *  * `followUpDate` - reset by providing an empty String
 *  * `priority` - minimum 0, maximum 100, default 50
 * 
 *  Providing any of those attributes with a `null` value or omitting it preserves
 *  the persisted attribute's value.
 * 
 *  The assignee cannot be adjusted with this endpoint, use the Assign task endpoint.
 *  This ensures correct event emission for assignee changes. */
export type Changeset = {
    /** The due date of the task. Reset by providing an empty String. */
    dueDate?: string | null;
    /** The follow-up date of the task. Reset by providing an empty String. */
    followUpDate?: string | null;
    /** The list of candidate users of the task. Reset by providing an empty list. */
    candidateUsers?: Array<string> | null;
    /** The list of candidate groups of the task. Reset by providing an empty list. */
    candidateGroups?: Array<string> | null;
    /** The priority of the task. */
    priority?: number | null;
    [key: string]: unknown;
} | null;

export interface UserTaskVariableSearchQuerySortRequest {
    /** The field to sort by. */
    field: "value" | "name" | "tenantId" | "variableKey" | "scopeKey" | "processInstanceKey";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** User task search query request. */
export type UserTaskVariableSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** User task search query request. */
export type UserTaskAuditLogSearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/**  The state of the user task.
 *  Note: FAILED state is only for legacy job-worker-based tasks. */
export type UserTaskStateEnum = "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED";

/** The user task variable search filters. */
export interface UserTaskVariableFilter {
    /** Name of the variable. */
    name?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
}

/** UserTaskStateEnum property with full advanced search capabilities. */
export type UserTaskStateFilterProperty = "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED" | {
    /** Checks for equality with the provided value. */
    $eq?: "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED";
    /** Checks for inequality with the provided value. */
    $neq?: "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
};

/** Advanced UserTaskStateEnum filter. */
export interface AdvancedUserTaskStateFilter {
    /** Checks for equality with the provided value. */
    $eq?: "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED";
    /** Checks for inequality with the provided value. */
    $neq?: "CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED";
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<"CREATING" | "CREATED" | "ASSIGNING" | "UPDATING" | "COMPLETING" | "COMPLETED" | "CANCELING" | "CANCELED" | "FAILED">;
    /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
    $like?: string;
}

/** The user task audit log search filters. */
export interface UserTaskAuditLogFilter {
    /** The audit log operation type search filter. */
    operationType?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE" | {
      /** Checks for equality with the provided value. */
      $eq?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";
      /** Checks for inequality with the provided value. */
      $neq?: "ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ASSIGN" | "CANCEL" | "COMPLETE" | "CREATE" | "DELETE" | "EVALUATE" | "MIGRATE" | "MODIFY" | "RESOLVE" | "RESUME" | "SUSPEND" | "UNASSIGN" | "UNKNOWN" | "UPDATE">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The audit log result search filter. */
    result?: "FAIL" | "SUCCESS" | {
      /** Checks for equality with the provided value. */
      $eq?: "FAIL" | "SUCCESS";
      /** Checks for inequality with the provided value. */
      $neq?: "FAIL" | "SUCCESS";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"FAIL" | "SUCCESS">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The audit log timestamp filter. */
    timestamp?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Greater than comparison with the provided value. */
      $gt?: string;
      /** Greater than or equal comparison with the provided value. */
      $gte?: string;
      /** Lower than comparison with the provided value. */
      $lt?: string;
      /** Lower than or equal comparison with the provided value. */
      $lte?: string;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
  };
    /** The actor type search filter. */
    actorType?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER" | {
      /** Checks for equality with the provided value. */
      $eq?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
      /** Checks for inequality with the provided value. */
      $neq?: "ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER";
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<"ANONYMOUS" | "CLIENT" | "UNKNOWN" | "USER">;
      /**  Checks if the property matches the provided like value.

 Supported wildcard characters are:

 * `*`: matches zero, one, or multiple characters.
 * `?`: matches one, single character.

 Wildcard characters can be escaped with backslash, for instance: `\*`. */
      $like?: string;
  };
    /** The actor ID search filter. */
    actorId?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
}

export interface VariableSearchQuerySortRequest {
    /** The field to sort by. */
    field: "value" | "name" | "tenantId" | "variableKey" | "scopeKey" | "processInstanceKey";
    /** The order in which to sort the related field. */
    order?: "ASC" | "DESC";
}

/** Variable search query request. */
export type VariableSearchQuery = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Variable filter request. */
export interface VariableFilter {
    /** Name of the variable. */
    name?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
    value?: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
    /** Tenant ID of this variable. */
    tenantId?: string;
    /** Whether the value is truncated or not. */
    isTruncated?: boolean;
    /** The key for this variable. */
    variableKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /**  The key of the scope that defines where this variable is directly defined. This can be a
 process instance key (for process-level variables) or an element instance key (for local
 variables scoped to tasks, subprocesses, gateways, events, etc.). Use this filter to
 find variables directly defined in specific scopes. Note that this does not include
 variables from parent scopes that would be visible through the scope hierarchy. */
    scopeKey?: string | {
      /** Checks for equality with the provided value. */
      $eq?: string;
      /** Checks for inequality with the provided value. */
      $neq?: string;
      /** Checks if the current property exists. */
      $exists?: boolean;
      /** Checks if the property matches any of the provided values. */
      $in?: Array<string>;
      /** Checks if the property matches none of the provided values. */
      $notIn?: Array<string>;
  };
    /** The key of the process instance of this variable. */
    processInstanceKey?: unknown;
}

/** Variable search query response. */
export interface VariableSearchQueryResult {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

/** Variable search response item. */
export interface VariableSearchResult {
    /** Name of this variable. */
    name: string;
    /** Tenant ID of this variable. */
    tenantId: string;
    /** The key for this variable. */
    variableKey: string;
    /**  The key of the scope where this variable is directly defined. For process-level
 variables, this is the process instance key. For local variables, this is the key of the
 specific element instance (task, subprocess, gateway, event, etc.) where the variable is
 directly defined. */
    scopeKey: string;
    /** The key of the process instance of this variable. */
    processInstanceKey: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
}

/** Variable search response item. */
export interface VariableResult {
    /** Name of this variable. */
    name: string;
    /** Tenant ID of this variable. */
    tenantId: string;
    /** The key for this variable. */
    variableKey: string;
    /**  The key of the scope where this variable is directly defined. For process-level
 variables, this is the process instance key. For local variables, this is the key of the
 specific element instance (task, subprocess, gateway, event, etc.) where the variable is
 directly defined. */
    scopeKey: string;
    /** The key of the process instance of this variable. */
    processInstanceKey: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
}

/** Variable response item. */
export interface VariableResultBase {
    /** Name of this variable. */
    name: string;
    /** Tenant ID of this variable. */
    tenantId: string;
    /** The key for this variable. */
    variableKey: string;
    /**  The key of the scope where this variable is directly defined. For process-level
 variables, this is the process instance key. For local variables, this is the key of the
 specific element instance (task, subprocess, gateway, event, etc.) where the variable is
 directly defined. */
    scopeKey: string;
    /** The key of the process instance of this variable. */
    processInstanceKey: string;
    /**  The key of the root process instance. The root process instance is the top-level
 ancestor in the process instance hierarchy. This field is only present for data
 belonging to process instance hierarchies created in version 8.9 or later. */
    rootProcessInstanceKey: string;
}

export interface VariableValueFilterProperty {
    /** Name of the variable. */
    name: string;
    /**  The value of the variable.
 Variable values in filters need to be in serialized JSON format. For example, a variable
 with string value `myValue` can be found with the filter value `"myValue"`. Consider
 appropriate escaping for special characters in JSON strings when constructing filter values. */
    value: string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};
}

export interface SetVariableRequest {
    /** JSON object representing the variables to set in the element’s scope. */
    variables: {
      [key: string]: unknown;
  };
    /**  If set to `true`, the variables are merged strictly into the local scope (as specified
 by the `elementInstanceKey`). Otherwise, the variables are propagated to upper scopes
 and set at the outermost one.

 Let's consider the following example:
 There are two scopes '1' and '2'. Scope '1' is the parent scope of '2'. The effective
 variables of the scopes are:
 1 => { "foo" : 2 }
 2 => { "bar" : 1 }

 An update request with elementInstanceKey as '2', variables { "foo": 5 }, and local set
 to `true` leaves scope '1' unchanged and adjusts scope '2' to { "bar": 1, "foo": 5 }. By
 default, with local set to `false`, scope '1' will be { "foo": 5 } and scope '2' will be
 { "bar": 1 }. */
    local?: boolean;
    /**  A reference key chosen by the user that will be part of all records resulting from this operation.
 Must be > 0 if provided. */
    operationReference?: number;
}

/** Zeebe Engine resource key (Java long serialized as string) */
export type LongKey = string;

/** System-generated key for a process instance. */
export type ProcessInstanceKey = string;

/** System-generated key for a deployed process definition. */
export type ProcessDefinitionKey = string;

/** System-generated key for a element instance. */
export type ElementInstanceKey = string;

/** System-generated key for a user task. */
export type UserTaskKey = string;

/** System-generated key for a deployed form. */
export type FormKey = string;

/** System-generated key for a variable. */
export type VariableKey = string;

/**  System-generated key for a scope. A scope can hold variables and represents either an
 *  element instance in a BPMN process or the process instance itself. */
export type ScopeKey = string;

/** System-generated key for a incident. */
export type IncidentKey = string;

/** System-generated key for a job. */
export type JobKey = string;

/** System-generated key for a decision definition. */
export type DecisionDefinitionKey = string;

/** System-generated key for a decision evaluation instance. */
export type DecisionEvaluationInstanceKey = string;

/** System-generated key for a decision evaluation. */
export type DecisionEvaluationKey = string;

/** System-generated key for a deployed decision requirements definition. */
export type DecisionRequirementsKey = string;

/** System-generated key for a deployed decision instance. */
export type DecisionInstanceKey = string;

/** System-generated key for an batch operation. */
export type BatchOperationKey = string;

/**  A reference key chosen by the user that will be part of all records resulting from this operation.
 *  Must be > 0 if provided. */
export type OperationReference = number;

/** System-generated key for an audit log entry. */
export type AuditLogKey = string;

/** ProcessDefinitionKey property with full advanced search capabilities. */
export type ProcessDefinitionKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced ProcessDefinitionKey filter. */
export interface AdvancedProcessDefinitionKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** ProcessInstanceKey property with full advanced search capabilities. */
export type ProcessInstanceKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced ProcessInstanceKey filter. */
export interface AdvancedProcessInstanceKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** ElementInstanceKey property with full advanced search capabilities. */
export type ElementInstanceKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced ElementInstanceKey filter. */
export interface AdvancedElementInstanceKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** JobKey property with full advanced search capabilities. */
export type JobKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced JobKey filter. */
export interface AdvancedJobKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** DecisionDefinitionKey property with full advanced search capabilities. */
export type DecisionDefinitionKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced DecisionDefinitionKey filter. */
export interface AdvancedDecisionDefinitionKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/**  ScopeKey property with full advanced search capabilities. Filter by the key of the
 *  element instance or process instance that defines the scope of a variable. */
export type ScopeKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced ScopeKey filter. */
export interface AdvancedScopeKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** VariableKey property with full advanced search capabilities. */
export type VariableKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced VariableKey filter. */
export interface AdvancedVariableKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** DecisionEvaluationInstanceKey property with full advanced search capabilities. */
export type DecisionEvaluationInstanceKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced DecisionEvaluationInstanceKey filter. */
export interface AdvancedDecisionEvaluationInstanceKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** AuditLogKey property with full advanced search capabilities. */
export type AuditLogKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced AuditLogKey filter. */
export interface AdvancedAuditLogKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** FormKey property with full advanced search capabilities. */
export type FormKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced FormKey filter. */
export interface AdvancedFormKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** DecisionEvaluationKey property with full advanced search capabilities. */
export type DecisionEvaluationKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced DecisionEvaluationKey filter. */
export interface AdvancedDecisionEvaluationKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** DecisionRequirementsKey property with full advanced search capabilities. */
export type DecisionRequirementsKeyFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** Advanced DecisionRequirementsKey filter. */
export interface AdvancedDecisionRequirementsKeyFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** A Problem detail object as described in [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457). There may be additional properties specific to the problem type. */
export interface ProblemDetail {
    /** A URI identifying the problem type. */
    "type": string;
    /** A summary of the problem type. */
    title: string;
    /** The HTTP status code for this problem. */
    status: number;
    /** An explanation of the problem in more detail. */
    detail: string;
    /** A URI path identifying the origin of the problem. */
    instance: string;
}

/** Id of a process definition, from the model. Only ids of process definitions that are deployed are useful. */
export type ProcessDefinitionId = string;

/** The model-defined id of an element. */
export type ElementId = string;

/** The user-defined id for the form */
export type FormId = string;

/** Id of a decision definition, from the model. Only ids of decision definitions that are deployed are useful. */
export type DecisionDefinitionId = string;

/** The user-defined id for the global listener */
export type GlobalListenerId = string;

/** The unique identifier of the tenant. */
export type TenantId = string;

/** The unique name of a user. */
export type Username = string;

/** A tag. Needs to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
export type Tag = string;

/** List of tags. Tags need to start with a letter; then alphanumerics, `_`, `-`, `:`, or `.`; length ≤ 100. */
export type TagSet = Array<string>;

/**  An optional, user-defined string identifier that identifies the process instance
 *  within the scope of a process definition (scoped by tenant). If provided and uniqueness
 *  enforcement is enabled, the engine will reject creation if another root process instance
 *  with the same business id is already active for the same process definition.
 *  Note that any active child process instances with the same business id are not taken into account. */
export type BusinessId = string;

export type SearchQueryRequest = {
    /** Pagination criteria. */
    page?: {
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** The index of items to start searching from. */
      from?: number;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `endCursor` value from the previous response to fetch the next page of results. */
      after: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  } | {
      /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
      before: string;
      /** The maximum number of items to return in one request. */
      limit?: number;
  };
};

/** Pagination criteria. Can use offset-based pagination (from/limit) OR cursor-based pagination (after/before + limit), but not both. */
export type SearchQueryPageRequest = {
    /** The maximum number of items to return in one request. */
    limit?: number;
} | {
    /** The index of items to start searching from. */
    from?: number;
    /** The maximum number of items to return in one request. */
    limit?: number;
} | {
    /** Use the `endCursor` value from the previous response to fetch the next page of results. */
    after: string;
    /** The maximum number of items to return in one request. */
    limit?: number;
} | {
    /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
    before: string;
    /** The maximum number of items to return in one request. */
    limit?: number;
};

export interface LimitPagination {
    /** The maximum number of items to return in one request. */
    limit?: number;
}

export interface OffsetPagination {
    /** The index of items to start searching from. */
    from?: number;
    /** The maximum number of items to return in one request. */
    limit?: number;
}

export interface CursorForwardPagination {
    /** Use the `endCursor` value from the previous response to fetch the next page of results. */
    after: string;
    /** The maximum number of items to return in one request. */
    limit?: number;
}

export interface CursorBackwardPagination {
    /** Use the `startCursor` value from the previous response to fetch the previous page of results. */
    before: string;
    /** The maximum number of items to return in one request. */
    limit?: number;
}

export interface SearchQueryResponse {
    /** Pagination information about the search results. */
    page: {
      /** Total items matching the criteria. */
      totalItems: number;
      /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
      hasMoreTotalItems: boolean;
      /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
      startCursor: string;
      /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
      endCursor: string;
  };
}

/** The order in which to sort the related field. */
export type SortOrderEnum = "ASC" | "DESC";

/** Pagination information about the search results. */
export interface SearchQueryPageResponse {
    /** Total items matching the criteria. */
    totalItems: number;
    /**  Indicates whether there are more items matching the criteria beyond the returned items.
 This is useful for determining if additional requests are needed to retrieve all results. */
    hasMoreTotalItems: boolean;
    /** The cursor value for getting the previous page of results. Use this in the `before` field of an ensuing request. */
    startCursor: string;
    /** The cursor value for getting the next page of results. Use this in the `after` field of an ensuing request. */
    endCursor: string;
}

/**  Checks if the property matches the provided like value.
 * 
 *  Supported wildcard characters are:
 * 
 *  * `*`: matches zero, one, or multiple characters.
 *  * `?`: matches one, single character.
 * 
 *  Wildcard characters can be escaped with backslash, for instance: `\*`. */
export type LikeFilter = string;

/** Basic advanced string filter. */
export interface BasicStringFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
}

/** Advanced string filter. */
export interface AdvancedStringFilter {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
}

/** String property with basic advanced search capabilities. */
export type BasicStringFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
    /** Checks if the property matches none of the provided values. */
    $notIn?: Array<string>;
};

/** String property with full advanced search capabilities. */
export type StringFilterProperty = string | {
  $eq?: string;
  $neq?: string;
  $exists?: boolean;
  $in?: Array<string>;
  $notIn?: Array<string>;
  $like?: string;
};

/** Advanced integer (int32) filter. */
export interface AdvancedIntegerFilter {
    /** Checks for equality with the provided value. */
    $eq?: number;
    /** Checks for inequality with the provided value. */
    $neq?: number;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Greater than comparison with the provided value. */
    $gt?: number;
    /** Greater than or equal comparison with the provided value. */
    $gte?: number;
    /** Lower than comparison with the provided value. */
    $lt?: number;
    /** Lower than or equal comparison with the provided value. */
    $lte?: number;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<number>;
}

/** Integer property with advanced search capabilities. */
export type IntegerFilterProperty = number | {
    /** Checks for equality with the provided value. */
    $eq?: number;
    /** Checks for inequality with the provided value. */
    $neq?: number;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Greater than comparison with the provided value. */
    $gt?: number;
    /** Greater than or equal comparison with the provided value. */
    $gte?: number;
    /** Lower than comparison with the provided value. */
    $lt?: number;
    /** Lower than or equal comparison with the provided value. */
    $lte?: number;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<number>;
};

/** Advanced date-time filter. */
export interface AdvancedDateTimeFilter {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Greater than comparison with the provided value. */
    $gt?: string;
    /** Greater than or equal comparison with the provided value. */
    $gte?: string;
    /** Lower than comparison with the provided value. */
    $lt?: string;
    /** Lower than or equal comparison with the provided value. */
    $lte?: string;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
}

/** Date-time property with full advanced search capabilities. */
export type DateTimeFilterProperty = string | {
    /** Checks for equality with the provided value. */
    $eq?: string;
    /** Checks for inequality with the provided value. */
    $neq?: string;
    /** Checks if the current property exists. */
    $exists?: boolean;
    /** Greater than comparison with the provided value. */
    $gt?: string;
    /** Greater than or equal comparison with the provided value. */
    $gte?: string;
    /** Lower than comparison with the provided value. */
    $lt?: string;
    /** Lower than or equal comparison with the provided value. */
    $lte?: string;
    /** Checks if the property matches any of the provided values. */
    $in?: Array<string>;
};

export interface FormResult {
    /** The tenant ID of the form. */
    tenantId?: string;
    /** The user-provided identifier of the form. */
    formId?: string;
    /** The form content. */
    schema?: Record<string, unknown>;
    /** The version of the the deployed form. */
    version?: number;
    /** The assigned key, which acts as a unique identifier for this form. */
    formKey?: string;
}

/** The start cursor in a search query result set. */
export type StartCursor = string;

/** The end cursor in a search query result set. */
export type EndCursor = string;
